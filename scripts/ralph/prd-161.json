{
  "project": "Automatos AI Platform — PRD-161 Board Execution Engine (Remediation Chain Block A)",
  "branchName": "ralph/prd-161-board-engine",
  "description": "Branch from main after Night-1 — re-verify refs (PRD-154 S4 landed dispatch-on-assign + priority fix). One Postgres-native dispatch spine: claim/lease/requeue so assigned work executes exactly once, fails honestly, retries deliberately. BINDING: Q39 assignment=immediate dispatch (per-workspace auto_start override), Q40 dispatcher independent of heartbeats, Q41 2 retries then failed, Q44 reject→same agent with feedback, D8 UI in studio board-tab, PRD-09 auth constraint. Full spec docs/PRDS/PRD-161-BOARD-EXECUTION-ENGINE.md.",
  "userStories": [
    {
      "id": "P161-S1",
      "title": "Claim loop: FOR UPDATE SKIP LOCKED + LISTEN/NOTIFY + lease columns",
      "description": "Extend board_tasks (NO new table) with lease_until + attempts; per-worker claim loop using FOR UPDATE SKIP LOCKED; pg_notify on create/assign wakes claimants, poll fallback. No fcntl leader election for pickup.",
      "acceptanceCriteria": [
        "4 workers × 50 tasks → each executed exactly once (concurrency test)",
        "Lease expiry requeues attempts+1, never silently closes done (crash test)",
        "assign→claim under 1s in-process (NOTIFY test)",
        "Reversible migration, single alembic head; cd orchestrator && pytest -q green"
      ],
      "files": ["orchestrator/api/board_tasks.py", "orchestrator/alembic/"]
    },
    {
      "id": "P161-S2",
      "title": "One real execution per task; delete heartbeat fold-in",
      "description": "DELETE the heartbeat 3-tasks-into-1-prompt batching; each claimed task runs the existing execution path individually with guardrail validation; results land as typed activity entries on the task, not blob text.",
      "acceptanceCriteria": [
        "Fold-in code deleted (no shim)",
        "Per-task result + typed activity schema (test)",
        "tests/test_board_task_handlers.py updated; test_board_sdk_auth.py untouched green",
        "cd orchestrator && pytest -q green"
      ],
      "files": ["orchestrator/services/heartbeat_service.py", "orchestrator/api/board_tasks.py"]
    },
    {
      "id": "P161-S3",
      "title": "Honest lifecycle: failed state, rejection feedback, stale sweeper",
      "description": "Terminal 'failed' state + task_failed notification (today failures close as done+error). Reject returns the task to the SAME agent as 'assigned' with reviewer feedback in context (Q44). Sweeper requeues stale leases and flags unresponsive agents (ack-deadline badge).",
      "acceptanceCriteria": [
        "State-machine test: assigned→in_progress→{done|failed|rejected→assigned}",
        "Studio board shows failed column + unresponsive badge — verify in browser using dev-browser skill",
        "task_failed notification emitted (test)",
        "cd orchestrator && pytest -q green"
      ],
      "files": ["orchestrator/api/board_tasks.py", "frontend/components/command-center/board-tab.tsx"]
    },
    {
      "id": "P161-S4",
      "title": "Throughput controls: slots, double-texting, to_thread, StuckDetector",
      "description": "Per-agent concurrency slots (config, default 2) + queue-don't-drop double-texting policy; asyncio.to_thread on sync Composio calls inside task execution (loop must never block other claims); port the OpenHands StuckDetector pattern (same-action-loop breaker) writing a task_learning memory when PRD-159 is merged, else logging.",
      "acceptanceCriteria": [
        "5 tasks one agent → ≤2 concurrent, rest queued (test)",
        "Sync tool call doesn't stall other claims (event-loop test)",
        "Induced identical-failing-action loop breaks within bounds (test)",
        "cd orchestrator && pytest -q green"
      ],
      "files": ["orchestrator/api/board_tasks.py", "orchestrator/config.py"]
    },
    {
      "id": "P161-S5",
      "title": "SLA wiring + board polish + SSE events",
      "description": "Wire the dead sla_deadline column: set on agent-created tasks, breach scan in the sweeper, indicator chips in studio board. Run-Now button. Remove the no-heartbeat warning (dispatcher makes it moot). Archive done > N days (config). SSE board events behind the PRD-09 TASKS_READ narrow dep (read-only — shared auth untouched).",
      "acceptanceCriteria": [
        "Overdue task flagged + notified (breach test)",
        "Run Now dispatches immediately — verify in browser using dev-browser skill",
        "SSE auth tests green incl. SDK-key read path",
        "cd orchestrator && pytest -q green; typecheck passes"
      ],
      "files": ["orchestrator/api/board_tasks.py", "frontend/components/command-center/board-tab.tsx"]
    }
  ]
}
