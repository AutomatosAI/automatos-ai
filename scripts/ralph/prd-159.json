{
  "project": "Automatos AI Platform — PRD-159 Memory Quality & Lifecycle (Remediation Chain Block A)",
  "branchName": "ralph/prd-159-memory-quality",
  "description": "Branch from main after Night-1 — re-verify refs. BINDING D5: orchestrator owns extraction (single cheap-model distill, operational taxonomy); mem0 = storage/search/dedup only. Q9 priority: tool outcomes → task learnings → playbook patterns → user/business facts. Q10: PRD-131d operational-exclusion DELETED. Q11: week-scale L2, mission/task memories don't decay until consolidated. PROTECTED: recipe step-communication suite (20 tests) + Composio hint suite (25 tests) must stay green untouched. Full spec docs/PRDS/PRD-159-MEMORY-QUALITY-LIFECYCLE.md.",
  "userStories": [
    {
      "id": "P159-S1",
      "title": "Operational distill prompt emitting typed facts; junk paths deleted",
      "description": "Rewrite the distill prompt (smart_memory.py:578-613): DELETE the 'Do NOT record transient interaction events' exclusion; emit typed {fact, type, importance} over taxonomy {tool_outcome, task_learning, playbook_pattern, user_fact, business_fact, preference, procedure}. Route the distiller to the cheap model tier via create_llm_manager config. DELETE the raw-exchange fallback (smart_memory.py:456-462) — on distill failure store NOTHING and log (that fallback is the 'user said hello' source).",
      "acceptanceCriteria": [
        "Golden suite: 12 transcript fixtures → expected typed facts (strict on type+presence, fuzzy on wording)",
        "'user said hello' class fixtures produce zero writes",
        "Distiller model-tier assertion test",
        "Existing distill tests updated; cd orchestrator && pytest -q green"
      ],
      "files": ["orchestrator/modules/memory/smart_memory.py"]
    },
    {
      "id": "P159-S2",
      "title": "Tool-execution outcome capture",
      "description": "Post-execution hook in the unified executor: failures and notable successes (new channel IDs, auth quirks, rate limits, schema surprises) become typed tool_outcome memories (workspace namespace, infer:false, content-hash dedup). Noise gate: trivial successes don't write. PROTECTED: hint-service suite untouched.",
      "acceptanceCriteria": [
        "Failed Composio call → tool_outcome memory with app+action+error class (test)",
        "Identical outcome twice → one row (dedup test)",
        "Trivial success → no write (test)",
        "Hint suite (25) green; cd orchestrator && pytest -q green"
      ],
      "files": ["orchestrator/consumers/chatbot/unified_executor.py", "orchestrator/modules/memory/"]
    },
    {
      "id": "P159-S3",
      "title": "Recall that fires: operational types, recipe namespaces, relevance floor, ordered logs",
      "description": "Default recall includes L2 + recipe/workflow namespaces (memory_stats.py:66-101, smart_memory.py:213-230, context_router.py:489-509): semantic search over operational types WITHOUT the temporal-regex gate; relevance floor applied server-side; daily logs ordered+paginated (mem0_client.py:466-496 sends size/sort — today reads an arbitrary first-50).",
      "acceptanceCriteria": [
        "'why did the deploy mission fail?' recalls a seeded task_failure (integration test)",
        "Playbook learnings appear in Explorer and recall outside their own playbook (test)",
        "Sub-floor junk never injected (test)",
        "Recipe suite (20) green; cd orchestrator && pytest -q green"
      ],
      "files": ["orchestrator/api/memory_stats.py", "orchestrator/modules/memory/smart_memory.py", "orchestrator/modules/memory/context_router.py", "orchestrator/modules/memory/storage/mem0_client.py"]
    },
    {
      "id": "P159-S4",
      "title": "Consolidation replaces decay-deletion; dead session machinery removed",
      "description": "Sleep-time consolidation job (message-count/idle triggered, replacing the dead cron + never-called end_session path): merge near-duplicates, contradiction-based invalidation (recency+confidence) as the PRIMARY lifecycle instead of time-decay, promote stable L2→L3, archive losers. DELETE the dead L1 session machinery (end_session, SessionMemory.decisions/action_items with no writers) — no shims.",
      "acceptanceCriteria": [
        "5 near-dups → 1 canonical with provenance (test)",
        "Newer contradicting fact supersedes; older archived with reason (test)",
        "Accessed L2 promotes to L3 via the job (test)",
        "Dead paths deleted; cd orchestrator && pytest -q green"
      ],
      "files": ["orchestrator/modules/memory/unified_memory_service.py", "orchestrator/jobs/"]
    },
    {
      "id": "P159-S5",
      "title": "Honest events + Explorer truth (fork sync fixes)",
      "description": "memory_stored SSE only after persistence with the real tier (service.py:1099-1107 + smart_orchestrator.py:384-434 race). In the automatos-mem0 fork: process UPDATE/DELETE events into the SQL view (memories.py:432-433 is ADD-only — ghost rows in Explorer); stop replacing orchestrator metadata with {source_app, mcp_client} (memories.py:418-421) so tier/category/provenance are filterable in vector search. Remove the 'both'-tier double-write default (single namespace + explicit agent-tier triggers; keyword '#'/'@' misrouting dies).",
      "acceptanceCriteria": [
        "SSE: zero-fact distill emits no memory_stored; tier matches persisted (test)",
        "Fork: UPDATE/DELETE reflected in SQL view (test)",
        "Vector search filters by tier (test)",
        "New writes produce no Explorer duplicates (test); cd orchestrator && pytest -q green"
      ],
      "files": ["orchestrator/consumers/chatbot/service.py", "orchestrator/modules/memory/smart_orchestrator.py", "../automatos-mem0/openmemory/api/app/routers/memories.py"]
    },
    {
      "id": "P159-S6",
      "title": "mem0 V3 fork upgrade + patch-set documentation",
      "description": "Upgrade the automatos-mem0 fork to the mem0 V3 baseline so the S5 patches + PRD-156 auth dependency ride a current base. Document the full patch set in the fork README as fork-maintenance notes.",
      "acceptanceCriteria": [
        "Fork tests green on V3; orchestrator integration suite green against the new image",
        "Patch-set doc in automatos-mem0 README"
      ],
      "files": ["../automatos-mem0/"]
    }
  ]
}
