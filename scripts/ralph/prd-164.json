{
  "project": "Automatos AI Platform — PRD-164 Planning Intelligence & Integration Seams (Remediation Chain PAYOFF)",
  "branchName": "ralph/prd-164-planning-intelligence",
  "description": "Branch from main ONLY after PRD-157, 159, 163, 166 are ALL merged — acceptance must FAIL honestly if read-side dependencies are missing (PRD-153 rule, no shims). The platform starts to FLOW: planners consult RAG+memory+graph+roster, agent selection goes semantic, outputs feed back into knowledge (D6), chat renders platform actions, recurring agents learn. BINDING: Q58 flywheel ON by default (workspace opt-out), Q60 heartbeat agents get memory, Q61 one planning ContextService mode, Q62 TOOL_WIDGET_MAP deleted, Q21 blended agent matching, Q22 field digest replaces 8K stuffing. PROTECTED: recipe (20) + hint (25) suites. Full spec docs/PRDS/PRD-164-PLANNING-INTELLIGENCE-SEAMS.md.",
  "userStories": [
    {
      "id": "P164-S1",
      "title": "Planning Context Pack consumed by all three planners",
      "description": "One ContextService 'planning' mode injecting: RAG retrieval on the goal (PRD-157 path), top-k mission summaries + task failures (PRD-159 recall), KG subgraph (PRD-165 endpoint), roster + performance. Consumed by MissionPlanner (planner.py:699 area), board plan_task (board_tasks.py:824 area), and AutoBrain — no parallel context assemblies.",
      "acceptanceCriteria": [
        "Golden test: seeded prior failure visibly changes a new plan DONE — tests/test_prd164_planning_pack.py::TestGoldenLearningDemo: seeded task_failure recalled via PRD-159 path flips MissionPlanner.decompose from the scrape plan to the API plan; failure text provably reaches the prompt only when seeded, with the pack's avoid-failures instruction",
        "All three planners consume the pack (grep gate: no parallel assembly) DONE — TestOneAssemblerGrepGate: planner.py/board_tasks.py/auto.py all call build_planning_context; exactly one assembler def (modules/context/service.py); planners contain no direct RAG/memory/graph source calls; pack RAG section goes through RAGService.retrieve -> build_retrieval_filters (PRD-157 choke point); plus live consumption tests for AutoBrain Tier-3 and board plan_task",
        "Pack respects token budget on oversized fixtures (test) DONE — TestPackBudget: apply_pack_budget (PRD-157 select_within_budget + truncate_to_token_budget) and the full assembler stay <= budget on ~36k-token fixtures; priority-2 planning_history survives pressure; algorithm verified locally via stubbed smoke run (no pytest on this machine)",
        "cd orchestrator && pytest -q green DONE — CI test.yml run 27449696553 SUCCESS on branch tip d8351235a (full orchestrator suite, real Postgres); no local DB on the build machine, changed files py_compile green locally"
      ],
      "files": ["orchestrator/modules/context_service/", "orchestrator/modules/coordination/planner.py", "orchestrator/api/board_tasks.py"]
    },
    {
      "id": "P164-S2",
      "title": "Semantic agent matching with reasons",
      "description": "AgentMatcher: embedded capability cards (Qdrant) + historical task performance from mission memory + live field signal (PRD-166) → ranked agents with reasons; explicit agent_overrides always win (PRD-163 S4). One embedding call per dispatch accepted (Q21).",
      "acceptanceCriteria": [
        "Golden matrix: 10 task fixtures → expected top agent (ties allowed) DONE — tests/test_prd164_agent_match.py::test_golden_matrix_top_agent drives the pure _rank_with_context seam with 10 fixtures (lexical role, semantic-card lift, no-flip on small semantic edge, tools, history, field tie-break, busy penalty, override); every ranked agent asserted to carry a reason; scoring hand-verified locally via stubbed smoke run (22 checks green — no pytest in this venv, file runs on CI)",
        "Override always wins (test) DONE — test_override_outranks_higher_scorer_and_is_flagged (PRD-163 S4 name-pinned agent ranks first with a provably LOWER blended score than #2) + test_override_wins_even_below_match_threshold (match() returns the override even under MATCH_THRESHOLD) + inactive-agent negative; planner capability-vocabulary behavior untouched (CANONICAL_ROLES path unchanged)",
        "Match reasons logged on the task and visible to the approval card DONE — every MatchResult carries a composed reason; dispatch persists input_context.agent_match {agent, score, reason, is_override, decided_at} + TASK_ASSIGNED payload gains match_reason; plan-time preview (_annotate_match_previews in _run_planning AND import_plan) mirrors match_agent/match_reason into run.plan → _plan_task_summary → mission_approval card payload (tests/test_prd164_match_reasons_surface.py) and the card RENDERS them (vitest mission-approval-match-reason.test.tsx 2/2; tsc clean on touched files, 540 errors = pre-existing baseline)",
        "cd orchestrator && pytest -q green DEFERRED — CI/morning check: test.yml on this push is the authority (no DB/Qdrant on this machine); locally py_compile green on all changed files, frontend vitest 77/77, signal paths fail-open + timeout-bounded so missing backends degrade to the legacy lexical score exactly (renormalization test)"
      ],
      "files": ["orchestrator/modules/coordination/", "orchestrator/modules/agents/"]
    },
    {
      "id": "P164-S3",
      "title": "Output flywheel: agent outputs become retrievable knowledge",
      "description": "Mission syntheses, generated documents, and submitted reports route through the EXISTING ingestion manager (chunked, searchable, graph-extracted) tagged source_type='agent_output'; KG incremental build learns the three source types it currently drops; deliverables get list/get agent tools + a mission-page tab; mission completion can emit generate_document (feature-flag if PRD-167 unmerged). Per-workspace opt-out honored (Q58).",
      "acceptanceCriteria": [
        "Completed mission's synthesis retrievable via RAG next turn (e2e test)",
        "Seeded report's entities appear in the KG (test)",
        "Opt-out workspace ingests nothing (test)",
        "Auto lists its own deliverables via tool (reachability + integration test)",
        "cd orchestrator && pytest -q green"
      ],
      "files": ["orchestrator/modules/rag/ingestion/manager.py", "orchestrator/modules/coordination/reconciler.py", "orchestrator/modules/tools/discovery/"]
    },
    {
      "id": "P164-S4",
      "title": "Field-digest dispatch + bounded replanning",
      "description": "Replace the 8K-char upstream-output stuffing with the PRD-166 field digest under a per-task budget (Q22); bounded replanning (LLMCompiler-style joiner) + progress ledger/stall counter (Magentic-One pattern) on mission execution with audit trail.",
      "acceptanceCriteria": [
        "Dispatch prompt size drops ≥60% on the multi-task fixture; golden task still passes",
        "Induced stall triggers replan-or-halt within bounds + audit (test)",
        "cd orchestrator && pytest -q green"
      ],
      "files": ["orchestrator/modules/coordination/coordinator_service.py"]
    },
    {
      "id": "P164-S5",
      "title": "Chat renders the platform; heartbeat agents learn",
      "description": "DELETE dead TOOL_WIDGET_MAP (frontend/components/widgets/router.ts — zero callers, stale names; Q62). Live widget routing for: mission cards (163 base), board tasks, deliverables, documents, schedule, memory writes — widget keys validated against the tool registry in the PRD-155 reachability test so name drift is impossible. Heartbeat agents get scoped memory recall + write hooks (Q60 — statelessness by design ends, modes.py:74-87 area).",
      "acceptanceCriteria": [
        "Each listed tool result renders its widget — verify in browser using dev-browser skill",
        "Widget-key drift fails reachability test (negative fixture)",
        "Heartbeat run recalls its previous run's memory (test)",
        "Hint suite (25) + recipe suite (20) green; cd orchestrator && pytest -q green; typecheck passes"
      ],
      "files": ["frontend/components/widgets/router.ts", "orchestrator/modules/agents/modes.py"]
    }
  ]
}
