{
  "project": "Automatos AI Platform — PRD-154 Wave-0 Quick Wins (Remediation Chain Night 1, first)",
  "branchName": "ralph/prd-154-wave0-quick-wins",
  "description": "Twelve independent stories fixing verified one-to-three-file breakages from the platform deep review (reports/PLATFORM_DEEP_REVIEW_2026-06.md §2; full spec docs/PRDS/PRD-154-WAVE0-QUICK-WINS.md with BINDING amendments). Branch from main. Rules: no shims, delete what you replace, no new tables/tools, no os.getenv outside config.py, no hardcoded values. Update tests that assert the old broken behavior — never skip/delete them. Chain testing policy: docs/PRDS/PRD-CHAIN-2026-06-REMEDIATION.md.",
  "userStories": [
    {
      "id": "P154-S1",
      "title": "RAG: batched full-chunk hydration consumed by the formatter",
      "description": "modules/rag/service.py:855-909 already hydrates full chunk text into candidate['expanded_content'] via one asyncpg query PER candidate, and NOTHING reads it — service.py:544 and :754 read only c['content'] (the 500-char S3 metadata preview written by ingestion/manager.py:1300). Replace the N+1 with ONE batched IN-query over parent chunk ids, and make the retrieval/format path consume the hydrated text so result_formatter.py's 4000-char per-doc budget (:354-356) actually fills. Do not change chunking or S3 writes.",
      "acceptanceCriteria": [
        "DONE — _expand_to_parent_context joins (doc_id,chunk_index) keys against document_chunks via unnest; read-sites service.py:545 & :756 now consume expanded_content. test_hydration_is_a_single_batched_query + consume tests assert hydrated text >500 chars reaches chunks.",
        "DONE — single unnest($1::int[],$2::int[]) round-trip replaces the N+1; test asserts conn.fetch.await_count == 1 for 3 candidates across 2 docs.",
        "DONE — added tests/test_rag_hydration.py (4 tests: batched-query, fallback-to-preview, basic_retrieval consume, optimizer consume). No existing formatter test asserted the old 500-char truncation (4000-char budget already shipped PRD-136 in modules/tools/formatting/result_formatter.py:356), so none required updating; story files path 'modules/rag/result_formatter.py' was stale.",
        "DONE — cd orchestrator && pytest -q: 2061 passed, 16 skipped, 1 xfailed."
      ],
      "files": ["orchestrator/modules/rag/service.py", "orchestrator/modules/rag/result_formatter.py", "orchestrator/tests/test_rag_hydration.py"]
    },
    {
      "id": "P154-S2",
      "title": "RAG: team filter fails closed; fix team-access PATCH SQL",
      "description": "service.py:391-393 except-branch returns ALL candidates unfiltered on DB error (fail-open security hole). Fail closed: on filter error return only candidates with empty team_access (public) and log error-level. Separately, api/documents.py:1871-1924 PATCH/bulk team-access SQL references documents.title and documents.updated_at which DO NOT EXIST in the model (core/models/core.py:548-580) — fix the SQL to the real columns (BINDING Q1: fail-closed).",
      "acceptanceCriteria": [
        "DONE — _filter_by_team except-branch now returns ONLY candidates with empty metadata.team_access (public) and logs at error level. test_filter_by_team_fails_closed_on_db_error forces SessionLocal to raise and asserts the restricted doc ('b', team_access=['finance']) is never returned.",
        "DONE — PATCH & bulk SQL fixed to real columns: dropped phantom 'updated_at = NOW()', RETURNING/response use 'filename' (Document has no title/updated_at). test_team_access_sql_targets_real_columns (deterministic, model-introspection + source guard) + @integration test_team_access_endpoints_execute_against_real_schema (runs the real endpoint SQL against CI Postgres; bogus ids -> 404/updated:0, proving every column plans against the live schema; skips with no DB).",
        "DONE — cd orchestrator && pytest -q: 2064 passed, 17 skipped, 1 xfailed."
      ],
      "files": ["orchestrator/modules/rag/service.py", "orchestrator/api/documents.py", "orchestrator/tests/test_prd154_s2_team_access.py"]
    },
    {
      "id": "P154-S3",
      "title": "Memory: infer:false for curated writes, real bulk delete, week-scale decay, touch-on-recall",
      "description": "Four root causes of memory junk/loss (report §2.2): (1) curated writers (distilled facts, mission/playbook/widget/tool stores via mem0_client.py:312-376) omit infer so mem0 re-extracts them with its personal-info prompt — send infer:false on every already-curated write. (2) mem0_client.py:498-506 calls DELETE /api/v1/memories/{id}/ which doesn't exist on the server (only bulk DELETE per mem0_openapi.json) — every delete 405s and trips the circuit breaker; switch to the bulk endpoint. (3) MEMORY_DECAY_RATE=0.1/hr + threshold 0.3 (config.py:104,106) archives importance-0.8 memories in ~15h — retune to week-scale via config. (4) touch_short_term has ZERO callers so L2→L3 promotion (unified_memory_service.py:1990-1996) can never fire — call it on recall.",
      "acceptanceCriteria": [
        "DONE — add() gained infer:bool=True; all 4 curated L3 writers (store_long_term, store_long_term_messages, store_daily_log, store_two_tier) pass infer=False. test_unified_memory: 4 behavior tests assert mem0.add called with infer=False + test_all_curated_writers_grep_infer_false greps the source.",
        "DONE — delete() now hits bulk DELETE /memories/ with {memory_ids,user_id} body (no per-id /memories/{id}/ 405). test_delete_uses_bulk_endpoint asserts URL+method+body; test_delete_three_times_keeps_breaker_closed asserts breaker.failures==0 after 3 deletes.",
        "DONE — config MEMORY_DECAY_RATE 0.1->0.004/hr (week-scale). test_prd154_s3_decay_config (real config) asserts importance-0.8 retention>=threshold at 168h AND the old 0.1 rate would archive it; TestDecayFormula rewritten to week-scale.",
        "DONE — search_short_term now awaits touch_short_term for each recalled row (best-effort gather). test_recall_touches_each_short_term_result asserts both result ids touched.",
        "DONE — updated test_unified_memory.py (decay formula + delete signature) and test_mem0_async_client.py; handlers_search.py passes workspace_id to delete_memory (bulk endpoint needs user_id). cd orchestrator && pytest -q: 2074 passed, 17 skipped, 1 xfailed."
      ],
      "files": ["orchestrator/modules/memory/integrations/mem0_client.py", "orchestrator/config.py", "orchestrator/modules/memory/unified_memory_service.py", "orchestrator/modules/tools/discovery/handlers_search.py", "orchestrator/tests/test_unified_memory.py", "orchestrator/tests/test_mem0_async_client.py", "orchestrator/tests/test_prd154_s3_decay_config.py"]
    },
    {
      "id": "P154-S4",
      "title": "Board: assignment dispatches execution; fix priority CASE",
      "description": "Assigning a task starts nothing today — _launch_task_execution exists (api/board_tasks.py:724) but is never called on assignment; pickup waits for an opt-in heartbeat most agents lack. Call it from create-with-assignee and the assign endpoint; guard double-fire for already-running tasks and skip recipe-mirror tasks. Also fix the broken priority CASE expression in services/heartbeat_service.py:919. PRD-09 constraint: do NOT touch shared hybrid auth or test_board_sdk_auth.py behavior.",
      "acceptanceCriteria": [
        "DONE — create_task + update_task(assign endpoint) now call _dispatch_on_assign, which flips the task to in_progress and fires _launch_task_execution inline. test_create_with_assignee_dispatches_execution + test_assign_endpoint_dispatches_execution (mock executor) assert exactly 1 launch same-tick with the right agent_id and status=='in_progress'.",
        "DONE — _dispatch_on_assign self-guards via _should_dispatch_on_assign (only status=='assigned'); an in_progress task is skipped. test_reassign_running_task_does_not_double_fire patches the executor and asserts zero launches when re-assigning a running task (+ predicate test_should_dispatch_skips_already_running).",
        "DONE — replaced alphabetical BoardTask.priority.desc() (urgent,medium,low,high) with data-driven _board_task_priority_order() CASE from _BOARD_TASK_PRIORITY_RANK {urgent:0,high:1,medium:2,low:3}, ORDER BY rank ASC. test_priority_rank_is_semantic_not_alphabetical asserts urgent>high>medium>low and != the broken alpha sort; test_assigned_task_order_uses_priority_case_not_string_desc asserts a compiled CASE over all four priorities; source guard asserts .priority.desc() is gone.",
        "DONE — added 11 tests to test_board_task_handlers.py (4 predicate, 4 dispatch-integration incl. recipe-mirror skip, 3 priority-CASE). test_board_sdk_auth.py untouched (not in diff) and green in the full run; PRD-09 hybrid auth not modified.",
        "DONE — cd orchestrator && python3 -m pytest -q: 2085 passed, 17 skipped, 1 xfailed (was 2074 + 11 new)."
      ],
      "files": ["orchestrator/api/board_tasks.py", "orchestrator/services/heartbeat_service.py", "orchestrator/tests/test_board_task_handlers.py"]
    },
    {
      "id": "P154-S5",
      "title": "Missions: fix platform_get_mission, context_messages, duplicate route, honest create reply",
      "description": "handlers_missions.py: platform_get_mission crashes every call — int() cast on a UUID PK (:96), reads t.output which doesn't exist, and a wrong schema string; fix all three. Auto-created missions lose chat context: only the UI suggestion-card sets context_messages (mission-suggestion-card.tsx:48) — the executor path (handlers_missions.py:12-30) must attach recent conversation too, and chat.tsx:870 must read message parts (current extraction broken). Delete the byte-duplicate /resume route. Document auto_approve in the mission-create tool schema and make the create reply state the awaiting_approval status and how to approve (no false 'mission started').",
      "acceptanceCriteria": [
        "DONE — get_mission no longer int()-casts the UUID PK (coerces to UUID, clean error on malformed id), reads t.output (real col) not t.result, and t.failure_detail not t.error_message. test_get_mission_returns_seeded_mission_by_uuid asserts a UUID id returns the seeded run with task output+failure surfaced; test_get_mission_invalid_uuid_returns_clean_error_not_crash; source guard test_get_mission_source_uses_real_columns. Verified OrchestrationRun/Task PKs are UUID and Task cols are output/failure_detail (no result/error_message).",
        "DONE — create_mission now attaches recent workspace chat as config.context_messages with source='chat' (mirrors handlers_search parts→text; honours explicit chat_id; never clobbers caller-supplied context). chat.tsx (real path components/chatbot/chat.tsx:870 & :1195) now extracts text from m.parts (was m.content=undefined under AI-SDK v5), mirroring the canonical :573-576 extraction. test_auto_created_mission_carries_recent_chat_as_context_messages + _explicit_..._not_overwritten + _without_chat_history_omits_context.",
        "DONE — deleted the byte-duplicate POST /{mission_id}/resume (the PRD-123 checkpoint variant at api/missions.py:1820, shadowed dead code — FastAPI keeps the first registration at :1532, the canonical paused→resume w/ budget bump). No orphaned imports (Query/Optional still used). test_only_one_resume_route_registered asserts exactly one @router.post(\"/{mission_id}/resume\").",
        "DONE — create reply now states the real state via _create_reply_message (awaiting_approval → 'awaiting your approval, review and approve to begin'; running → 'now running (auto-approved)') + an explicit awaiting_approval bool; no false 'execute automatically' on the default path. auto_approve documented in the platform_create_mission config schema; platform_get_mission mission_id schema fixed integer→string (UUID). test_create_reply_states_awaiting_approval_not_auto_started + _running_when_auto_approved + _create_tool_schema_documents_auto_approve + _get_tool_schema_mission_id_is_string_not_integer.",
        "DONE — recipe suite untouched (not in diff). cd orchestrator && python3 -m pytest -q: 2097 passed, 17 skipped, 1 xfailed (was 2085 + 12 new S5 tests). Frontend NOTE: chat.tsx edit adds ZERO new tsc errors (only pre-existing errors at :77-79 remain; my :870/:1195 edits are clean); the branch's 714 frontend tsc errors are pre-existing platform tech-debt, unrelated to S5, and not gated by CI (test.yml runs orchestrator pytest only)."
      ],
      "files": ["orchestrator/modules/tools/discovery/handlers_missions.py", "frontend/components/chatbot/chat.tsx", "orchestrator/modules/tools/discovery/actions_missions.py", "orchestrator/api/missions.py", "orchestrator/tests/test_prd154_s5_missions.py"]
    },
    {
      "id": "P154-S6",
      "title": "Knowledge Graph: export community+confidence, idOf guards, deterministic palette, communities tool rewire",
      "description": "_export_graph drops node.community and confidence — the UI's default community coloring is broken until exported (backfill confidence where null). Add the idOf() null-guard at the 4 frontend crash sites. Type→color becomes a deterministic hash palette defined as data (BINDING Q26; no hardcoded hex per design rules). Point platform_graph_communities at DbWorkspaceClient (currently wired to a backend that returns nothing for DB-backed workspaces).",
      "acceptanceCriteria": [
        "DONE — GraphifyService._annotate_export_nodes attaches community (reverse-mapped from the communities {cid:[node_ids]} dict) + a backfilled confidence (_DEFAULT_NODE_CONFIDENCE='EXTRACTED' where a merged target arrived without one) onto every node; _export_graph calls it before writing graph.json (nx.node_link_data carries only node attrs, so community/confidence never reached the node and the UI's default community colouring read undefined). test_prd154_s6_graph TestExportNodeAnnotation (5): community on each node, confidence preserved+backfilled, null-community key for unclustered, immutable (no input mutation), source-wiring pin.",
        "DONE — extracted frontend/components/knowledge/graph-viz-utils.ts: idOf() null-guards link-endpoint id extraction (typeof null==='object' no longer throws) and colorForType/colorForCommunity are a deterministic hash over GRAPH_PALETTE defined as DATA (BINDING Q26, no per-type hex). Applied idOf at the 4 BusinessGraphVisualization crash sites (data link-filter, degree map, focusNeighbourhood, renderData). Panel legend swatch now reads colorForType so it matches the node fill (removed 5 hardcoded type hexes from TYPE_DISPLAY). vitest __tests__/graph-viz-utils.test.ts (11): idOf returns id/undefined and never throws on null; same-type-same-color determinism; palette-bound; community modulo-wrap; neutral fallback.",
        "DONE — handle_graph_communities now reads graph/communities.json via core.graph_storage.DbWorkspaceClient (the Postgres workspace_graphs store _export_graph WROTE it to) instead of core.workspace_client.WorkspaceClient (file/S3 backend that returns nothing for a DB-backed workspace). test_prd154_s6_graph TestCommunitiesHandlerUsesDbStore (3): returns 2 communities for a DB-backed ws (recording fake), specific community_id lookup round-trips, source guard pins DbWorkspaceClient and bans the WorkspaceClient import.",
        "DONE — cd orchestrator && python3 -m pytest -q: 2105 passed, 17 skipped, 1 xfailed (2097 + 8 new S6 backend tests). Frontend: npx tsc --noEmit holds at the branch's 714 PRE-EXISTING errors (zero new; touched files BusinessGraphVisualization/BusinessGraphPanel/graph-viz-utils all clean) and npm run test green (19 passed incl. 11 new). NOTE: the 714 tsc errors are platform-wide tech-debt unrelated to S6 and NOT CI-gated (test.yml runs orchestrator pytest only); next lint is env-blocked locally (Node 18.17.0 < 18.18.0)."
      ],
      "files": ["orchestrator/modules/", "frontend/components/knowledge/"]
    },
    {
      "id": "P154-S7",
      "title": "Field memory: render the graph (react-force-graph), archive-don't-destroy, agent_id fix",
      "description": "The 3D field graph has NEVER rendered: @react-three/fiber v9 peer-requires React 19, app is React 18.2 — <Canvas> crashes at mount (mission-field-viz.tsx:532) with no ErrorBoundary. Replace the R3F stack with react-force-graph-3d wrapped in an ErrorBoundary falling back to react-force-graph-2d; REMOVE @react-three/fiber and drei from package.json (delete the losing stack). Stop destroying fields at mission end: _cleanup_terminal_fields (coordinator_service.py:711-737) marks archived/expired_at instead of deleting Qdrant data and popping field_id (BINDING D7 stepping stone). handlers_field.py must read params['_agent_id'] so field tools work for agents.",
      "acceptanceCriteria": [
        "DEFERRED — morning browser check: open a live AND a completed mission's Field tab; confirm the 3D force graph renders the central 'Shared Field' hub + one node per agent + pattern nodes (archived ones dimmed). Deterministic proxy DONE: patternsToFieldGraph (new mission-field-viz-utils.ts) builds {nodes,links} — test_renders nodes+edges for a live AND a completed (archived) mission asserts core+2 agents+3 patterns, 5 links, archived flag+dimmed colour. Headless loop cannot run dev-browser; fix implemented fully.",
        "DONE — extracted FieldVizErrorBoundary (field-viz-error-boundary.tsx, fallback prop) wraps ForceGraph3D with a ForceGraph2D fallback in mission-field-viz.tsx. vitest __tests__/field-viz-error-boundary.test.tsx (2): a throwing 3D child renders the 2D fallback; no-throw renders the primary. The old raw <Canvas> (@react-three/fiber v9 peer-requires React 19, app is React 18) crashed at mount with NO boundary.",
        "DONE — _cleanup_terminal_fields (services/coordinator_service.py) now stamps field_archived+field_expired_at and KEEPS field_id + Qdrant data (single shared field_memory collection, no per-mission teardown) instead of destroy+pop; orphan reaper still sweeps purged runs. Removed now-orphan _destroy_mission_field. test_prd154_s7_field: archives_not_destroys (field_id retained, _get_field never called), archived_field_stays_queryable, skips_already_archived (idempotent), source guard bans destroy/pop.",
        "DONE — handlers_field._actor_agent_id reads params['_agent_id'] (the executor's actor convention; executor calls handler(db,ws,params) with empty kwargs so the old kwargs.get('agent_id',0) was always 0), coerces safely, falls back to 0. Applied in field_query+field_inject. test_prd154_s7_field: inject/query read _agent_id (==7/==3), coerce string '5'->5, bad value->0, absent->0, all success:True (no 400).",
        "DONE — package.json: removed @react-three/fiber, @react-three/drei, @react-three/postprocessing (the dead R3F stack — imported nowhere after the rewrite); added react-force-graph-3d ^1.29.1 (react-force-graph-2d already present, used by S6 KG viz). Grep gate: fiber+drei absent. Typecheck: npx tsc --noEmit holds at 713 errors (≈ branch's 714 pre-existing baseline) with ZERO in any touched file (mission-field-viz/field-viz-error-boundary/mission-field-panel); the 713 are platform-wide pre-existing tech-debt, NOT CI-gated (test.yml runs orchestrator pytest only; frontend Dockerfile uses `npm install --legacy-peer-deps`, not npm ci). vitest 29 passed (4 files, incl. 10 new). cd orchestrator && python3 -m pytest -q: 2114 passed, 17 skipped, 1 xfailed (2105 + 9 new S7). NOTE: lockfiles (package-lock/yarn) left at HEAD — both are pre-existingly drifted (npm ci fails on the committed state) and the prod build reconciles via npm install --legacy-peer-deps; committing the ~2000-line reconciliation would be unrelated drift cleanup, out of S7 scope."
      ],
      "files": ["frontend/components/missions/mission-field-viz.tsx", "frontend/components/missions/mission-field-viz-utils.ts", "frontend/components/missions/field-viz-error-boundary.tsx", "frontend/package.json", "orchestrator/services/coordinator_service.py", "orchestrator/modules/tools/discovery/handlers_field.py", "orchestrator/tests/test_prd154_s7_field.py"]
    },
    {
      "id": "P154-S8",
      "title": "CodeGraph: semantic search routes to the working path; results carry path:line",
      "description": "search_type='semantic' currently routes to a broken path while a working pgvector path exists — route to it, forward the dropped params, remove the <50-char content filter hiding most hits, reconcile the query-log schema mismatch and make logging non-fatal (a log failure must never fail the search). Results emit path:line and symbol signature so agents can act on them.",
      "acceptanceCriteria": [
        "Semantic search returns results on a seeded index; params forwarded (test)",
        "Result snapshot contains path:line + signature",
        "Induced query-log failure does not fail search (test)",
        "cd orchestrator && pytest -q green"
      ],
      "files": ["orchestrator/modules/codegraph/codegraph_service.py", "orchestrator/api/codegraph.py"]
    },
    {
      "id": "P154-S9",
      "title": "One toast system: mount sonner, codemod the rest away",
      "description": "providers.tsx:91 mounts only react-hot-toast's Toaster while 49 files call sonner and 21 use shadcn use-toast — those toasts NEVER render (mission approve/reject feedback, template save errors, field-op results all silently dropped). BINDING Q73: sonner wins. Mount sonner's <Toaster/>, codemod react-hot-toast (17 files) and use-toast (21 files + duplicated store) call sites to sonner, remove both losing systems from package.json and the tree.",
      "acceptanceCriteria": [
        "Mission approve/reject and template-save-error toasts visibly render — verify in browser using dev-browser skill",
        "Zero call sites of react-hot-toast or use-toast remain (grep); both removed from package.json",
        "Typecheck and lint pass"
      ],
      "files": ["frontend/app/providers.tsx", "frontend/components/", "frontend/package.json"]
    },
    {
      "id": "P154-S10",
      "title": "Honest pilot surfaces: ticker, fake stats, global search, skills API, fetch ban",
      "description": "BINDING D10 (pilots see Studio). (1) StudioTicker fabricates live ops metrics with a LIVE dot (studio-ticker.tsx:39-47, mounted main-layout.tsx:104) — wire to the real analytics endpoints already serving command-center, or remove the component. (2) Remove hardcoded 85.5% agent performance (api/agents.py:331-336), fake Studio tab counts (studio-menu.ts:88-96), fake green validation checkmarks. (3) Global search calls wrong paths and swallows errors (use-global-search.ts:43,59) — fix to real routes, surface errors. (4) Agent skill create/remove targets a dead pre-v1 API (api-client.ts:1895-1941) — migrate to /api/v1/skills like the page's workspace-skills-tab already does. (5) Add an ESLint rule banning raw fetch('/api') so components must use apiClient.",
      "acceptanceCriteria": [
        "No string-literal metrics/counts in studio chrome (grep gate listed literals)",
        "Global search returns live results for a seeded agent — verify in browser using dev-browser skill",
        "Skill add/remove round-trips against /api/v1/skills (test)",
        "ESLint fails on raw fetch('/api' (rule fixture test)",
        "Typecheck passes; cd orchestrator && pytest -q green"
      ],
      "files": ["frontend/components/studio/studio-ticker.tsx", "frontend/config/studio-menu.ts", "frontend/hooks/use-global-search.ts", "frontend/lib/api-client.ts", "orchestrator/api/agents.py"]
    },
    {
      "id": "P154-S11",
      "title": "Calendar stopgap: retry on inactive scheduler + request timeout",
      "description": "Root cause (1-of-4-workers APScheduler state) is fixed properly in PRD-162. Stopgap so the calendar stops caching empty results: a get_schedule response with scheduler_active=false becomes a thrown error so React Query retries instead of rendering an empty calendar from a 200; add AbortSignal.timeout(15000) and a visible error state with retry button in CalendarTab.",
      "acceptanceCriteria": [
        "vitest: inactive-scheduler response triggers retry, not empty render",
        "Timeout shows error state with retry — verify in browser using dev-browser skill",
        "Typecheck passes"
      ],
      "files": ["frontend/components/command-center/calendar-tab.tsx"]
    },
    {
      "id": "P154-S12",
      "title": "NL2SQL tab revival + safety trims + entity-KG gate + token strip",
      "description": "(1) Three Databases tabs use raw fetch with no auth/baseUrl and 404 in split-origin deploys — migrate DatabaseQueryAnalytics.tsx:25-27, SemanticLayerBuilder.tsx:125,161, QueryTemplatesGrid.tsx:90,136 to apiClient (revives the Training tab instantly). (2) Honor the dialect field in generation. (3) On failure return a structured error — never an executable error-string SELECT. (4) Auth-gate the legacy entity-KG endpoints (api/knowledge_graph.py:84-559, currently unauthenticated cross-tenant) pending their PRD-165 deletion. (5) Strip the GitHub token from clone error messages (codegraph_service.py:489).",
      "acceptanceCriteria": [
        "All six Databases sub-tabs load data in split-origin dev — verify in browser using dev-browser skill",
        "Failure path returns no executable sql field (test)",
        "Entity-KG endpoints 401 without auth (test)",
        "Clone error with bad token contains no token substring (test)",
        "Typecheck passes; cd orchestrator && pytest -q green"
      ],
      "files": ["frontend/components/knowledge/DatabaseQueryAnalytics.tsx", "frontend/components/knowledge/SemanticLayerBuilder.tsx", "frontend/components/knowledge/QueryTemplatesGrid.tsx", "orchestrator/api/knowledge_graph.py", "orchestrator/modules/codegraph/codegraph_service.py"]
    }
  ]
}
