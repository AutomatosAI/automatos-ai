{
  "project": "Automatos AI Platform — PRD-167 Document Templates Block Editor (Remediation Chain net-new track)",
  "branchName": "ralph/prd-167-template-editor",
  "description": "Branch from main after PRD-156 merges (rides its Jinja2 sandbox + IDOR fixes). BINDING D2: Notion-style block editor, variable chips from profiles/workspace/brand, brand kit, live preview — JSON stays the storage format, rendering to PDF (WeasyPrint) and DOCX (python-docx, Q71). S1 settles the editor library (research debt: BlockNote vs Plate vs Puck never got its OSS pass). Full spec docs/PRDS/PRD-167-DOC-TEMPLATES-BLOCK-EDITOR.md.",
  "userStories": [
    {
      "id": "P167-S1",
      "title": "Editor library evaluation (time-boxed spike)",
      "description": "Evaluate BlockNote vs Plate vs Puck against: custom variable-chip inline node, controlled JSON output, React 18 compat, MIT/Apache license (open-core), table + image blocks. Decision memo with scored matrix in the PR. Loser dependencies never enter package.json; spike artifacts deleted.",
      "acceptanceCriteria": [
        "Decision memo committed with scored matrix",
        "Only the winning library in package.json"
      ],
      "files": ["docs/PRDS/PRD-167-DOC-TEMPLATES-BLOCK-EDITOR.md"]
    },
    {
      "id": "P167-S2",
      "title": "Block schema on existing tables + seed migration",
      "description": "blocks JSONB column on the EXISTING template tables (no new table — repo rule). Block types: heading, text, table, image/logo, variable, page-break, section. Strict validation with field-level errors (no silent swallow). Migrate all seed templates legacy-JSON → blocks; after parity, DELETE the legacy-only render path.",
      "acceptanceCriteria": [
        "Malformed blocks rejected with field-level errors (validation suite)",
        "Seed templates migrated; golden PDFs byte-comparable pre/post",
        "Reversible migration, single alembic head; cd orchestrator && pytest -q green"
      ],
      "files": ["orchestrator/modules/documents/template_service.py", "orchestrator/modules/documents/seed_templates.py", "orchestrator/alembic/"]
    },
    {
      "id": "P167-S3",
      "title": "Variable resolution service + catalog endpoint",
      "description": "{{user.*}} / {{company.*}} / {{brand.*}} / {{date.*}} resolver; GET /api/documents/variables drives editor chips with sample values; unresolved variables produce a render-time error list, never silent blanks; resolution uses the requesting user's profile, auth-provider-agnostic (PRD-150).",
      "acceptanceCriteria": [
        "Resolver unit suite incl. missing-field policy",
        "Catalog endpoint returns full set with samples (test)",
        "cd orchestrator && pytest -q green"
      ],
      "files": ["orchestrator/modules/documents/"]
    },
    {
      "id": "P167-S4",
      "title": "Workspace brand kit",
      "description": "Brand kit per workspace: logo upload (platform storage path), colors, fonts → exposed as {{brand.*}} and applied to PDF/DOCX styles. REPLACE all hardcoded Automatos branding in render paths.",
      "acceptanceCriteria": [
        "Branded PDF golden test (logo embedded, palette applied)",
        "Grep gate: no hardcoded brand strings/colors in renderers",
        "cd orchestrator && pytest -q green"
      ],
      "files": ["orchestrator/modules/documents/", "frontend/components/settings/"]
    },
    {
      "id": "P167-S5",
      "title": "Editor + live preview + gallery",
      "description": "Block editor page (S1 winner) with variable-chip insertion and brand-kit image/logo block; live preview pane (debounced render); template gallery with global starters, copy-on-customize, thumbnails. DOCX render via python-docx from the same block tree (Q71).",
      "acceptanceCriteria": [
        "Non-technical flow: create → variables → logo → preview → save → render PDF+DOCX — verify in browser using dev-browser skill",
        "Inline validation errors render — verify in browser using dev-browser skill",
        "vitest ≥80% on block-tree transform module; typecheck passes"
      ],
      "files": ["frontend/components/workspace/TemplateGallery.tsx", "frontend/app/deliverables/"]
    },
    {
      "id": "P167-S6",
      "title": "Agent tools + deliverable registration",
      "description": "platform_list_templates / platform_get_template_schema (3-file pattern); generate_document(template_id, data) produces a deliverable with source attribution (feature-flag the PRD-164 flywheel hook if unmerged). Rendered docs register as deliverables. SSTI fixtures from PRD-156 re-run against the editor path.",
      "acceptanceCriteria": [
        "Auto fills a template from chat; deliverable appears (integration test + dev-browser verify)",
        "Reachability green; SSTI fixtures inert on the new path",
        "cd orchestrator && pytest -q green"
      ],
      "files": ["orchestrator/modules/tools/discovery/", "orchestrator/modules/documents/"]
    }
  ]
}
