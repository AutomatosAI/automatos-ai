{
 "project": "Automatos AI Platform — PRD-225 Agent Questions: ASK ME + Telegram (Auto-as-Manager wave, chain 4/6)",
 "branchName": "ralph/prd-225-ask-me",
 "description": "STACKED branch: ralph/prd-225-ask-me ← ralph/prd-226-manager-doctrine (chain 4/6; 227 wiring + 224 ticket lane/watches + 226 doctrine are IN your tree; diff scope = merge-base vs the 226 branch). Spec seeded at docs/PRDS/PRD-225-AGENT-QUESTIONS-ASK-ME.md; wave map docs/PRDS/PRD-WAVE-AUTO-MANAGER.md. SCOPE = questions as task state: extend approval_grants into an asks model (THE WAVE'S ONLY MIGRATION — exactly one new alembic revision, single head), platform_ask_human park-don't-wait tool, the answer→resume path reusing grant machinery, the Command Center Questions tab with the blocked-cascade, the Telegram answer bridge, and the per-channel ingress trust gate. BAKED DECISIONS (Gerard 2026-08-27, from PRD-225 §8 proposals): dismiss keeps the subject BLOCKED (no fabricated answer; the asker may re-ask; an explicit 'use your judgment' answer is the unblock path — write that hint in the tab's dismiss tooltip); question_pending respects quiet hours EXCEPT when the ask transitively blocks ≥3 downstream tasks → urgent bypass; Telegram target = the workspace default chat (settings.integrations.telegram_default_chat_id — existing fallback in channels/sender.py:145-146). ANCHORS (verified 2026-08-27 — RE-VERIFY by grep): model orchestrator/core/models/approval_grants.py:58-123 (GrantStatus pending|granted|revoked|expired|denied :35-49; subjects SUBJECT_BOARD_TASK|SUBJECT_PLAYBOOK_RUN|SUBJECT_TOOL_CALL :53-55; hot index :97); resume machinery orchestrator/api/approval_grants.py:172-309 (_requeue_subject :172-198, _requeue_blocked_task :199-223, _resume_tool_call :224-309, _fail_subject :310+) — REUSE, never duplicate; grant creation + notify orchestrator/modules/tools/execution/tool_grants.py:372-426 and orchestrator/services/board_approval.py:70-237; board blocking pattern orchestrator/api/board_tasks.py:1036-1150 (blocked_reason format, fail-closed gate); dispatcher orchestrator/core/services/notification_dispatcher.py:45-255 (VALID_EVENT_TYPES :45-74, quiet hours :164-177 with urgent bypass, external delegation :212-214); Telegram driver orchestrator/channels/drivers/telegram.py:105-180; inbound webhook orchestrator/api/webhooks.py (platform detect :157-185, reply context :186-213 — Telegram chat_id+from_user :190-193, replay/dedup :450-470, routing via UniversalRouter, reply delivery _deliver_reply :231-286); channels model orchestrator/core/models/channels.py:25; sender orchestrator/channels/sender.py:111-220; frontend tab host frontend/components/command-center/command-center-shell.tsx:37-111 (TabKey :37-43, TABS :45-55, ?tab= :77-78, tabCounts :96-111, render switch :191-196); inbox pattern frontend/components/command-center/governance/approvals-inbox.tsx (+ its test governance-approvals-inbox.test.tsx); grant hooks frontend/hooks/use-approval-grants.ts + use-approval-grants-api.ts (DUPLICATES — see US-004 consolidation note); bell frontend/components/notifications/notification-bell.tsx linkFor (227's approval_grant/watch cases in tree); dependency edges for the cascade: BoardTask.parent_task_id + orchestrator/core/models/orchestration.py:292 OrchestrationTaskDependency. BINDING RULES: EXACTLY ONE new alembic revision on this branch (the approval_grants extension: kind VARCHAR default 'approval' | question_md TEXT | options JSONB nullable | answer_text TEXT | answered_by | asked_by_agent_id | channel_refs JSONB) and 'alembic heads' must equal 1 after it — a second revision or a rival head is a hard acceptance failure; NO new tables (extending approval_grants IS the design — house rule); trail-is-rows: re-asks are new rows against the same subject, NO qa-history JSONB; reuse _requeue_subject/_resume_tool_call for answer→resume — regression tests on approval-kind grant/deny/revoke MUST stay green untouched; ONE new route only (the answer endpoint) → RouterSpec if separately mounted (prefer the EXISTING approval-grants router — no new router file), regenerate + COMMIT orchestrator/reports/route-manifest.json with bumped route_count, matching frontend/lib/api-client.ts path (route-contract CI reads api-client.ts only); questions list = the EXISTING grants list route filtered by kind (no new list endpoint); dead vocabulary stays dead (no AWAITING_HUMAN writers); JSONB rebuild-don't-mutate (channel_refs, planning_data.human_qa); no os.getenv outside orchestrator/config.py; fixtures use obviously-fake tokens/secrets (gitleaks; public repo); canonical terms; PURE tests (@integration skips cleanly; real Postgres in CI per-story push); NEVER 'git add -A'/'git add .'/'-u' (node_modules untracked, NOT gitignored); never 'git stash -u'. Frontend gate: cd frontend && npm run -s test green after every frontend story.",
 "userStories": [
  {
   "id": "US-001",
   "title": "The asks model: extend approval_grants (THE wave migration)",
   "description": "One alembic revision extending approval_grants: kind VARCHAR(16) NOT NULL DEFAULT 'approval'; question_md TEXT NULL; options JSONB NULL; answer_text TEXT NULL; answered_by (match the existing actor column style — re-verify how granted_by/decided-by is typed at approval_grants.py:72-93); asked_by_agent_id (FK style matching agent_id); channel_refs JSONB NULL. Update the SQLAlchemy model + any pydantic schemas. Status vocabulary REUSED: pending=open ask, granted=answered, denied=dismissed, expired via existing expires_at. Existing behavior untouched: every existing approval-kind test green with zero edits. Downgrade path drops the columns cleanly.",
   "acceptanceCriteria": [
    "DONE — one new file orchestrator/alembic/versions/prd225_s1_asks_on_grants.py (down_revision=prd185_s1b_toollog_user_nullable), upgrade+downgrade both defined; test_prd225_asks_model.py::test_alembic_single_head asserts ScriptDirectory.get_heads()==1 (pure, no Postgres) + test_prd225_is_reachable_from_the_head walks the chain",
    "DONE — model gains kind/question_md/options/answer_text/answered_by/answered_at/asked_by_agent_id/channel_refs; kind default+server_default='approval' NOT NULL (test_kind_defaults_to_approval_and_is_not_null); 31 existing approval-grant tests green with zero edits (test_prd181_approval_grants + test_p2w2_grant_resume/tool_grant_consume/grants_oversight + test_grant_approve_commit_ordering + test_prd200_approval_renotify)",
    "DONE — cd orchestrator && python3 -m pytest -q tests/test_prd225_asks_model.py green (7 passed); regression slice 31 passed"
   ],
   "files": [
    "orchestrator/alembic/versions/",
    "orchestrator/core/models/approval_grants.py",
    "orchestrator/tests/"
   ]
  },
  {
   "id": "US-002",
   "title": "platform_ask_human — park, notify, return",
   "description": "3-file platform-tool pattern: platform_ask_human(subject_type, subject_id, question, options?, expires_hours?). Handler: validates the subject exists in the caller's workspace; parks it — board task → status 'blocked' + blocked_reason 'Awaiting human answer (ask #N)' + blocked_at (mirror the approval gate pattern at api/board_tasks.py:1074-1082); playbook_run/tool_call subjects follow the grant precedent (re-verify how tool_grants.py:372-426 parks a tool call and mirror it with kind='question'); inserts the question-kind row (question_md required, markdown; asked_by_agent_id from caller context — server-injected, never a tool param); dispatches NEW event type 'question_pending' through NotificationDispatcher (add to VALID_EVENT_TYPES; in_app + configured external destinations; link_type='question', link_id=row id); computes the downstream-blocked count (transitive dependents via parent_task_id + OrchestrationTaskDependency, cycle-safe) and marks the notification urgent when ≥3 (the baked quiet-hours bypass); RETURNS IMMEDIATELY with {ask_id, parked: true} — the tool never waits. Doctrine cross-ref: 226's skill already tells agents to park-and-move-on; this is the mechanism.",
   "acceptanceCriteria": [
    "DONE — 3-file pattern: actions_asks.py::register_asks_actions (wired in platform_actions.py), handlers_asks.py::ask_human, platform_executor map 'platform_ask_human': ask_human. Smoke: register_all_actions → 188 actions incl platform_ask_human (perm=write). Tests test_ask_human_registered_in_actions + test_ask_human_wired_into_executor_map",
    "DONE — board_task parks status='blocked' + blocked_reason='Awaiting human answer (ask #N)' + blocked_at (test_board_task_parks_with_exact_reason); playbook_run/tool_call park as the kind='question' grant row per grant precedent (parametrized test_non_board_subjects_park_as_a_row). create_grant extended with question kwargs (backward-compatible; 41 grant/dispatcher regression tests green)",
    "DONE — question_pending added to VALID_EVENT_TYPES (notification_dispatcher.py); dispatch fired link_type='question' link_id=ask_id (test_dispatch_fires_with_question_link, dispatcher monkeypatched); ≥3 board dependents → severity='urgent' (test_large_cascade_marks_urgent), <3 → None (test_small_cascade_is_not_urgent); services/ask_cascade.reachable_from cycle-safe (test_reachable_from_is_cycle_safe)",
    "DONE — handler has no sleep/while-True/poll (test_handler_never_waits greps source); asked_by_agent_id sourced from server-minted params['_agent_id'] only, spoofed tool-arg ignored (test_asked_by_agent_id_is_server_minted)",
    "DONE — cd orchestrator && python3 -m pytest -q tests/test_prd225_ask_human.py green (13 passed)"
   ],
   "files": [
    "orchestrator/modules/tools/discovery/",
    "orchestrator/core/services/notification_dispatcher.py",
    "orchestrator/tests/"
   ]
  },
  {
   "id": "US-003",
   "title": "Answer → resume: the one new route",
   "description": "POST /{grant_id}/answer on the EXISTING approval-grants router (api/approval_grants.py — NO new router file): body {answer_text} OR {option} (chosen from the row's options). Validates kind='question' + status pending; writes answer_text/answered_by/answered timestamp, flips to granted; appends {q: question_md, a: answer_text, answered_by, at} into the subject's execution context — board task planning_data.human_qa list (JSONB rebuild-don't-mutate) or the tool-call resume payload — then resumes via the EXISTING _requeue_subject/_resume_tool_call; confirms into chat via deliver_background_message ('Answered — <agent> resuming <task>', link to the subject). deny on a question-kind row = dismiss: no fabbed answer, subject STAYS blocked (baked decision), row status denied, trail intact. Existing grant/deny endpoints reject kind='question' for approve (answer is the only completion path) — with a clear error. ROUTE MANIFEST: regenerate + commit orchestrator/reports/route-manifest.json (hand-add + count bump per convention — scripts/dump_routes.py); add the api-client.ts call (route-contract CI reads api-client.ts only).",
   "acceptanceCriteria": [
    "DONE — POST /{grant_id}/answer on the EXISTING approval-grants router reuses _requeue_subject: board-task question re-queues the parked task (test_answer_requeues_board_task_and_records_qa: status→assigned, blocked_reason cleared) and tool_call runs _resume_tool_call (test_answer_resumes_tool_call); Q&A appended to planning_data.human_qa / details.human_qa rebuild-don't-mutate (test_answer_appends_to_existing_human_qa proves append not overwrite)",
    "DONE — deny on kind='question' is dismiss: subject stays BLOCKED, no fabricated answer, row DENIED, trail intact (test_dismiss_leaves_subject_blocked); grant_approval rejects question with 422 'answer via /answer' (test_grant_rejects_question); answer rejects approval-kind (test_answer_rejects_non_question); 44 approval-kind regression tests green untouched",
    "DONE — _confirm_answer_into_chat calls deliver_background_message with link_type='question' link_id=grant.id (test_answer_confirms_into_chat, monkeypatched)",
    "DONE — route-manifest.json hand-added POST /api/v1/approval-grants/{grant_id}/answer, count 767→768 (all pre-existing routes retained incl workflows/execute; local regen is env-partial so hand-add per convention); test_route_manifest.py green (3 passed); api-client.ts answerQuestion() added; node frontend/scripts/check-route-contract.js exit 0 (0 new violations)",
    "DONE — cd orchestrator && python3 -m pytest -q tests/test_prd225_answer_resume.py green (9 passed)"
   ],
   "files": [
    "orchestrator/api/approval_grants.py",
    "orchestrator/reports/route-manifest.json",
    "frontend/lib/api-client.ts",
    "orchestrator/tests/"
   ]
  },
  {
   "id": "US-004",
   "title": "The Questions tab — open asks + the blocked cascade",
   "description": "Command Center tab 'questions': one TABS entry + one tabCounts line (open question-kind count) + one component (command-center-shell.tsx:45-111 + render switch :191-196), following the ApprovalsInbox structure (governance/approvals-inbox.tsx + its test as the model). Card: question_md rendered as markdown (reuse the platform's existing markdown renderer — grep for the chat's), asker agent badge, subject link, answer textarea (⌘/Ctrl-Enter submits) + option buttons when options present, dismiss (tooltip notes 'answer \"use your judgment\" to unblock instead'), and THE CASCADE — downstream tasks transitively blocked behind this subject (parent_task_id + task dependency edges from the subject's mission where applicable; cycle-safe; show ≤6 with '+N more'). List reuses the EXISTING grants list endpoint filtered by kind=question — NO new list route. Data hooks: extend the EXISTING use-approval-grants hooks — and CONSOLIDATE the duplicate pair (use-approval-grants.ts vs use-approval-grants-api.ts, both live today — house rule no duplicate hooks: merge into one module, migrate both consumers ApprovalsInbox + ToolApprovalWidget, DELETE the loser in this same story). Bell: add case 'question' → /command-center?tab=questions (extends 227's linkFor cases + drift-guard fixture list).",
   "acceptanceCriteria": [
    "DONE — questions-tab.tsx renders open asks (useQuestions=pending+kind=question) newest-first, question_md via the shared chatMarkdownComponents renderer, asker badge + subject link, and the cascade ≤6 + '+N more' (questions-tab.test.tsx: markdown, order, cascade cap/overflow, cycle-fixture flat-list). Backend list enriches question rows via board_task_cascade_detail (test_prd225_questions_list.py: cap=6, total, cycle-safe)",
    "DONE — Answer posts to useAnswerQuestion→POST /answer and the card returns null (leaves list); ⌘/Ctrl-Enter + option buttons submit; Dismiss (useDenyApproval) flips the card to a trail note 'the asker may re-ask' with the question still visible (questions-tab.test.tsx: answer-leaves, keydown, option, dismiss-trail)",
    "DONE — shell tabCounts.questions = questions?.grants?.length; command-center-shell-questions.test.tsx asserts the Questions tab badges '3' for 3 open and no digit when 0",
    "DONE — use-approval-grants-api.ts git-rm'd; ONE use-approval-grants module remains; ToolApprovalWidget + its test migrated to it (mutateAsync(id) bare); grep: 0 real imports of the deleted path (only history comments)",
    "DONE — notification-bell linkFor gains case 'question' → /command-center?tab=questions; notification-bell-links.test.ts fixture + assertions + shell-tab cross-check updated for 'question'",
    "DONE — cd frontend && npm run -s test green (50 files, 294 tests); route-contract check exit 0 (0 new)"
   ],
   "files": [
    "frontend/components/command-center/command-center-shell.tsx",
    "frontend/components/command-center/questions-tab.tsx",
    "frontend/hooks/",
    "frontend/components/notifications/notification-bell.tsx",
    "frontend/"
   ]
  },
  {
   "id": "US-005",
   "title": "Telegram answer bridge — reply correlation + /answer fallback",
   "description": "Outbound: when question_pending dispatches to telegram, capture the sent message_id from the driver's SendResult (channels/drivers/telegram.py:105-154 — re-verify SendResult carries it; if not, extend the driver's return, all drivers unaffected) and write {telegram: {chat_id, message_id}} into the row's channel_refs (rebuild-don't-mutate; post-send, fail-soft — a capture failure leaves a working in-app question). Inbound (api/webhooks.py, BEFORE UniversalRouter routing): if the Telegram payload's reply_to_message_id matches a pending question's channel_refs → treat the text as the answer via US-003's internals (shared service function, not an HTTP self-call); else if the text matches '^/answer <id> <text>' → same path by id (workspace-scoped lookup; wrong-workspace or non-pending → polite reply, no info leak); else → existing routing untouched. Confirm back into the same Telegram thread via the existing _deliver_reply path. Replay/dedup guards (:450-470) apply unchanged.",
   "acceptanceCriteria": [
    "DONE — SendResult gains message_id+target (base.py); telegram driver populates message_id from the sendMessage result; handlers_asks._capture_question_telegram sends to the workspace default chat via send_to_channel and writes channel_refs.telegram={chat_id,message_id} rebuild-don't-mutate (test_outbound_captures_channel_refs); ok=False or a raise leaves channel_refs untouched, no error (test_outbound_capture_failure_degrades / _swallows_raise)",
    "DONE — webhooks._maybe_answer_question runs BEFORE UniversalRouter: a reply whose reply_to_message_id matches a pending question's channel_refs answers it via the SHARED apply_question_answer (not an HTTP self-call), resuming the subject (test_reply_to_correlated_message_answers); /answer <id> <text> works (test_slash_answer_command); unmatched + reply-to-unknown fall through to routing returning None (test_unmatched_message_falls_through / _reply_to_unknown_message_falls_through); 46 existing webhook regression tests green",
    "DONE — wrong-workspace /answer (test_wrong_workspace_answer_is_safe) and already-answered target (test_already_answered_target_is_safe) get a safe 'isn't open' reply and change nothing (only pending question-kind rows in-workspace are findable — no info leak)",
    "DONE — confirmation 'Answered #N — the agent is resuming.' delivered via the existing _deliver_reply path (test_reply_to_correlated_message_answers asserts one reply)",
    "DONE — fixtures use obviously-fake ids (chat 'c1', message_id '777', 'chat-9'); no bot tokens in tests (gitleaks-safe)",
    "DONE — cd orchestrator && python3 -m pytest -q tests/test_prd225_telegram_bridge.py green (9 passed); all 43 PRD-225 backend tests green together"
   ],
   "files": [
    "orchestrator/api/webhooks.py",
    "orchestrator/channels/drivers/telegram.py",
    "orchestrator/core/services/notification_dispatcher.py",
    "orchestrator/tests/"
   ]
  },
  {
   "id": "US-006",
   "title": "Ingress trust gate — strict | communication_only | allow_all per channel",
   "description": "Per-channel trigger_mode on the EXISTING channels model config (core/models/channels.py:25 — re-verify its settings/config JSONB and extend it, rebuild-don't-mutate; NO new table/column if a settings JSONB exists, else one nullable column rides US-001's migration — do NOT create a second revision: if a column is truly required, amend the US-001 revision BEFORE it ever ran in CI is not possible mid-chain, so prefer the JSONB; a second migration file is a hard failure). Modes: strict (DEFAULT — inbound directives are HELD: create a question-kind row 'Inbound directive awaiting approval' with the message as question_md, subject=the channel row, nothing executes; approving = answering with 'route it'), communication_only (conservative classifier: unconfident ⇒ directive ⇒ held; chatter routes), allow_all (current behavior). Correlated answers (US-005 reply/answer matches) BYPASS the gate — they are responses, not directives. Classifier: pure function, deliberately conservative, unit-tested on a fixture table of directive/chatter examples. Gate decisions logged (channel id, mode, verdict — no message body in logs). Channel CRUD API exposes trigger_mode read/update via the existing channels routes (no new route).",
   "acceptanceCriteria": [
    "DONE — webhooks._apply_trust_gate (runs BEFORE routing + platform-tool interception): strict holds a directive as a kind='question' subject_type='channel' row, returns held, nothing reaches UniversalRouter (test_strict_holds_directive_as_question); allow_all returns None → routes as today (test_allow_all_routes_everything); communication_only routes chatter, holds directives (test_communication_only_routes_chatter_holds_directive) over the classify fixture table (10 directive + 10 chatter examples in test_prd225_trust_gate.py)",
    "DONE — a reply to a correlated question under a STRICT channel is ANSWERED, not held (test_correlated_answer_bypasses_gate_under_strict): correlation (2c) returns before the gate (2d), and no 'channel' question row is created",
    "DONE — services/ingress_gate.trigger_mode_of defaults to 'strict' when unset/invalid; a channel with config={} holds even chatter (test_default_mode_is_strict); unknown mode fails safe to strict (test_should_hold_per_mode); no channel row ⇒ no gate, legacy inbound unchanged (test_no_channel_row_means_no_gate)",
    "DONE — gate logs '[trust-gate] ws=.. channel=.. platform=.. mode=.. verdict=..' — no message body (test_gate_log_has_no_message_body asserts the directive text is absent from all gate log lines via caplog)",
    "DONE — NO new migration: trigger_mode rides the channel config JSONB (with_trigger_mode rebuild-don't-mutate; api/channels.py update merges it without clobbering creds, list surfaces only trigger_mode not the whole config). Branch has exactly 1 migration (prd225_s1_asks_on_grants), alembic heads==1",
    "DONE — cd orchestrator && python3 -m pytest -q tests/test_prd225_trust_gate.py green (28 passed); 169 PRD-225 + webhook + channel-driver/sender regression tests green together"
   ],
   "files": [
    "orchestrator/api/webhooks.py",
    "orchestrator/core/models/channels.py",
    "orchestrator/api/channels.py",
    "orchestrator/tests/"
   ]
  },
  {
   "id": "P225-RVW-1",
   "title": "CRITICAL — Telegram answer path binds only (workspace,id); an unauthorized chat can inject answers that resume agent work",
   "description": "REVIEW FINDING (security-reviewer + manual, verified). webhooks.py:243-260 (_find_pending_question, _find_question_by_telegram_message) scope the answer lookup to _pending_questions(workspace_id) ONLY — neither compares the replying Telegram chat against the grant's stored channel_refs.telegram.chat_id (captured at handlers_asks.py:194-202). Telegram delivers EVERY update for the bot token to the one workspace webhook, so any user who can reach the bot (discoverable @username / shared group / ex-member) can (a) reply-collide on the per-chat-sequential reply_to_message_id, or (b) send '/answer <id> <text>' against the global auto-increment ApprovalGrant.id (core/models/approval_grants.py:69) with NO rate limit by default (main.py:865-866 registers SlowAPIMiddleware only when POLICY_PLANE_ENABLED, which config.py:789-798 defaults 'off'). apply_question_answer (api/approval_grants.py:269-294) then flips the grant to GRANTED, appends the attacker text verbatim into the subject's human_qa, and re-dispatches the parked board task (_requeue_blocked_task). Compounding: answered_by is built from reply_ctx['from_user'] = the sender's self-chosen Telegram first_name (webhooks.py:194,322), so the 'human' attribution is itself spoofable (e.g. 'telegram:CEO'). Blast radius is kind='question' rows IN-workspace (the URL webhook_key binds the workspace — NOT cross-workspace). Existing tests cover wrong-workspace and already-answered but NEVER same-workspace-different-chat — the case that matters.",
   "acceptanceCriteria": [
    "DONE — _find_question_by_telegram_message(db, ws, message_id, reply_chat_id) matches only when str(tg['message_id'])==str(message_id) AND str(tg['chat_id'])==str(reply_chat_id) via _telegram_ref; _maybe_answer_question passes reply_ctx.get('chat_id') into it (webhooks.py:252-320)",
    "DONE — /answer routes through _find_answerable_question: pending + in-workspace + str(channel_refs.telegram.chat_id)==str(sending chat) all required; any miss (wrong id / wrong ws / non-pending / non-delivery chat) returns None → the one 'isn't open' reply (test_slash_answer_from_non_delivery_chat_is_refused + test_wrong_workspace_answer_is_safe + test_already_answered_target_is_safe share the identical reason/text)",
    "DONE — _extract_reply_context captures ctx['from_id']=msg.from.id; _apply_telegram_answer builds answered_by='telegram:{from_id or chat_id or user}', never first_name (test_answered_by_carries_numeric_id: 'telegram:12345' with first_name='CEO'); verified target==numeric chat_id end-to-end (telegram.py:151 SendResult.target=str(target), sender.py:145-146 default=telegram_default_chat_id)",
    "DONE — 3 new tests in test_prd225_telegram_bridge.py: test_reply_from_other_chat_does_not_answer (c2→c1 reply falls through, unchanged), test_slash_answer_from_non_delivery_chat_is_refused (c2 /answer refused, unchanged), test_answered_by_carries_numeric_id; cd orchestrator && python3 -m pytest -q tests/test_prd225_telegram_bridge.py green (12 passed); PRD-225 + webhook regression 99 passed together"
   ],
   "files": [
    "orchestrator/api/webhooks.py",
    "orchestrator/tests/test_prd225_telegram_bridge.py"
   ]
  },
  {
   "id": "P225-RVW-2",
   "title": "CRITICAL — strict trust gate misses caption / Meta-WhatsApp / edited-message content the router still routes",
   "description": "REVIEW FINDING (security-reviewer, verified). webhooks.py:354-368 (_inbound_text) reads ONLY message.text, event.text, or top-level text|content|Body; :397-399 treats an empty result as 'nothing to hold (non-text update)' and returns None, so the message falls through to routing REGARDLESS of trigger_mode (including strict). But the router's own ingestor extracts MORE: core/routing/ingestors/webhook.py:54 (Telegram message.caption), :66-73 (Meta-WhatsApp entry[].changes[].value.messages[].text.body), :75-77 (json.dumps(body) fallback). So content the gate scores as empty still becomes envelope.content and reaches UniversalRouter / AutoBrain platform-tool interception (webhooks.py:747-791). Three verified strict-mode bypasses: (1) a connected Meta-WhatsApp channel fails to hold EVERY message (no text/event/Body key); (2) a Telegram photo/document with a caption; (3) a Telegram edited_message (body has 'edited_message', not 'message'). This directly defeats US-006's core guarantee that in strict NOTHING reaches the router for a directive. The trust-gate tests' _tg() helper only ever builds {'message':{'text':..}} — no caption/edited/WhatsApp shape is exercised.",
   "acceptanceCriteria": [
    "DONE — _inbound_text (webhooks.py:404) rewritten to mirror WebhookIngestor content extraction (referenced in its docstring): Telegram message.text/caption AND edited_message.text/caption, Slack event.text, Twilio Body, Meta-WhatsApp entry[].changes[].value.messages[].text.body; json.dumps fallback deliberately NOT mirrored so a text-less status callback still scores empty (test_inbound_text_matches_ingestor_shapes parametrizes caption/edited-text/edited-caption/whatsapp)",
    "DONE — under strict, each shape is HELD as a kind='question' subject_type='channel' row (returns trust_gate_hold, which short-circuits at webhooks.py:731-733 BEFORE the ingestor/router/AutoBrain interception): test_strict_holds_whatsapp_text_message, test_strict_holds_telegram_caption, test_strict_holds_telegram_edited_message",
    "DONE — 3 new hold tests + 1 parametrized extraction test in test_prd225_trust_gate.py cover all three shapes; cd orchestrator && python3 -m pytest -q tests/test_prd225_trust_gate.py green (35 passed); PRD-225 + webhook regression 106 passed together on a clean run (the lone composio dedup 'failure' was cross-run Redis SETNX state on fixed id wh_1 from repeated local runs — proven green on a cleared key, api/composio.py path untouched by this change)"
   ],
   "files": [
    "orchestrator/api/webhooks.py",
    "orchestrator/tests/test_prd225_trust_gate.py"
   ]
  },
  {
   "id": "P225-RVW-3",
   "title": "HIGH — blocked cascade ignores OrchestrationTaskDependency edges; returns 0 for the primary mission case",
   "description": "REVIEW FINDING (code-reviewer + manual, verified). services/ask_cascade.py:4-8 (docstring) claims TWO edge sources — parent_task_id AND the mission OrchestrationTaskDependency DAG — but :51-95 walks ONLY parent_task_id; OrchestrationTaskDependency is never imported or queried anywhere in the PRD. orchestration_board_bridge.py:170-197 makes every mission-step board task a FLAT sibling under one single mission-level parent (parent_task_id = the mission board task), never a child of another step. So the primary real case — an agent executing step N calls platform_ask_human(subject_type='board_task', subject_id=<step N's own board_task id>) — has ZERO parent_task_id children: count_downstream_blocked returns 0 even when steps N+1.. are all blocked on it via OrchestrationTaskDependency. Consequences: US-002's baked urgent-bypass (>=3 dependents) never fires for a genuinely blocking mission ask, and US-004's tab renders an empty cascade. BoardTask.orchestration_task_id (core/models/core.py:1581) is the link to the DAG. No test builds an OrchestrationTaskDependency fixture. NOTE (CLAUDE.md §12): if the DAG traversal is genuinely out of v1 scope that is Gerard's call to make explicitly — do not keep a parent_task_id-only impl behind a docstring/AC that promise both; either build it or correct both.",
   "acceptanceCriteria": [
    "DONE — new _board_children merges BOTH sources into the adjacency: parent_task_id children AND the DAG (parent.orchestration_task_id → OrchestrationTaskDependency.depends_on_task_id == it → dependents' task_id → BoardTask.orchestration_task_id back to the board task); _board_subtree_adjacency BFSes board rows under ONE visited set so a cycle in either source terminates (ask_cascade.py:51-140)",
    "DONE — test_mission_dag_cascade_counts_dependents: steps 6-9 depend on step 5 via OrchestrationTaskDependency only (parent_task_id=mission 100, NOT 5) → count_downstream_blocked==4, is_urgent_cascade True, board_task_cascade_detail lists {6,7,8,9}; test_mission_dag_ask_marks_urgent drives the real ask_human → severity='urgent'; test_mission_dag_cascade_is_cycle_safe (5⇄6 dependency cycle terminates)",
    "DONE — module docstring (already claimed both sources) now matches; _board_subtree_adjacency + count_downstream_blocked docstrings updated to 'BOTH sources / the DAG' (implemented, not descoped)",
    "DONE — cd orchestrator && python3 -m pytest -q tests/test_prd225_ask_human.py tests/test_prd225_questions_list.py green (21 passed); full PRD-225 backend suite 84 passed together"
   ],
   "files": [
    "orchestrator/services/ask_cascade.py",
    "orchestrator/tests/"
   ]
  },
  {
   "id": "P225-RVW-4",
   "title": "MEDIUM — cascade counts terminal (done/failed) descendants as blocked, and does an N+1 per-task lookup on the 30s-polled list",
   "description": "REVIEW FINDING (code-reviewer, verified). (a) ask_cascade.py:51-95 (_board_subtree_adjacency) collects every descendant BoardTask regardless of status — no filter for done/failed/cancelled. Since mission-step board tasks are flat children of the mission board task (orchestration_board_bridge.py:170-197), an ask against the mission-level board task pulls in every step ever created, finished or not: 7 done + 1 blocked reports downstream_blocked=8 (trips the urgent bypass) and the tab shows 'Blocking 8 downstream tasks' — true value 1. (b) ask_cascade.py:118-130 (board_task_cascade_detail) resolves the shown tasks with a per-id loop of db.query(BoardTask).filter(id==tid) — up to 6 per row, called per question row (up to 200) by approval_grants.py:56-70 on GET /api/v1/approval-grants, which useQuestions() polls every 30s (use-approval-grants.ts:38) from every open Command Center tab.",
   "acceptanceCriteria": [
    "DONE — _board_children skips terminal (done/failed/cancelled) descendants AND their subtrees (ask_cascade.py:_TERMINAL_STATUSES + _add guard), so board_task_cascade AND count_downstream_blocked exclude them; test_terminal_children_excluded_from_cascade: mission-level ask over 7 done + 1 blocked flat sibling → cascade==['9'], count==1 (not 8), is_urgent_cascade False, detail total 1",
    "DONE — board_task_cascade_detail loads shown ids via ONE db.query(BoardTask).filter(workspace_id, id.in_(int_ids)).all() then re-orders by BFS (ask_cascade.py:199-223); test_cascade_detail_loads_shown_tasks_in_one_query asserts db.stats['in_filters']==1 for a 3-child cascade (traversal uses equality filters, only the detail batch is in_); regression: test_prd225_ask_human fake-session taught in_op (7 passed questions_list, 86 PRD-225 backend, 37 approval-kind regression green)"
   ],
   "files": [
    "orchestrator/services/ask_cascade.py",
    "orchestrator/tests/"
   ]
  },
  {
   "id": "P225-RVW-5",
   "title": "MEDIUM — the ingress trust gate fails OPEN on any internal exception",
   "description": "REVIEW FINDING (security-reviewer, verified). webhooks.py:729-738 wraps _apply_trust_gate in try/except and, on ANY exception, logs and falls through to normal routing (comment: 'routing normally'). A trust gate that fails open silently disables strict for that message on any internal error — a security control one bug away from an unlogged-as-such bypass. (No concrete trigger found today — the internal lookups are isinstance-guarded — so this is a fail-safe-direction correction, not a live exploit.)",
   "acceptanceCriteria": [
    "DONE — _apply_trust_gate fails CLOSED once a channel is confirmed: the mode/classify/hold body is wrapped so ANY error returns {status:held, routed:False, reason:'trust_gate_error', ask_id:None} (logged at error, no message body) instead of raising; the handler except block (webhooks.py:2d) no longer 'routes normally' — it logs at error and, unless _channel_is_allow_all (True only for a provable allow_all), returns the held response so nothing reaches the WebhookIngestor/UniversalRouter/AutoBrain interception. allow_all is resolved before the classifier so a route-everything channel is a safe no-op",
    "DONE — test_gate_error_fails_closed_under_strict: should_hold monkeypatched to raise under a strict channel → res is not None (held), routed False, reason 'trust_gate_error', NO ApprovalGrant written (nothing executed), an error-level gate log with no directive body; test_gate_error_under_allow_all_still_routes (allow_all short-circuits before should_hold → None → routes); test_channel_is_allow_all_fails_closed (allow_all True; strict/no-channel False). cd orchestrator && python3.10 -m pytest tests/test_prd225_trust_gate.py green (38 passed); 136 PRD-225 + webhook regression green together"
   ],
   "files": [
    "orchestrator/api/webhooks.py",
    "orchestrator/tests/"
   ]
  },
  {
   "id": "P225-RVW-6",
   "title": "MEDIUM — unauthenticated channel text renders as clickable markdown links in the admin Questions tab",
   "description": "REVIEW FINDING (security-reviewer, verified). The trust gate embeds RAW inbound text into question_md with no sanitization (webhooks.py:416-420, f-string interpolates text_in.strip()). questions-tab.tsx:101-106 renders question_md via ReactMarkdown + chatMarkdownComponents, whose 'a' renderer (markdown-components.tsx:44-53) passes href straight through (target=_blank). So anyone who can message a strict/communication_only channel gets their raw text — including markdown links like [route it now](https://attacker.example) — stored and displayed as a CLICKABLE link inside the admin's own trusted Questions tab, next to copy priming them to act ('Answer \"route it\" to let it proceed'). react-markdown without rehype-raw blocks raw HTML/script and strips javascript: URIs, so this is a phishing/social-engineering vector, not proven XSS.",
   "acceptanceCriteria": [
    "DONE — _apply_trust_gate stores the channel body via _fence_untrusted (webhooks.py): a fenced code block whose fence is one backtick longer than the longest inner backtick run (no breakout), so inside GFM markdown the link syntax [x](url) and bare autolinks are LITERAL text, never an anchor. Scoped to channel-sourced text only — agent-authored question_md (platform_ask_human) is untouched. Backend: test_channel_directive_is_stored_fenced_inert (link + bare URL sit inside the fence, raw text preserved), test_fence_untrusted_prevents_backtick_breakout",
    "DONE — questions-tab.test.tsx 'a strict-held channel directive renders inert': fenced question_md → NO a[href=https://attacker.example], NO a[href=https://evil.example], no link role for the inbound text, raw text still readable (CodeBlock renders it as literal, never invoking the a renderer); control test 'agent-authored question still renders real markdown links' proves the fix is scoped. cd frontend && npm run -s test green (50 files, 296 tests); 40 trust-gate + 52 backend green"
   ],
   "files": [
    "orchestrator/api/webhooks.py",
    "orchestrator/tests/",
    "frontend/components/command-center/questions-tab.tsx"
   ]
  },
  {
   "id": "P225-RVW-7",
   "title": "MEDIUM — a config-only channel update silently drops trigger_mode (reverts to strict)",
   "description": "REVIEW FINDING (manual, verified). api/channels.py update_channel (~:449-473): when the payload carries 'config' but not 'trigger_mode', new_config = dict(payload['config']) REPLACES the whole config and the stored trigger_mode is not carried over — so trigger_mode_of() reads the default 'strict' on the next read. The code comment (~:452-454) explicitly claims 'a config update never drops the mode', which is false. list_channels (:229) surfaces only trigger_mode (not the raw config, which holds creds), so a client editing credentials cannot round-trip trigger_mode and any creds edit silently resets an operator's allow_all / communication_only choice back to strict. Fail-safe in direction but violates the stated invariant and surprises operators.",
   "acceptanceCriteria": [
    "DONE — update_channel: when 'config' is in the payload without an explicit 'trigger_mode', it backfills the stored mode via with_trigger_mode(new_config, normalize_trigger_mode(_current_channel_config(db,id)['trigger_mode'])) — rebuild-don't-mutate — so a creds-only edit no longer replaces the whole config and drops the mode; an explicit payload trigger_mode still wins (the if-branch). A modeless channel materializes no 'strict' (normalize returns None → no backfill). New _current_channel_config helper shared with the mode-only branch (channels.py:433-500)",
    "DONE — test_prd225_channels_trigger_mode.py (fake db intercepts the raw SQL): test_config_only_update_preserves_stored_trigger_mode (PUT {config:{bot_token:'NEW'}} vs stored allow_all → written config trigger_mode=='allow_all', bot_token=='NEW'), test_explicit_trigger_mode_overrides_stored, test_modeless_channel_config_edit_materializes_no_mode. cd orchestrator && python3.10 -m pytest tests/test_prd225_channels_trigger_mode.py green (3 passed)"
   ],
   "files": [
    "orchestrator/api/channels.py",
    "orchestrator/tests/"
   ]
  },
  {
   "id": "P225-RVW-8",
   "title": "MEDIUM — platform_ask_human reports parked:true for a terminal subject that can never resume",
   "description": "REVIEW FINDING (code-reviewer, verified). handlers_asks.py:94 skips the block-flip when the board task is already terminal (status in done/failed — _PARKED_STATUSES), but :116-127 still creates the question grant and unconditionally returns {success:True, parked:True, message:'Parked ...'}. Answering that question later no-ops the resume — _requeue_blocked_task only acts if task.status=='blocked' (api/approval_grants.py:385) — yet the grant still flips to GRANTED and _confirm_answer_into_chat (api/approval_grants.py:326-344) tells the human the work is 'resuming'. The tool lies about parking and the human is told resumption happened when it did not.",
   "acceptanceCriteria": [
    "DONE — ask_human rejects a terminal board_task BEFORE create_grant: after the not-found check, if board_task.status in _TERMINAL_STATUSES (done/failed/cancelled) it returns {success:False, parked:False, error:'board task N is already <status>; a finished task cannot be parked or resumed by an answer'} — no grant row, no dispatch, no 'Parked ...' message; the park flip is unreachable for terminals so line 94 simplified to 'if board_task is not None'. _PARKED_STATUSES renamed _TERMINAL_STATUSES (+cancelled, matching ask_cascade). handlers_asks.py:22,69-81,105-110",
    "DONE — test_terminal_board_task_is_rejected_not_parked parametrized over done/failed/cancelled: success False, parked False, error names the finished state, no 'Parked', no 'message' key, ZERO ApprovalGrant rows, status untouched (not flipped to blocked), dispatched==[]. cd orchestrator && python3.10 -m pytest tests/test_prd225_ask_human.py green (19 passed); 97 PRD-225 backend green together"
   ],
   "files": [
    "orchestrator/modules/tools/discovery/handlers_asks.py",
    "orchestrator/tests/"
   ]
  },
  {
   "id": "P225-RVW-9",
   "title": "CRITICAL — strict gate is bypassed by any content-bearing non-text update; the raw JSON body reaches AutoBrain's platform-tool interception even in strict",
   "description": "REVIEW FINDING (security-reviewer CRITICAL + manual, verified). webhooks.py:404-458 (_inbound_text) reads ONLY message/edited_message .text/.caption, event.text, Twilio Body, WhatsApp messages[].text.body — its own docstring admits the WebhookIngestor json.dumps fallback is 'deliberately NOT mirrored'. webhooks.py:519-521 returns None for empty text ('nothing to hold (non-text update)') BEFORE should_hold/the classifier ever runs, so a text-less update falls through to routing regardless of trigger_mode, strict included. But WebhookIngestor (core/routing/ingestors/webhook.py:76-77) sets envelope.content = json.dumps(body) for any body it finds no text in, and webhooks.py:921 runs AutoBrain._match_platform_query over that blob with an UNANCHORED word-boundary regex (consumers/chatbot/auto.py:1153-1166) — a keyword ANYWHERE in the dumped JSON hits, routing to the auto-cto system agent (broad platform-tool access) with the full attacker JSON as its prompt (webhooks.py:929-940). Exact payload: any Telegram user who can message the bot sends a caption-less DOCUMENT whose filename is 'run the recipe.pdf' (verbatim from _PLATFORM_KEYWORDS['platform_execute_recipe'], auto.py:221-290) — document/poll/contact/venue/sticker updates have no .text/.caption, so _inbound_text returns '' while the ingestor serialises the whole update. On a strict channel this is never held and can trigger platform_execute_recipe / platform_delete_recipe / platform_delete_document / platform_reprocess_document — a full bypass of the governance control this PRD exists to add, on the public-ingress surface. test_prd225_trust_gate.py:242-329 proves parity ONLY for caption/edited/whatsapp-body shapes; poll/document-filename/contact/venue/sticker are untested.",
   "acceptanceCriteria": [
    "DONE — new _telegram_message_content(m) extracts document/audio/video/voice.file_name, poll.question, contact first+last, venue title+address, sticker.emoji (after text/caption); under strict each is HELD as a kind='question' subject_type='channel' row that short-circuits at webhooks.py:888-892 BEFORE the ingestor(:908)/AutoBrain interception(:919). test_strict_holds_document_filename ('run the recipe.pdf' → would have hit platform_execute_recipe), test_strict_holds_poll_question ('delete recipe 5 now?'), test_strict_holds_contact_name (first='reprocess' last='document'), test_strict_holds_venue (title='run automation') all assert reason=='trust_gate_hold' + a channel question row carrying the text",
    "DONE — _inbound_text delegates message/edited_message extraction to _telegram_message_content: text/caption THEN poll.question, contact.first_name+last_name, venue.title+address, document/audio/video/voice.file_name, sticker.emoji (webhooks.py:404-503). The ingestor's json.dumps blanket fallback stays deliberately NOT mirrored, so a genuinely user-contentless status/delivery callback still scores empty and routes",
    "DONE — test_gate_covers_all_ingestor_content_shapes parametrizes 12 shapes (7 ingestor-extracted text + 5 media subfields), feeds each through WebhookIngestor.ingest AND _inbound_text, asserts parity (ingestor yields non-json.dumps ⇒ _inbound_text non-empty) PLUS RVW-9 coverage (every content-bearing shape non-empty)",
    "DONE — cd orchestrator && python3.10 -m pytest -q tests/test_prd225_trust_gate.py green (56 passed, +16); 113 PRD-225 backend + 3/4 webhook-regression files green together. The lone test_p2w2_webhook_signature_reject::..v3_signature_accepted 'fail' is the documented cross-run Redis SETNX dedup on fixed id wh_1 ('Duplicate Composio webhook wh_1 — no-op ack'); this diff = webhooks.py + test_prd225_trust_gate.py ONLY (api/composio.py + redis untouched), green on clean Redis / CI"
   ],
   "files": [
    "orchestrator/api/webhooks.py",
    "orchestrator/tests/test_prd225_trust_gate.py"
   ]
  },
  {
   "id": "P225-RVW-10",
   "title": "HIGH — Telegram default-chat is hijackable by any inbound sender, re-opening the RVW-1 answer-injection class through the mutable anchor",
   "description": "REVIEW FINDING (manual, verified; NOT caught by the RVW-1 fix, which only bound the answer lookup to the STORED channel_refs — the stored anchor is itself attacker-settable). webhooks.py:864 (step 2b) calls _persist_integration_default(db, workspace, 'telegram_default_chat_id', reply_ctx['chat_id']) on EVERY inbound telegram message, BEFORE correlation (2c, :873) and the trust gate (2d, :890); _persist_integration_default (webhooks.py:695-711) overwrites the stored value whenever it differs (last-sender-wins). The polling adapter does the same (channels/telegram_adapter.py:118-146,154). Telegram exposes no stable metadata.default_target (the driver's verify sets only bot_id/username, telegram.py:95-99), so channels/sender.py:145-146 (_resolve_target step 3) resolves the send target to that mutable telegram_default_chat_id. handlers_asks.py:206-220 (_capture_question_telegram) delivers the question to it and stores it as channel_refs.telegram={chat_id: result.target, ...}. The RVW-1 authorization (webhooks.py:264-304) then binds the answer to channel_refs.telegram.chat_id == sending chat — but that anchor is now the ATTACKER'S chat. Exploit: an attacker who can message the bot (discoverable @username / shared group / ex-member — the exact RVW-1 threat model) sends any message, so telegram_default_chat_id becomes the attacker chat; the next platform_ask_human delivers the agent's question to the attacker and pins their chat as the anchor; the attacker replies and apply_question_answer resumes the parked board task with the attacker's free text appended verbatim into planning_data.human_qa. This re-opens the RVW-1 in-workspace answer-injection class through the mutable anchor; existing tests fix the stored anchor and never exercise a hijacked default. Amplifier (pre-existing, security-reviewer): the webhook verifies no Telegram X-Telegram-Bot-Api-Secret-Token and applies HMAC only when a webhook_secret was configured, and SlowAPIMiddleware is off by default (main.py, POLICY_PLANE_ENABLED=False) — so there is no rate limit on the retarget.",
   "acceptanceCriteria": [
    "DONE — the delivery/correlation anchor is now SET-ONCE at both persist sources: webhooks._persist_integration_default (:754) and telegram_adapter._persist_default_chat_id (:118) seed telegram_default_chat_id/slack_default_channel only when UNSET and never overwrite it from inbound traffic (an already-set value ⇒ log 'ignoring inbound retarget', return). First-inbound seeds it; later senders cannot move it; an operator changes it explicitly via Settings→Integrations (save_integrations), never inbound. Chose the AC's 'persist sets it once' option — no change needed to sender._resolve_target/handlers_asks",
    "DONE — test_default_chat_is_not_hijacked_by_inbound: designated chat A stored, an inbound from chat B via _persist_integration_default leaves the anchor A (asserted), then _capture_question_telegram (send_to_channel mock resolves the still-A default) writes channel_refs.telegram.chat_id=='A'; a reply from B → _maybe_answer_question None (falls through), grant PENDING; a reply from A → route_type 'question_answer', grant GRANTED",
    "DONE — test_answer_injection_via_retarget_is_blocked drives B-messages → ask-fires (anchored to A) → B-replies-to-777: res is None (not correlated), grant stays PENDING, the parked BoardTask stays 'blocked' (NOT resumed), planning_data.human_qa == [] (no attacker text appended)",
    "DONE — cd orchestrator && python3.10 -m pytest -q tests/test_prd225_telegram_bridge.py green (14 passed, +2); 213 PRD-225 + webhook(dedup/debounce) + channel driver/sender/adapter-contract regression green together (telegram_adapter change clean)"
   ],
   "files": [
    "orchestrator/api/webhooks.py",
    "orchestrator/channels/sender.py",
    "orchestrator/modules/tools/discovery/handlers_asks.py",
    "orchestrator/tests/test_prd225_telegram_bridge.py"
   ]
  },
  {
   "id": "P225-RVW-11",
   "title": "HIGH — the answer confirmation claims 'resuming' when nothing resumed; strict-held directives and tool_call/playbook_run asks have no working resume path",
   "description": "REVIEW FINDING (code-reviewer HIGH x2, verified). _confirm_answer_into_chat (api/approval_grants.py:326-344) unconditionally posts 'Answered — resuming <subject>' without checking the resume outcome — the RVW-8 'the tool lied' class, recurring on the answer side. Three subjects resume to a no-op yet still claim success: (1) the trust gate stages subject_type='channel' rows (webhooks.py:534-544) whose question_md literally says 'Answer \"route it\" to let it proceed', but 'channel' matches none of _requeue_subject's three constants (approval_grants.py:347-371, silent return); the held directive is never re-injected into UniversalRouter (grep: nothing branches on subject_type=='channel' except the gate that creates it), so a strict channel can HOLD a directive but has NO working release path (dismiss and answer both discard it), contradicting US-006's 'approving = answering with route it'. (2) tool_call asks: handlers_asks.py:97-108 create_grant never sets tool_name/details.action/params (the tool schema has no such field, actions_asks.py:28-64), so _resume_tool_call always hits 'if not action' (approval_grants.py:440-449) and records 'grant carries no stored action to resume' — nothing executes. (3) playbook_run asks: resume_playbook_run_grant (services/watch_rerun.py) needs details.watch_action, absent here, so it records 'grant carries no executable watch_action (None)' — a no-op. All three still fire the 'resuming' chat confirmation. Only board_task genuinely resumes, yet PRD G1/G3 promise all three subject types.",
   "acceptanceCriteria": [
    "DONE — _requeue_subject now returns True iff the subject GENUINELY resumed (board re-queue fired, or a stored tool/watch action executed — via new _executed_result_succeeded); apply_question_answer passes that to _confirm_answer_into_chat(resumed=...), which says 'Answered — resuming <subject>' only when True, else 'Answer recorded for <subject> — nothing to auto-resume'. The approval path (grant_approval:166) ignores the new return (unchanged). test_confirmation_is_honest_when_no_resume: a channel hold answered → GRANTED, confirmation has NO 'resuming' and contains 'recorded'",
    "DONE (refuse option, flagged for Gerard — NOT a silent descope) — genuine resume of tool_call/playbook_run QUESTIONS needs a re-entry mechanism that does not exist (only board_task re-queues via the dispatcher; the resume fns no-op on a question grant that carries no action), which is a separable feature, so per the AC's second branch platform_ask_human REFUSES those subjects up front (handlers_asks.py: subject_type!='board_task' → success:False, parked:False, clear error naming the subject, no grant row, no dispatch) exactly as RVW-8 refuses a terminal board task. test_tool_call_ask_resumes_or_is_refused + test_playbook_run_ask_resumes_or_is_refused (no row, dispatched==[])",
    "DONE — chose the copy-honesty branch: the trust-gate channel hold copy changed from 'Answer \"route it\" to let it proceed' to '**held for review** … this message was held and NOT executed … answering here records a note but does not auto-route the directive' (webhooks.py), so the stored instruction matches the (no-resume) behaviour. test_channel_hold_release_semantics_match_copy asserts both sides: copy has 'not executed'+'does not auto-route' and no 'to let it proceed', AND answering the hold → GRANTED with an honest 'recorded'/no-'resuming' confirmation",
    "DONE — cd orchestrator && python3.10 -m pytest -q tests/test_prd225_answer_resume.py tests/test_prd225_trust_gate.py green (part of 117 PRD-225 backend passed); 37 approval-kind regression tests green with ZERO approval-kind test file edits (git status confirms none modified; test_p2w2_grant_resume calls _requeue_subject directly and stays green — the bool return is backward-compatible)"
   ],
   "files": [
    "orchestrator/api/approval_grants.py",
    "orchestrator/api/webhooks.py",
    "orchestrator/modules/tools/discovery/handlers_asks.py",
    "orchestrator/tests/"
   ]
  },
  {
   "id": "P225-RVW-12",
   "title": "HIGH — the trust gate never applies to legacy integrations-configured channels; strict is the advertised default but those bots are silently ungated",
   "description": "REVIEW FINDING (security-reviewer HIGH, verified). _apply_trust_gate resolves the channel via _channel_for_platform (webhooks.py:390-401), which queries ONLY channel_connections; if none, it returns None with 'no connected channel, the gate does not apply' (webhooks.py:504-506). But a Telegram/Slack bot configured through the pre-existing Settings→Integrations surface (api/workspaces.py:144-168 save_integrations, PUT /api/workspaces/current/integrations, gated only by workspace:manage) sets telegram_bot_token in workspace.settings.integrations WITHOUT creating a channel_connections row — and both the outbound sender and the answer bridge honor that legacy config (channels/sender.py:182-193 legacy fallback), so those bots are fully live. Result: DEFAULT_TRIGGER_MODE='strict' (ingress_gate.py:36) is advertised as the safe default but silently does NOT apply to any workspace whose bot predates the Channels UI — plausibly most existing pilot workspaces — and nothing surfaces that the channel is ungated. Confirmed intentional by test_no_channel_row_means_no_gate, but it leaves the new security control with a silent coverage hole on the public-ingress surface.",
   "acceptanceCriteria": [
    "DONE — chose the read-path option (a): new _gated_channel_for_platform(db, workspace, platform) returns the ChannelConnection row, ELSE — when the bag has {platform}_bot_token — a synthetic legacy channel whose config.trigger_mode reads settings.integrations.{platform}_trigger_mode (default strict). _apply_trust_gate + _channel_is_allow_all both use it, so a legacy bot is gated identically. (Rejected option (b) 'save_integrations backfills a channel row' — it can't cover EXISTING legacy workspaces without a second migration, a Hard NO.) test_legacy_integrations_channel_is_gated_strict: communication_only legacy bot holds a directive, routes chatter",
    "DONE — test_legacy_integrations_default_is_strict: only settings.integrations.telegram_bot_token (no channel row) → an inbound directive is HELD as a subject_type='channel' question by default; telegram_trigger_mode='allow_all' → routes, nothing held. Operator opt-out is reachable: {telegram,slack}_trigger_mode added to _ALLOWED_INTEGRATION_KEYS in save_integrations and validated via normalize_trigger_mode (garbage dropped ⇒ strict) — test_save_integrations_sets_the_legacy_opt_out",
    "DONE — cd orchestrator && python3.10 -m pytest -q tests/test_prd225_trust_gate.py green (60 passed, +4); 140 PRD-225 + webhook (dedup/catalog/debounce) regression passed together"
   ],
   "files": [
    "orchestrator/api/webhooks.py",
    "orchestrator/services/ingress_gate.py",
    "orchestrator/tests/test_prd225_trust_gate.py"
   ]
  },
  {
   "id": "P225-RVW-13",
   "title": "MEDIUM — the blocked cascade traversal is N+1/multiplicative on the 30s-polled grants list",
   "description": "REVIEW FINDING (code-reviewer + manual, verified). _board_children (services/ask_cascade.py:107-121) issues one query for parent_task_id children + one for OrchestrationTaskDependency edges + ONE MORE query PER edge for the dep_board lookup (a .filter(...).first() inside 'for edge in edges'). _board_subtree_adjacency (:149-159) calls _board_children once per visited node. _grant_payload (api/approval_grants.py:61-67) recomputes the FULL cascade for every kind='question'/subject_type='board_task' row on every GET /api/v1/approval-grants (list_grants :102-121, up to 200 rows), which useQuestions polls every 30s (frontend/hooks/use-approval-grants.ts:38) from each open Command Center. P225-RVW-4 batched only the DISPLAY load (shown ids), not this traversal. A workspace with N open board-task questions rooted in M-step mission DAGs pays roughly N×(2M+edges) synchronous round-trips per poll. Cycle-safety itself is fine; this is a scalability cliff, not a wrong-output bug.",
   "acceptanceCriteria": [
    "DONE — _board_children Source 2 (ask_cascade.py) replaces the per-edge dep_board .first() loop with ONE batched load per level: collect dependent_ot_ids = [edge.task_id for edge in OrchestrationTaskDependency where depends_on_task_id==parent.ot], then a single BoardTask.orchestration_task_id.in_(dependent_ot_ids). Each traversal level is now O(1) dep-board queries (parent-tree query + edge query + one batched in_), independent of the DAG fan-out. test_cascade_traversal_batches_dependent_lookups: for fan-out 3 AND 30, db.stats['in_filters']==1 both times (the old per-edge loop fired 0 in_ + M point reads); cascade results unchanged (3 vs 30 dependents found)",
    "DONE — cd orchestrator && python3.10 -m pytest -q tests/test_prd225_ask_human.py tests/test_prd225_questions_list.py green (27 passed); full PRD-225 suite 121 passed (RVW-3 mission-DAG + RVW-4 terminal-exclusion tests on the same path stay green — the batching changed the query pattern, not the cascade result)"
   ],
   "files": [
    "orchestrator/services/ask_cascade.py",
    "orchestrator/tests/"
   ]
  },
  {
   "id": "P225-RVW-14",
   "title": "MEDIUM — apply_question_answer has no compare-and-swap; two concurrent answers to the same question both apply",
   "description": "REVIEW FINDING (security-reviewer, verified). _load_grant (api/approval_grants.py:124-132) and the Telegram lookups (webhooks.py:242-304) are plain non-locking SELECTs; apply_question_answer (approval_grants.py:269-291) mutates and commits off that unlocked read with no SELECT...FOR UPDATE and no atomic UPDATE...WHERE status='pending'. Authorization is chat-scoped, not principal-scoped (by design), so any member of a shared authorized group chat can answer, widening the race, and there is no rate limit by default (main.py SlowAPIMiddleware gated on POLICY_PLANE_ENABLED=False). Two near-simultaneous answers (two group members, or '/answer id a' then '/answer id b', or a Telegram reply racing POST /{id}/answer) both pass the pending check and both apply: duplicate human_qa entries (_record_answer_on_subject), duplicate 'resuming' confirmations, and under truly concurrent transactions a duplicate notify_task_available. Bounded today (board re-queue is idempotent-ish; tool_call/playbook_run no-op), but the missing guard becomes severe once a subject with a populated details.action rides this shared path.",
   "acceptanceCriteria": [
    "DONE — apply_question_answer (approval_grants.py) now flips pending→granted via an atomic compare-and-swap: db.query(ApprovalGrant).filter(id==grant.id, status=='pending').update({status:granted, answer_text, answered_by, answered_at}, synchronize_session=False). If it affects 0 rows the caller lost the race → rollback+refresh+return (record/resume/confirm NOTHING). Only the winner syncs the in-memory row, appends human_qa, resumes, and confirms — so a reply racing POST /answer, or two /answer messages from a shared chat, apply exactly once. test_concurrent_answers_apply_once: two apply_question_answer calls on one grant → status GRANTED, answer 'Ship A' (first wins), len(human_qa)==1, len(confirmed)==1",
    "DONE — cd orchestrator && python3.10 -m pytest -q tests/test_prd225_answer_resume.py green (part of 122 PRD-225 passed); 44 approval-kind regression tests green (test_prd181_approval_grants/p2w2_grant_resume/tool_grant_consume/grants_oversight/commit_ordering/prd200_renotify/mutation_admin_gate — 166 passed together), ZERO approval-kind test files edited (CAS is on the question path only; grant_approval untouched). Fake _Query.update()/_FakeSession.refresh() added to the 3 PRD-225 fakes that drive apply_question_answer"
   ],
   "files": [
    "orchestrator/api/approval_grants.py",
    "orchestrator/tests/"
   ]
  },
  {
   "id": "P225-RVW-15",
   "title": "MEDIUM — agent-authored question_md is stored unfenced; the same clickable-phishing-link sink RVW-6 hardened for channel text, on the sibling producer",
   "description": "REVIEW FINDING (security-reviewer, verified). RVW-6 fenced CHANNEL-sourced directive text via _fence_untrusted (webhooks.py:461-489,538) precisely because questions-tab.tsx renders question_md through ReactMarkdown+remarkGfm with GFM autolinking, turning [x](url) and bare URLs into clickable anchors in the admin's trusted surface. But platform_ask_human stores its question raw (handlers_asks.py:102 question_md=question) and the SAME sink (questions-tab.tsx:103-105) renders it for every grant. An agent relaying attacker-controlled text into its question (a document/webpage/inbound message it is summarizing that contains '[Approve the transfer](https://evil.example/phish)' or a bare autolinkable URL) produces a clickable phishing anchor next to the answer box. Compound/indirect (needs an upstream prompt-injection precursor to plant the text in the agent's question), hence MEDIUM — but it is the exact clickable-anchor class _fence_untrusted was built to stop, on the sibling producer it does not cover. Note: RVW-6 deliberately scoped fencing to channel text to keep legit agent markdown formatting, so the fix here is link-inerting (strip href/autolink) rather than full fencing, preserving emphasis/lists.",
   "acceptanceCriteria": [
    "DONE — chose the renderer option (keeps stored question_md intact): new questionMarkdownComponents in markdown-components.tsx spreads chatMarkdownComponents but overrides the 'a' renderer to an INERT <span> (no href, no anchor); questions-tab.tsx renders question_md with it instead of chatMarkdownComponents. So an agent-relayed '[Approve the transfer](https://evil.example/phish)' or a bare autolinked URL shows readable text with NO clickable anchor, while emphasis/lists/code/tables still render (link-inerting, not fencing). test 'agent question with a URL renders no clickable anchor (P225-RVW-15)': body text present, a[href=phish]/a[href=bare] null, no link role, **urgent** still rendered. RVW-6 channel-fenced test still green (fenced text renders as a CodeBlock, unaffected)",
    "DONE — cd frontend && npm run -s test green (50 files, 296 tests; questions-tab.test.tsx 10 passed); cd orchestrator && python3.10 -m pytest -q tests/test_prd225_ask_human.py green (19 passed — RVW-15 is frontend-only, no backend change so no server-side inert test needed)"
   ],
   "files": [
    "orchestrator/modules/tools/discovery/handlers_asks.py",
    "frontend/components/command-center/questions-tab.tsx",
    "frontend/",
    "orchestrator/tests/"
   ]
  },
  {
   "id": "P225-RVW-16",
   "title": "CRITICAL — the strict trust gate is bypassed by any content-bearing shape the scorer misses but the ingestor json.dumps (WhatsApp media caption/filename, Telegram service-messages, Slack file titles); the raw JSON reaches AutoBrain's platform-tool interception even in strict",
   "description": "REVIEW FINDING (security-reviewer CRITICAL + code-review, verified end-to-end; RVW-9 generalised). The gate's content scorer (_inbound_text / _telegram_message_content, api/webhooks.py:440-553) and the router's ACTUAL content builder (WebhookIngestor.ingest, core/routing/ingestors/webhook.py:43-77) are TWO INDEPENDENT hand-maintained per-field allowlists of which subfield carries the message. They agree ONLY for the shapes P225-RVW-2/RVW-9 tested. For any content-bearing shape the gate's list misses, _inbound_text returns '' and _apply_trust_gate waves it through at webhooks.py:614-616 (`if not text_in.strip(): return None` — reached BEFORE should_hold, so BOTH strict AND communication_only are defeated), while the ingestor falls back to `content = json.dumps(body)` (webhook.py:76-77) and AutoBrain's UNANCHORED keyword search (_match_platform_query, consumers/chatbot/auto.py:1153-1166) matches a platform_* phrase ANYWHERE in that blob, routing to the auto-cto system agent (full platform_* ActionRegistry) with the attacker JSON as prompt and delivering the reply back to the sender's own chat (webhooks.py:1042-1080). THREE verified deterministic bypasses, no prior relationship required: (A) WhatsApp media — _inbound_text reads only messages[0].text.body (webhooks.py:542-551); a caption/filename lives under messages[0].image.caption / .document.caption / .document.filename / .video.caption; WhatsApp is a live registered driver (channels/drivers/whatsapp.py) defaulting strict (ingress_gate.py:36); payload {entry:[{changes:[{value:{messages:[{type:image,image:{caption:'workspace info'}}]}}]}]} matches platform_get_workspace_info. (B) Telegram service-messages NOT in _telegram_message_content (webhooks.py:440-493): new_chat_title (group rename), new_chat_members[].first_name (a joiner's self-chosen name), my_chat_member / left_chat_member — my_chat_member fires merely from adding the bot to ANY group via its public @username; payload with new_chat_title='list my documents' matches platform_list_documents. (C) Slack file-only message — both _inbound_text (webhooks.py:533-536) and the ingestor (webhook.py:56-60) key only on event.text; files[].title / files[].name (attacker-chosen at upload) are never inspected. Read/info-disclosure platform_* tools (get_llm_usage, search_memory, list_documents, query_data, search_chat_history, get_workspace_info, list_connected_apps) have NO separate confirmation gate (that layer fires only for `destructive`, modules/tools/capabilities/classifier.py:676-678), so the CTO agent's tool loop executes and the reply is delivered back to the attacker. The scoring bypass is deterministic (no LLM judgment to get PAST the gate). test_prd225_trust_gate.py covers only the RVW-2/RVW-9 shapes — ZERO coverage for WhatsApp media, Telegram service-messages, or Slack files. Fix the CLASS, not the three instances.",
   "acceptanceCriteria": [
    "DONE — BOTH sanctioned fixes: (1) core/routing/ingestors/webhook.py::extract_inbound_text is the ONE shared extractor; api/webhooks.py::_inbound_text delegates to it and WebhookIngestor.ingest builds content from it — the two per-field allowlists are now a single function that cannot diverge. (2) invert: webhooks.py:958 has_recognised_text=bool(extract_inbound_text(body).strip()) guards the 3b AutoBrain._match_platform_query interception, so an unrecognised shape's json.dumps blob is never keyword-matched. Named shapes score non-empty (test_rvw16_gate_and_ingestor_share_one_extractor, 7 params: WhatsApp image/video/document caption + document filename, Telegram new_chat_title + new_chat_members name, Slack files[].title + .name); my_chat_member/left_chat_member/unknown update-type score empty → guard skips → provably never keyword-matched (test_rvw16_unrecognised_shape_is_never_keyword_matched, 3 params)",
    "DONE — 5 hold tests added (test_strict_holds_whatsapp_image_caption, _whatsapp_document_filename, _telegram_new_chat_title, _telegram_new_member_name, _slack_file_title), each asserts res['reason']=='trust_gate_hold' + grant.kind==KIND_QUESTION + subject_type=='channel' + the directive text present in question_md; strict short-circuits at the 2d gate BEFORE the ingestor (3) and AutoBrain interception (3b)",
    "DONE — parity closed: test_rvw16_gate_and_ingestor_share_one_extractor asserts per shape _inbound_text(body).strip()==expected AND env.content==_inbound_text(body) (ONE extractor, cannot diverge) AND env.content!=json.dumps(body) (real content, not the keyword-matchable blob). The RVW-9 test_gate_covers_all_ingestor_content_shapes parity branch — previously vacuous for media subfields — is now real (ingestor extracts them via the shared function); stale comment corrected",
    "DONE — cd orchestrator && python3 -m pytest -q tests/test_prd225_trust_gate.py = 75 passed; full PRD-225 backend (137) + webhook regression together = 167 passed (test_prd225_*.py + test_p2w2_webhook_dedup + test_p2w2_webhook_signature_reject + test_prd183_s1_catalog_webhook + test_prd189_s3_webhook_debounce). The lone composio wh_1 'failure' is cross-run Redis SETNX state on the fixed id wh_1 (api/composio.py path — untouched by this change) — proven 167/167 green on a cleared key"
   ],
   "files": [
    "orchestrator/api/webhooks.py",
    "orchestrator/core/routing/ingestors/webhook.py",
    "orchestrator/tests/test_prd225_trust_gate.py"
   ]
  },
  {
   "id": "P225-RVW-17",
   "title": "HIGH — the Telegram answer confirmation says 'the agent is resuming' unconditionally; RVW-11's honesty gate never reached the second (Telegram) confirmation channel, and the CAS race-loser is told 'resuming' too",
   "description": "REVIEW FINDING (code-reviewer HIGH, verified). _apply_telegram_answer (api/webhooks.py:359-384) calls `await apply_question_answer(...)` and DISCARDS its return, then unconditionally replies f'Answered #{grant.id} — the agent is resuming.' (webhooks.py:376). P225-RVW-11 made the in-app confirmation honest — _confirm_answer_into_chat(resumed=...) (api/approval_grants.py:359-386) says 'nothing to auto-resume' when resumed is False — but that honesty was never propagated to this SECOND, independent confirmation channel. Two reachable defects: (a) two people answer the same Telegram-delivered question (a Telegram reply racing POST /{id}/answer, or two members of a shared authorised chat — auth is chat-scoped not principal-scoped, the exact RVW-14 race): the loser's apply_question_answer hits the CAS 0-row branch (approval_grants.py:305-310) and resumes/records NOTHING, yet webhooks.py still tells that user 'the agent is resuming' — false, and it was not even their answer that applied; (b) the winner whose _requeue_subject returns False (resumed False) gets the in-app 'nothing to auto-resume' AND the Telegram 'resuming' for the SAME answer — a direct self-contradiction across the two channels. This is the RVW-8/RVW-11 the-tool-lied class, on the one confirmation channel the honesty fix missed.",
   "acceptanceCriteria": [
    "DONE — apply_question_answer now returns AnswerOutcome(grant, applied, resumed) NamedTuple (approval_grants.py): lost-race branch → AnswerOutcome(grant, applied=False, resumed=False); won → AnswerOutcome(grant, applied=True, resumed=<_requeue_subject result>). No caller reads the return as a grant (verified by grep): the HTTP answer_question path uses the outer grant object and ignores the tuple (unaffected); grant_approval/deny/revoke untouched",
    "DONE — _apply_telegram_answer branches on outcome (webhooks.py): applied+resumed → 'Answered #N — the agent is resuming.'; applied+not-resumed → 'Answer recorded for #N — nothing to auto-resume.' (mirrors _confirm_answer_into_chat, no 'resuming'); not-applied → 'Question #N was already answered.' — never a false resume. Tests test_telegram_confirmation_is_honest_on_no_resume (monkeypatch _requeue_subject→False) + test_telegram_lost_race_reports_already_answered (CAS 0-row loser, grant not overwritten) both green",
    "DONE — cd orchestrator && python3 -m pytest -q tests/test_prd225_telegram_bridge.py tests/test_prd225_answer_resume.py = green; approval-kind regression slice green (test_prd181_approval_grants + test_p2w2_grant_resume/tool_grant_consume/grants_oversight/grant_mutation_admin_gate + test_grant_approve_commit_ordering + test_prd200_approval_renotify) — 71 passed together, ZERO approval-kind test-file edits (only test_prd225_telegram_bridge.py changed); full PRD-225 backend suite 139 passed"
   ],
   "files": [
    "orchestrator/api/webhooks.py",
    "orchestrator/api/approval_grants.py",
    "orchestrator/tests/test_prd225_telegram_bridge.py"
   ]
  },
  {
   "id": "P225-RVW-18",
   "title": "MEDIUM — deny/revoke have no compare-and-swap; a /deny (or /revoke) racing a just-applied /answer overwrites the answered grant to 'denied', leaving a self-contradictory governance/audit row (RVW-14 class, not extended to deny/revoke)",
   "description": "REVIEW FINDING (code-reviewer flagged HIGH; rated MEDIUM here to match its own class precedent RVW-14, which was MEDIUM — the board task itself stays correctly resumed, the damage is an inconsistent audited governance row, not a security breach or task corruption). deny_approval (api/approval_grants.py:172-196) and revoke_approval (:199-215) do a plain non-locking SELECT (_load_grant, :124-132) -> Python status check (`if grant.status != PENDING: 422`) -> deny_grant/revoke_grant mutate status in-memory (core/services/approval_grants.py — status set, NO WHERE guard, verified) -> db.commit() whose SQLAlchemy UPDATE is keyed on the PK only (WHERE id=:id). P225-RVW-14 added a CAS (UPDATE ... WHERE id AND status='pending') to apply_question_answer (:289-304) for EXACTLY this race but did NOT extend it to deny/revoke; ApprovalGrant has no optimistic-lock column (verified) and there is no SELECT..FOR UPDATE. Race: an admin dismisses a question in the Questions tab while a Telegram reply (or another admin) answers it — both funnel through the same grant. If /answer's CAS commits first (status->granted, answer_text/answered_by set, human_qa appended, board task re-queued to 'assigned'), the racing /deny — its Python check passed on the stale pending read, its UPDATE has no status guard — overwrites status->'denied' (+revoked_by) unconditionally. Result: a grant row that says status='denied'+revoked_by AND answer_text/answered_by/answered_at populated, with BOTH a 'question:answered' AND a 'question:dismissed' entry in the EU-AI-Act-audited log for one id. (Reverse order is safe — the answer's CAS no-ops.)",
   "acceptanceCriteria": [
    "DONE — new _cas_resolve_grant(db, grant, *, to_status, allowed_from, actor, now) (approval_grants.py) does the atomic UPDATE approval_grants SET status, revoked_at, revoked_by WHERE id AND status IN(allowed_from), synchronize_session=False: deny_approval → allowed_from=(pending,) → to 'denied'; revoke_approval → allowed_from=(pending,granted) → to 'revoked'. 0-row → db.rollback()+refresh → return False → endpoint raises 422 (no in-memory mutate, no _fail_subject, no audit-as-success, no commit); on win it syncs the in-memory row. Mirrors apply_question_answer's CAS (the deny/revoke endpoints previously did _load_grant→python-check→deny_grant/revoke_grant in-memory→PK-only commit)",
    "DONE — test_deny_racing_answer_is_a_noop (apply_question_answer flips pending→granted; racing deny_approval → 422, grant STAYS granted, answer_text 'Ship A' intact, revoked_by None) + test_revoke_racing_resolution_is_a_noop (first revoke wins granted→revoked; racing second → 422, revoked_by unchanged). Behaviour-preserving on the uncontended path (test_dismiss_leaves_subject_blocked still green). Fake _Query.filter() taught column.in_([...]) (list→set membership) — mechanics only, ZERO assertion changes; only test_prd225_answer_resume.py edited (approval-kind regression files untouched). Shared deny_grant/revoke_grant service fns kept (other callers e.g. tool_grants.py:345)",
    "DONE — cd orchestrator && python3 -m pytest -q tests/test_prd225_answer_resume.py = 13 passed; approval-kind regression slice green together = 59 passed / 8 @integration-skipped (test_prd181_approval_grants + test_p2w2_grant_resume/grants_oversight/tool_grant_consume/grant_mutation_admin_gate + test_grant_approve_commit_ordering + test_prd200_approval_renotify + test_prd204_rerun); full PRD-225 + tool-grant suite 158 passed"
   ],
   "files": [
    "orchestrator/api/approval_grants.py",
    "orchestrator/tests/"
   ]
  }
 ]
}
