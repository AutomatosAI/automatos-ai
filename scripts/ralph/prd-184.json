{
  "project": "Automatos AI Platform — PRD-184 Kill-list (Phase-2 reserved deletion slot) — DELETE-NOW tier only",
  "branchName": "ralph/prd-184-kill-list-dead-surface",
  "description": "Cut from origin/main @ 9dd4c848a — re-verify EVERY path/anchor by grep, they drift. SCOPE = the DELETE-NOW tier ONLY (grep-proven zero-caller dead/fabricating surface): 6 stories US-001..US-006. EXPLICITLY OUT OF SCOPE for this Ralph run (Gerard's call / needs care, DO NOT build, DO NOT add as acceptance): S7 /execute wire-or-delete (open decision), S8 KG prove-then-cut (needs consumer trace), S9 legacy-workflow-engine RETIRE (needs jira_bug_triage migrated onto Mission/Playbook first — behaviour change), S10 PlaybookMiner+/chat/[id] RETIRE (has live callers to migrate), and the 4 decide-then-cut items. mem0 residue is OWNED BY PRD-211, NOT here. Lockfiles are OWNED BY PRD-209, NOT here. THE DELETION GATE (non-negotiable): each target is DELETED only after grep-proves ZERO live callers on this branch; if a target has a real caller, that story is RALPH_BLOCKED with the caller cited — NEVER force-delete a referenced symbol, NEVER flatten a live caller. Every deletion ships a source-grep GUARD test in the SAME commit so the surface cannot silently regrow (the recurring guard-drift lesson: add/repoint the guard where the symbol dies). BINDING: no os.getenv outside config.py; NO backward-compat shim (delete, don't _legacy); trim the owning barrel __init__ in the same commit; PURE tests only; NEVER 'git add -A'/'git add .' (node_modules is untracked and NOT gitignored — a blind add poisons the branch); stage only the specific paths (git rm <path> / git add <specific>); never 'git stash -u'. Full spec: docs/PRDS/PRD-184-KILL-LIST-DEAD-SURFACE-REMOVAL.md (§5 audit table — DONE items are already cut by waves 0-3; build ONLY the 8 REMAINING minus the 4 held above = these 6).",
  "userStories": [
    {
      "id": "US-001",
      "title": "Delete the learning/evaluation theatre packages",
      "description": "Delete the two empty-theatre packages that signpost away from the real loops: `orchestrator/modules/evaluation/` (~30 LOC, zero external callers) and `orchestrator/modules/learning/` (__init__, feedback/, patterns/, plus playbooks/ EXCEPT do NOT touch miner.py — that is the held S10 retire). Grep-prove the only live importers are `orchestrator/modules/__init__.py` (trim the re-export in the same commit) — api_playbooks.py is the held S10 retire, out of scope. If ANY other live importer exists, RALPH_BLOCKED with the caller cited.",
      "acceptanceCriteria": [
        "DONE — grep-proven: `git grep learning.feedback|learning.patterns|from .feedback|from .patterns` = EMPTY (zero importers); `modules.evaluation` only its own now-deleted __init__ docstring. modules.learning's sole LIVE importer is the held api/api_playbooks.py:13 (mounted main.py:55) via PlaybookMiner + modules/__init__ docstring. Zero live importers of the deleted tree.",
        "DONE — evaluation/ fully deleted; the dead theatre learning/feedback/ + learning/patterns/ (both EMPTY __init__.py, 0 bytes) deleted. learning/__init__.py + playbooks/ + tests/ KEPT: they are the live reachability chain for the held PlaybookMiner (api_playbooks.py) — `from modules.learning import PlaybookMiner` cannot resolve without them, and the deletion gate forbids breaking a live held caller. AC's literal 'learning/ deleted except miner.py' is unsatisfiable while honoring that boundary; the true dead theatre is fully cut.",
        "DONE — modules/__init__.py: 'evaluation' removed from __all__ + docstring; 'learning' kept (survives). `python3 -c 'import modules'` clean (__all__ drops evaluation); modules.evaluation/learning.feedback/learning.patterns now raise ModuleNotFoundError.",
        "DONE — tests/test_prd184_us001_learning_evaluation_deleted.py added: 4 pure file-read tests incl. test_no_learning_evaluation_imports + a held-PlaybookMiner-chain boundary proof. RED before deletion (3 fail: dirs exist, barrel lists evaluation), GREEN after (4 passed).",
        "DONE — PURE (file-read only, imports no app pkg). Post-change full suite: 3 failed / 3972 passed / 46 errors — ALL pre-existing infra gates (Clerk unset, pg_dump 14.20-vs-server-18.3, shared-Redis webhook flake, Postgres-refused DB tests); grep-proven NONE of the 3 failures reference the deleted packages. Zero new failures from this change.",
        "DONE — no mypy/pyright lane in repo (checked orchestrator/ + .github/); pytest collects 4180 clean + `import modules` resolves = import/compile-clean (typecheck-equivalent for a pure deletion)."
      ],
      "files": ["orchestrator/modules/evaluation/", "orchestrator/modules/learning/", "orchestrator/modules/__init__.py", "orchestrator/tests/"]
    },
    {
      "id": "US-002",
      "title": "Delete the llm-core dead scaffolding (incl. anthropic_client.py)",
      "description": "Delete the ~1,700 LOC of zero-external-caller llm-core scaffolding: `orchestrator/core/llm/function_executor.py`, `function_registry.py`, `response_parser.py`, `semantic_skill_matcher.py`, `orchestrator/core/global_function_registry.py`, and `orchestrator/api/anthropic_client.py` (zero importers — the earlier broad grep hits were the Anthropic SDK variable name, re-verify). These survive ONLY via the `orchestrator/core/llm/__init__.py` barrel (~lines 40-43, grep to confirm) — repoint/trim the barrel in the SAME commit. This story OWNS anthropic_client.py (PRD-212 defers to it). Any real external caller of any symbol → RALPH_BLOCKED, cite it.",
      "acceptanceCriteria": [
        "DONE — grep-proven: the 4 core/llm files are re-exported ONLY by the core/llm/__init__ barrel (function_executor's `from .function_registry` is an internal cross-import, both die together); zero external symbol importers of FunctionExecutor/FunctionResult/FunctionRegistry/FunctionSpec/ResponseParser/ParsedResponse/SemanticSkillMatcher/get_skill_matcher (the only hit — tool_registry.py:21 'GlobalFunctionRegistry' — is a docstring prose bullet). global_function_registry + api/anthropic_client = ZERO importers.",
        "DONE — all 6 files git-rm'd; core/llm/__init__.py trimmed (removed the 4 import lines + 10 __all__ entries + stale docstring bullets), KEPT rerank_manager + manager + clients + embeddings. `import core.llm` clean: __all__ now the 10 live infra symbols only; dead modules raise ModuleNotFoundError.",
        "DONE — api/anthropic_client.py deleted. It was a STRANDED dead duplicate (does `from .base import ...` but api/base.py doesn't exist → not importable; zero importers). NOT mounted (grep main.py empty), NOT a router. The LIVE provider core/llm/clients/anthropic_client.py (used by manager.py:586) SURVIVES — guarded.",
        "DONE — tests/test_prd184_us002_llm_core_scaffolding_deleted.py added: 4 pure file-read tests incl. test_llm_core_no_dead_scaffolding + a live-provider boundary proof. RED before deletion (3 fail), GREEN after (4 passed).",
        "DONE — PURE (file-read only). Post-change full suite: 3 failed / 3976 passed / 46 errors — IDENTICAL failure+error signature to the US-001 baseline (same 3 infra gates, same 46 DB-refused errors); passed +4 = the new guards exactly. Zero regression.",
        "DONE — no mypy/pyright lane in repo; `import core.llm` resolves + pytest collects clean = import/compile-clean (typecheck-equivalent for a pure deletion)."
      ],
      "files": ["orchestrator/core/llm/function_executor.py", "orchestrator/core/llm/function_registry.py", "orchestrator/core/llm/response_parser.py", "orchestrator/core/llm/semantic_skill_matcher.py", "orchestrator/core/global_function_registry.py", "orchestrator/api/anthropic_client.py", "orchestrator/core/llm/__init__.py", "orchestrator/tests/"]
    },
    {
      "id": "US-003",
      "title": "Delete the exec_planning stub vertical and de-route it",
      "description": "Delete `orchestrator/modules/tools/execution/exec_planning.py` (~339 LOC, 8 hardcoded template-writer 'planning' tools, 0 LLM, exposed to no agent). De-route in the SAME commit: remove the import at `unified_executor.py` (~:36) and the dispatch branches (~:969-975, grep to confirm), and drop the 8 tool names from any registry. Grep-prove NO agent toolset references those 8 tool names BEFORE cutting; if any agent is wired to them, RALPH_BLOCKED.",
      "acceptanceCriteria": [
        "DONE — grep-proven (Python-driven, shell-quoting-safe) all 10 routed tool names (create_implementation_plan/write_technical_content/refine_content/write_document/review_code/security_scan/generate_tests/run_tests/research_topic/analyze_data — the JSON's '8' was anchor drift): 7 have ZERO repo hits; security_scan(40)=plugin_security_scanner/security_scan_id subsystem, run_tests(4)=UI placeholder+skill-string+README+playbook step_id, analyze_data(2)=analyze_database substring — NONE is an agent toolset/registry/skill wiring. `exec_planning` + the 3 handler methods are referenced ONLY in unified_executor.py; NO test asserts them.",
        "DONE — exec_planning.py git-rm'd; in unified_executor.py (same commit) removed: the import (line 36), the 2 PRD-22 Expansion dispatch blocks (10 entries), and the 3 handler methods (_execute_planning_tool/_execute_writing_tool/_execute_analysis_tool). File parses; `from ...unified_executor import UnifiedToolExecutor` imports clean; sibling dispatch entries (document/composio/widget) intact.",
        "DONE — tests/test_prd184_us003_exec_planning_deleted.py added: 3 pure file-read tests incl. test_exec_planning_deleted_and_unrouted + a live-neighbour boundary proof. RED before (2 fail), GREEN after (3 passed).",
        "DONE — PURE (file-read only). Post-change full suite: 3 failed / 3979 passed / 46 errors — identical infra signature to US-001/US-002 (same 3 gates, same 46 DB errors); passed +3 = the new guards. No dispatch regression.",
        "DONE — no mypy/pyright lane; unified_executor.py AST-parses + UnifiedToolExecutor imports + pytest collects clean = import/compile-clean (typecheck-equivalent)."
      ],
      "files": ["orchestrator/modules/tools/execution/exec_planning.py", "orchestrator/modules/tools/execution/unified_executor.py", "orchestrator/tests/"]
    },
    {
      "id": "US-004",
      "title": "Delete the dead tool-runtime concurrency helper (+ conditional ToolService)",
      "description": "DEFINITIVE: delete `orchestrator/modules/tools/execution/concurrency.py` (~166 LOC, zero callers — clean). CONDITIONAL (only if grep-proves zero live callers, else LEAVE and note in the story — do NOT flatten a live caller): `orchestrator/modules/tools/service.py` ToolService (live code uses ComposioToolService; delete only if the sole ToolService( site is the tools/__init__.py:10 barrel factory with no external caller, trimming the barrel too) and the specific crash-on-db_session delegate method in `composio_tool_router.py` (the FILE stays — it is live via exec_composio/unified_executor; excise only the dead delegate method). concurrency.py is the acceptance gate; ToolService/router-delegate are best-effort.",
      "acceptanceCriteria": [
        "DONE — concurrency.py grep-proven ZERO callers: `execution.concurrency`/`from .concurrency`/`import concurrency` = 0 importers; its two exported symbols is_read_safe+partition_tool_batch = 0 hits tree-wide (the services.concurrency_guard/starlette.concurrency hits are unrelated modules); execution/__init__.py imports only UnifiedToolExecutor. git-rm'd (166 LOC).",
        "DONE — tests/test_prd184_us004_tools_concurrency_deleted.py added: test_no_tools_concurrency_import asserts the file is gone + all 4 tokens (path + is_read_safe + partition_tool_batch) absent across modules/services/core/api/consumers/evals. RED before (2 fail: concurrency.py+service.py existed), GREEN after (3 passed).",
        "DONE — ToolService grep-proven DEAD → DELETED: only refs were the owning barrel modules/tools/__init__.py (1 live `from .service import` + 2 __all__ entries + docstring Usage) + a modules/__init__.py docstring bullet; ZERO external importers (the modules.tools.services.* hits are the DIFFERENT plural ComposioToolService package). service.py git-rm'd (145 LOC); both barrels trimmed. composio_tool_router-delegate: the JSON's 'crash-on-db_session delegate' anchor DRIFTED — no such dead method exists; every method in composio_tool_router.py is reachable from the live get_tool_router_for_agent factory → LEFT UNTOUCHED.",
        "DONE — composio_tool_router.py UNTOUCHED & live: get_tool_router_for_agent imported by modules/tools/execution/composio_router_executor.py:29 (→ exec_composio → unified_executor); ComposioToolRouter.search_tools/execute_tool are called by that executor. No dead delegate method to excise. Boundary test test_composio_tool_router_stays_live guards the file + 4 live symbols survive.",
        "DONE — PURE (file-read only, imports no app pkg). Full suite: 3 failed / 3982 passed / 163 skipped / 46 errors — identical infra signature to US-003 baseline (3979→3982 = +3 new guards exactly; same 3 gates: Shopify-key boot / pg_dump 14-vs-18 / webhook-flake; same 46 DB-refused sqlalchemy errors). NONE of the 3 failures/46 errors reference concurrency/service/ToolService/router. `import modules.tools` resolves clean; `from modules.tools import ToolService` now ImportError.",
        "DONE — no mypy/pyright lane in repo; `import modules.tools` resolves + pytest collects clean = import/compile-clean (typecheck-equivalent for a pure deletion)."
      ],
      "files": ["orchestrator/modules/tools/execution/concurrency.py", "orchestrator/modules/tools/service.py", "orchestrator/modules/tools/__init__.py", "orchestrator/tests/"]
    },
    {
      "id": "US-005",
      "title": "Delete the 7 driverless legacy channel adapters",
      "description": "Delete the 7 byte-near-identical driverless adapters at `orchestrator/channels/{teams,google_chat,signal,imessage,irc,matrix,line}_adapter.py` (~1,570 LOC, no driver) and `_ping_platform_legacy` in `orchestrator/api/channels.py` (~:144, 0 callers). Grep-prove NO live channel dispatch imports them (the one active channel path does not) BEFORE deleting. Any live import → RALPH_BLOCKED, cite it.",
      "acceptanceCriteria": [
        "DONE — grep-proven: the 7 adapter classes (Teams/GoogleChat/Signal/IMessage/IRC/Matrix/Line-Adapter) were referenced ONLY by channels/manager.py::_ADAPTER_MAP (a lazy string→class registry) + the PRD-142 W3-S13 contract test's ADAPTERS list — ZERO live product importers. The live /api/channels connect/verify/webhook/poll flow AND GET /api/channels/platforms use channels.drivers.* EXCLUSIVELY, and the 7 have NO driver in channels/drivers/ (only telegram/slack/discord/whatsapp/webhook do). _ping_platform_legacy: only its own def @ api/channels.py:144 — 0 callers.",
        "DONE — 7 channels/{teams,google_chat,signal,imessage,irc,matrix,line}_adapter.py git-rm'd (~1,570 LOC); _ping_platform_legacy removed from api/channels.py; the 7 driverless entries trimmed from _ADAPTER_MAP (manager.py keeps telegram/slack/discord/whatsapp — the 4 with a channels/drivers/ driver); the W3-S13 contract test's ADAPTERS list trimmed to those 4 (contract still pins every shipped adapter — not weakened). _SUPPORTED_PLATFORMS/_REQUIRED_CONFIG LEFT intact: live string-keyed validation lists with a documented intentional-breadth purpose ('clear unsupported 400 vs 500; driver registry is source of truth') — they reference platform strings, NOT the adapters; trimming them would change connect-validation behaviour, out of this delete story's scope.",
        "DONE — tests/test_prd184_us005_legacy_channel_adapters_deleted.py added: 4 pure file-read tests (test_no_legacy_channel_adapters + test_adapter_map_de_routed + test_ping_platform_legacy_removed + a test_live_channel_adapters_survive boundary proof for the 4 survivors). RED before (3 fail: files/map/ping present), GREEN after (4 passed).",
        "DONE — PURE (file-read only). Full suite: 3 failed / 3937 passed / 163 skipped / 46 errors — identical infra signature (same 3 gates: Shopify-key boot / pg_dump / webhook-flake; same 46 DB-refused sqlalchemy errors). Delta vs US-004 baseline = -49 contract-test items (7 methods × 7 removed adapters) +4 new guards = 3982→3937 exactly. NONE of the 3 failures/46 errors reference the deleted adapters/manager/_ping_platform_legacy; the driver-based channel path is unaffected; the W3-S13 contract test is green with the 4 survivors.",
        "DONE — no mypy/pyright lane in repo; api/channels.py + channels/manager.py + the contract test AST-parse + pytest collects clean = import/compile-clean (typecheck-equivalent for a pure deletion)."
      ],
      "files": ["orchestrator/channels/", "orchestrator/api/channels.py", "orchestrator/tests/"]
    },
    {
      "id": "US-006",
      "title": "Delete the frontend placebo relics",
      "description": "Delete `frontend/app/api-control/page.jsx` (~98 LOC, placebo for the PRD-168-removed mock system) and `frontend/app/styleguide/page.tsx` (~442 LOC, routed in prod). Remove the fabricated `workspaceMeta='pilot · 11 op'` pill from `frontend/components/layout/studio-sidebar.tsx`. DO NOT touch lockfiles (PRD-209 owns) or the /chat/[id] route (held S10). Grep-prove no live route/link points at /api-control or /styleguide BEFORE deleting.",
      "acceptanceCriteria": [
        "grep-prove no live navigation/link targets /api-control or /styleguide BEFORE deletion",
        "app/api-control/ and app/styleguide/ deleted; the workspaceMeta 'pilot · 11 op' literal removed from studio-sidebar.tsx",
        "test_no_placebo_routes guard added (grep: no /api-control or /styleguide route dir; no workspaceMeta literal)",
        "frontend typecheck passes (cd frontend && the repo's tsc/vitest lane green); no lockfile touched",
        "Typecheck passes"
      ],
      "files": ["frontend/app/api-control/", "frontend/app/styleguide/", "frontend/components/layout/studio-sidebar.tsx"]
    }
  ]
}
