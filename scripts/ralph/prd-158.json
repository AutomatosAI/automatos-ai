{
  "project": "Automatos AI Platform — PRD-158 Teams Model & Knowledge UX (Remediation Chain Block B)",
  "branchName": "ralph/prd-158-teams-knowledge-ux",
  "description": "Branch from main after PRD-157 merges (uses its filter builder) — re-verify refs. Finishes locked decision D1 end-to-end: real teams table, dropdowns not free-text, server-side team filtering, post-upload team management. BINDING: Q2 teams table + single-team agents (docs stay multi-team), Q3 selector is a pure human filter, Q5 per-connection cloud default. Full spec docs/PRDS/PRD-158-TEAMS-MODEL-KNOWLEDGE-UX.md.",
  "userStories": [
    {
      "id": "P158-S1",
      "title": "teams table + /api/teams + one normalizer",
      "description": "Migration: teams(id, workspace_id, name, normalized_name unique per workspace); backfill DISTINCT lowercased values from agents.team + documents.team_access. GET/POST /api/teams; org-chart joins the table; ALL writes normalize through the one helper in core/team_access.py (no second normalizer).",
      "acceptanceCriteria": [
        "Backfill test: 'Support'/'support' fixtures → one team",
        "Reversible migration, single alembic head",
        "org-chart and /api/teams agree (consistency test)",
        "cd orchestrator && pytest -q green"
      ],
      "files": ["orchestrator/alembic/", "orchestrator/core/team_access.py", "orchestrator/api/agents.py"]
    },
    {
      "id": "P158-S2",
      "title": "Team dropdowns: upload + cloud-sync connections",
      "description": "Replace free-text chips in document-upload.tsx:448-482 and upload-provider-modal.tsx:204-232 with multi-select from /api/teams (+ admin inline create). Cloud-storage connection settings gain a default-team picker applied to synced docs (BINDING Q5).",
      "acceptanceCriteria": [
        "Upload with 2 teams persists normalized team_access (API test)",
        "Grep gate: no free-text team input path remains",
        "Cloud-synced doc inherits connection default (test)",
        "Verify both dialogs in browser using dev-browser skill; typecheck passes"
      ],
      "files": ["frontend/components/documents/document-upload.tsx", "frontend/components/documents/upload-provider-modal.tsx", "frontend/components/documents/cloud-storage-panel.tsx"]
    },
    {
      "id": "P158-S3",
      "title": "Knowledge page server-side team filter + counts + eye-view",
      "description": "GET /api/documents gains team= (through the PRD-157 filter builder); the team selector moves from inside LocalStorageBrowser to the page header as a primary filter with per-team counts; 'view as team X' toggle shows the agent-eye-view for scope debugging.",
      "acceptanceCriteria": [
        "?team= filters server-side with counts on a >100-doc fixture (proves not the client-side ≤100 hack)",
        "Selector usable without entering Library cards — verify in browser using dev-browser skill",
        "Typecheck passes; cd orchestrator && pytest -q green"
      ],
      "files": ["orchestrator/api/documents.py", "frontend/components/documents/document-management.tsx"]
    },
    {
      "id": "P158-S4",
      "title": "Team management after upload",
      "description": "Team chips + editor in document-details modal; bulk team assignment over the PRD-154-fixed PATCH/bulk endpoints; team badges on document rows.",
      "acceptanceCriteria": [
        "Edit teams on an existing doc round-trips (API test + dev-browser verify)",
        "Bulk assign 10 docs (test)",
        "Typecheck passes"
      ],
      "files": ["frontend/components/documents/document-details-modal.tsx"]
    },
    {
      "id": "P158-S5",
      "title": "Widget/SDK docs surface on the real schema",
      "description": "api/widgets/docs.py queries documents.title/content/updated_at — columns that don't exist in core/models/core.py:548-580 (surface can never have worked against repo schema). Verify prod drift (Q7), reconcile if needed, rebuild the queries on the real schema + the PRD-157 retrieval path (vector search, not ILIKE).",
      "acceptanceCriteria": [
        "Drift check documented in PR; queries match the model",
        "Widget docs search returns results on seeded data (test)",
        "cd orchestrator && pytest -q green"
      ],
      "files": ["orchestrator/api/widgets/docs.py"]
    }
  ]
}
