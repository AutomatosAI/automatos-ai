{
  "project": "Automatos AI Platform — PRD-163 Missions Lifecycle & Plan Mode for Auto (Remediation Chain Block A)",
  "branchName": "ralph/prd-163-missions-planmode",
  "description": "Branch from main after Night-1 — re-verify refs (PRD-154 S5 fixed platform_get_mission, context_messages, duplicate route). Auto plans like Claude Code: read-only exploration → structured plan → approval (human or policy) → execution with honest lifecycle control. BINDING: D9 policy enum + $ ceiling + countdown card, D11 dollar budgets, Q53 AWAITING_HUMAN stays dead, Q54 approved plans execute verbatim via plan-import, Q55 MIN_TASKS floor dies, Q56 created_by = chatting user, Q57 approval-time edits get an apply path. Full spec docs/PRDS/PRD-163-MISSIONS-LIFECYCLE-PLAN-MODE.md.",
  "userStories": [
    {
      "id": "P163-S1",
      "title": "Mission lifecycle tools for Auto + correct attribution",
      "description": "Register approve/reject/pause/resume/cancel/replan as platform tools over EXISTING coordinator methods (3-file pattern), each declaring needs_approval semantics. mission_plan_ready + awaiting_approval notifications resolve to the creating USER (Q56 — today the agent is created_by and notification resolution breaks).",
      "acceptanceCriteria": [
        "Auto drives a seeded mission pause→resume→cancel (integration test)",
        "Notifications land for the chatting user (test)",
        "PRD-155 reachability green; cd orchestrator && pytest -q green"
      ],
      "files": ["orchestrator/modules/tools/discovery/handlers_missions.py", "orchestrator/modules/coordination/coordinator_service.py"]
    },
    {
      "id": "P163-S2",
      "title": "Plan mode (read-only) + verbatim plan import",
      "description": "plan_only mission create: planner runs with the dispatcher tool schema filtered to permission_level=read, returns the full plan (tasks, agents, deps, cost estimate) WITHOUT executing. platform_update_mission_plan + plan-import endpoint accept (possibly edited) planner JSON and execute it VERBATIM — planner validates structure only, no re-decomposition (Q54). Remove the MIN_TASKS=3 floor (Q55).",
      "acceptanceCriteria": [
        "Plan-mode run makes zero write-tool calls (tool-audit test)",
        "Imported plan executes the exact DAG (plan-vs-executed diff test)",
        "1-task plan is legal (test)",
        "cd orchestrator && pytest -q green"
      ],
      "files": ["orchestrator/modules/coordination/planner.py", "orchestrator/api/"]
    },
    {
      "id": "P163-S3",
      "title": "Approval policy engine: enum + dollar ceiling + countdown + audit",
      "description": "Workspace policy enum always_ask | auto_below_budget | full_auto, approval_dollar_ceiling, optional auto_proceed_after_seconds (countdown); per-request chat override ('auto-approve this one'); full_auto requires the §12.3 autonomy gate flag; every auto-approval writes a DISTINCT audit event with policy + ceiling snapshot.",
      "acceptanceCriteria": [
        "Policy matrix test: enum × under/over ceiling × gate on/off → expected path",
        "Countdown fires and is cancelable (test)",
        "Audit event on every auto-approval (test)",
        "cd orchestrator && pytest -q green"
      ],
      "files": ["orchestrator/modules/coordination/", "orchestrator/config.py"]
    },
    {
      "id": "P163-S4",
      "title": "In-chat approval card with apply-able edits",
      "description": "AG-UI-shaped interrupt card in chat: plan summary, cost estimate vs ceiling, per-task agent-override editing that PATCHes the plan before approval (Q57 apply path), approve/reject/auto-proceed countdown. Widget registered with live tool names from day one (reachability-validated).",
      "acceptanceCriteria": [
        "Auto-created mission surfaces the card; approving starts execution — verify in browser using dev-browser skill",
        "Edited agent override persists into execution (test)",
        "Reject returns structured feedback into Auto's context (test)",
        "Typecheck passes"
      ],
      "files": ["frontend/components/chat/", "frontend/components/widgets/"]
    },
    {
      "id": "P163-S5",
      "title": "Dollar budgets, async planning, real DAG edges, dead-path deletion",
      "description": "Dollar-ceiling budget policy replaces the token-estimate pause (update test_budget_gate.py, don't skip it); timeout scaling per power mode; real dependency edges in TaskResponse + the DAG canvas renders them; async planning (create returns immediately, plan arrives via notification); DELETE dead AWAITING_HUMAN panel/endpoints and non-applying plan-modification stubs (Q53/Q57 resolution).",
      "acceptanceCriteria": [
        "Budget pause at $ ceiling in a metered test, resumable after raise",
        "Seeded multi-dep plan renders real edges — verify in browser using dev-browser skill",
        "Dead paths deleted; contract green; test_budget_gate.py updated",
        "Recipe suite (20) green; cd orchestrator && pytest -q green"
      ],
      "files": ["orchestrator/modules/coordination/", "frontend/components/missions/mission-dag-canvas.tsx", "orchestrator/tests/test_budget_gate.py"]
    }
  ]
}
