{
 "project": "Automatos AI Platform — PRD-226 The Manager's Doctrine (Auto-as-Manager wave, chain 3/6)",
 "branchName": "ralph/prd-226-manager-doctrine",
 "description": "STACKED branch: ralph/prd-226-manager-doctrine ← ralph/prd-224-ticket-lane (chain 3/6; 227's wiring + 224's ASSIGN lane and ticket watches are IN your tree — the three-lane rubric you write must match how 224 actually behaves; diff scope = merge-base vs the 224 branch). Spec seeded at docs/PRDS/PRD-226-AUTO-MANAGER-DOCTRINE.md; wave map docs/PRDS/PRD-WAVE-AUTO-MANAGER.md. SCOPE = the management doctrine as content: (US-001) the 9-point doctrine into Auto's seeded soul + platform-management skill, with an idempotent hash-guarded backfill for existing workspace Auto rows; (US-002) the AutoBrain assessment prompt carries the three-lane rubric + reuse-before-create; (US-003) MissionPlanner emits 4-part dispatch contracts (OBJECTIVE·OUTPUT·TOOLS·BOUNDARIES + definition_of_done) and verification consumes the DoD when present; the ASSIGN lane's ticket-description directive shares the SAME contract fragment (single source). BAKED DECISIONS (Gerard 2026-08-27, from PRD-226 §8 proposals): Auto KEEPS the CTO voice — the doctrine is added as management sections, not a persona rewrite; narration doctrine is always-on (no per-workspace dial this wave); backfill replaces a workspace's soul ONLY when its current text hash-matches a previously shipped seed version — customized souls are SKIPPED and reported in the backfill output. THE DOCTRINE (write it from PRD-226 §4, adapted): 1 Awareness (ground answers in platform_board_summary/platform_list_missions/platform_list_agents until PRD-228's fleet tool lands) · 2 Three lanes chosen deliberately (DELEGATE=answer this conversation; ASSIGN=named single agent off-thread on the board, supervised; MISSION=multi-agent project) — say which lane and why in one line · 3 Delegate-don't-implement · 4 Reuse before creating (check the roster; honor named routing; 'one capable owner beats a duplicate'; say that you checked) · 5 Dispatch as a 4-part contract, references not pasted content · 6 Board as ledger (multi-step asks get a board card) · 7 Asks are decisions not reports (one bold sentence, options as bullets, ≤~700 chars, markdown; never idle-wait — park and move on) · 8 Recurring work becomes a playbook (propose platform_create_playbook + schedule on repetition) · 9 Narrate (one-line explanations of assignments/escalations/sign-offs). ANCHORS (verified 2026-08-27 — RE-VERIFY by grep): seeds orchestrator/core/seeds/auto-cto-custom-soul.txt + platform-management-skill.md, applied by orchestrator/core/seeds/seed_auto_agent.py:29-84 (per-workspace rows, slug auto-{workspace_id}; find its existing update/sync mechanism and follow it for the backfill); AutoBrain assessment prompt builder in orchestrator/consumers/chatbot/auto.py (rubric from 224's US-004 is already in the tree — EXTEND it, don't duplicate); planner prompt orchestrator/modules/coordination/planner.py:341,488, prompt ~:756, parse ~:1099; verification orchestrator/modules/coordination/verification.py. BINDING RULES: personas live in the DB, seed files are the source (house rule: no file hacks for DB data — the backfill writes rows); prompt fragments shared between the ASSIGN directive and the planner live in ONE place, never copy-pasted (grep must show a single definition); doctrine text changes are eval-gated — extend the LOCAL gold sets (fixtures NEVER committed: repo is public; write the eval harness additions so they load fixtures from the gitignored local path and SKIP cleanly when absent); token budget: the CHATBOT-context doctrine block stays ≤ ~350 tokens (assert a character ceiling in a test); ZERO new alembic revisions UNLESS seed_auto_agent's existing update mechanism requires a data migration — if it does, that is a hard STOP: reply RALPH_BLOCKED and say so (do not invent a migration path); NO new tables/routes; no os.getenv outside orchestrator/config.py; canonical terms; PURE tests; NEVER 'git add -A'/'git add .'/'-u'; never 'git stash -u'. No frontend stories.",
 "userStories": [
  {
   "id": "US-001",
   "title": "Doctrine into the soul + skill seeds, with hash-guarded backfill",
   "description": "Add the 9-point doctrine to core/seeds/auto-cto-custom-soul.txt (identity-level: lanes, delegate-don't-implement, reuse-before-create, narrate) and the procedural mechanics to core/seeds/platform-management-skill.md (contract shape, ask formatting, playbook habit, board-as-ledger) — keeping the CTO voice, replacing nothing of the existing identity. Then the backfill: follow seed_auto_agent.py's EXISTING update/sync mechanism (re-verify how it applies seed changes to existing per-workspace rows; if none exists, add an idempotent sync function invoked from the seed path) that updates a workspace's Auto row ONLY when its current soul text hash-matches any previously shipped seed version (keep a short list of known-version hashes beside the seed); customized souls are skipped and the skip is reported (log line with workspace id). A doctrine-block character-ceiling test enforces the ≤~350-token budget for the CHATBOT-context section.",
   "acceptanceCriteria": [
    "DONE — Both seed files carry the 9 doctrine points (string-presence test per point); CTO identity text retained (diff shows additions, not a rewrite). Evidence: test_soul_seed_carries_all_nine_doctrine_points + test_skill_seed_carries_all_nine_doctrine_points + test_soul_retains_cto_identity_additions_not_a_rewrite in tests/test_prd226_doctrine.py; `git diff HEAD -- auto-cto-custom-soul.txt` is append-only (existing CTO body untouched).",
    "DONE — Fresh-workspace seed applies the new soul (test via seed path); a row matching the OLD seed hash gets updated; a row with customized text is SKIPPED and reported (tests). Evidence: test_fresh_workspace_seed_applies_new_soul (drives seed_auto_agent), test_backfill_updates_old_friendly_default, test_backfill_updates_alembic_backfilled_default, test_backfill_skips_customized_soul_and_leaves_it_untouched, test_sync_auto_personas_reports_each_bucket (counts {updated:1,skipped:1,current:1}). Backfill runs on the EXISTING seed path — seed_auto_agent else-branch calls _backfill_auto_persona; callers api/chat.py:156, api/workspaces.py:353, core/auth/hybrid.py:370.",
    "DONE — Backfill is idempotent (running twice changes nothing the second time — test). Evidence: test_backfill_is_idempotent asserts (first,second)==('updated','current') with no second mutation.",
    "DONE — Doctrine block character ceiling asserted (test). Evidence: test_doctrine_block_character_ceiling asserts len(MANAGER_DOCTRINE_BLOCK) <= 1400 chars (~350 tokens).",
    "DONE — ZERO new alembic files. Evidence: `git status alembic/` clean; backfill uses the seed path (no migration). _ALEMBIC_BACKFILL_PERSONA is the text of the pre-existing seed_auto_agents_existing_workspaces.py migration, used only as a known-hash, not a new file.",
    "DONE — PRD-226 tests green: 11 passed in tests/test_prd226_doctrine.py. Full suite: 3 failed + 46 errors are ALL pre-existing/infra (no local Postgres → sqlalchemy errors; pg_dump 14 vs server 18; Clerk/Composio secrets unset) — none touch seeds/personas; zero regressions from this story."
   ],
   "files": [
    "orchestrator/core/seeds/auto-cto-custom-soul.txt",
    "orchestrator/core/seeds/platform-management-skill.md",
    "orchestrator/core/seeds/seed_auto_agent.py",
    "orchestrator/tests/"
   ]
  },
  {
   "id": "US-002",
   "title": "Assessment rubric: extend 224's three lanes with reuse-before-create",
   "description": "In the AutoBrain assessment prompt builder (auto.py — 224's US-004 rubric is already in your tree): extend with the reuse-before-create signals (prefer an existing roster agent whose role fits; honor named routing; creating a new agent requires 'nothing fits' and saying you checked) and the one-line lane-narration instruction. Do NOT duplicate the rubric — one rubric block, extended in place. LLM-free tests: string presence of the new signals in the built prompt; existing 224 lane tests stay green.",
   "acceptanceCriteria": [
    "DONE — Single rubric block (grep shows one definition site); reuse-before-create + named-routing + narrate-one-line present in the built prompt. Evidence: build_assessment_prompt extended IN PLACE (test_rubric_has_exactly_one_definition_site asserts exactly one `def build_assessment_prompt`); test_prompt_has_reuse_before_create_signals / test_prompt_honours_named_routing / test_prompt_instructs_one_line_lane_narration in tests/test_prd226_rubric.py.",
    "DONE — 224's stubbed-assessment tests unchanged and green. Evidence: tests/test_prd224_assign_lane.py untouched, 31 passed alongside the new rubric tests (36 passed together); test_three_lane_rubric_still_intact re-asserts 224's anchors survive the extension.",
    "DONE — PRD-226 rubric tests green: 5 passed (tests/test_prd226_rubric.py). Full suite: 3 failed + 46 errors are the same pre-existing infra baseline as before this story (4313→4318 passed, +5 = the new tests) — zero regressions."
   ],
   "files": [
    "orchestrator/consumers/chatbot/auto.py",
    "orchestrator/tests/"
   ]
  },
  {
   "id": "US-003",
   "title": "4-part dispatch contracts: planner emits, verification consumes, ASSIGN shares",
   "description": "One shared contract fragment (single definition — a constant/builder importable by both consumers): OBJECTIVE · OUTPUT · TOOLS (use/avoid + references to read instead of re-deriving) · BOUNDARIES (scope limits + definition of done), references not pasted content. (a) MissionPlanner.decompose's prompt (~planner.py:756) requires each task spec in that shape; the parser (~:1099) extracts definition_of_done into the task spec dict (store inside the EXISTING plan/task JSONB — rebuild-don't-mutate; NO schema change). (b) verification.py consumes definition_of_done when present (score against it; absent → today's inference path unchanged — no hard dependency). (c) The 224 ASSIGN directive's ticket-description instruction imports the SAME fragment. Tests: planner parse fixture with a 4-part spec extracts DoD; verification uses it when present and falls back cleanly when absent; grep proves one fragment definition with ≥2 importers.",
   "acceptanceCriteria": [
    "DONE — Shared fragment defined once, imported by planner prompt builder AND the ASSIGN directive (grep: 1 definition, ≥2 imports). Evidence: DISPATCH_CONTRACT_FRAGMENT defined ONLY in modules/coordination/dispatch_contract.py:15; imported by modules/coordination/planner.py:31 and consumers/chatbot/auto.py:37. Tests: test_fragment_has_exactly_one_definition_site (tree-walk), test_fragment_imported_by_planner_and_assign, test_both_consumers_bind_the_same_object (runtime `is` identity), test_planner_prompt_embeds_the_fragment, test_assign_directive_embeds_the_fragment_both_paths.",
    "DONE — Planner parse stores definition_of_done inside the existing JSONB spec (no schema change; rebuild-don't-mutate; test). Evidence: PlannedTask gained a frozen `definition_of_done` field; _parse_plan extracts it (test_parse_extracts_definition_of_done / absent→None / blank→None); it persists via the EXISTING input_context JSONB in coordinator_service._persist_decomposition (new dict, not mutated) and the plan-import path; test_no_schema_change_dod_rides_input_context_jsonb asserts OrchestrationTask has NO definition_of_done column but has input_context; test_planned_task_is_frozen_rebuild_not_mutate.",
    "DONE — Verification scores against DoD when present; absent-DoD path byte-identical behavior to before (regression test). Evidence: _build_judge_prompt gained an optional definition_of_done → dod_section is '' when absent (inserted as {criteria_text}{dod_section}); test_judge_prompt_absent_dod_is_byte_identical asserts base==explicit_none==blank and the exact criteria→deterministic bytes; test_judge_prompt_with_dod_scores_against_it; reconciler reads it from input_context JSONB (getattr-safe) and passes it through; W1-S9 idle-in-tx ordering test stays green.",
    "DONE — ZERO new alembic files. Evidence: `git status alembic/` clean; DoD rides existing input_context JSONB.",
    "DONE — PRD-226 contract tests green: 14 passed (tests/test_prd226_contract.py); 224 assign-lane suite 31 passed; planner/verification/coordinator suites 69 passed. Full suite: 3 failed + 46 errors = the same pre-existing infra baseline (4318→4332 passed, +14 = the new tests); the transient W1-S9 break from a getattr-less read was fixed before commit. Zero regressions."
   ],
   "files": [
    "orchestrator/modules/coordination/dispatch_contract.py",
    "orchestrator/modules/coordination/planner.py",
    "orchestrator/modules/coordination/verification.py",
    "orchestrator/modules/coordination/reconciler.py",
    "orchestrator/services/coordinator_service.py",
    "orchestrator/consumers/chatbot/auto.py",
    "orchestrator/tests/"
   ]
  }
 ]
}
