# Unified Notification System (PRD-128) Progress Log

Project: Automatos AI Platform
Feature: ralph/prd-128-unified-notifications
Started: 2026-04-10

---

## Story Status

- 2026-04-10: archived stale PRD-122 IMPLEMENTATION_PLAN; rebuilt plan for PRD-128 (10 stories).
- 2026-04-10: **US-001 done** — alembic migration `prd128_notifications` creates `notification_preferences` and `notifications` tables with all PRD-specified columns, FKs, and partial/composite indexes. Chained off `prd127_attachment_ids` so `down_revision` is not None. No unique constraint on `(workspace_id, user_id, event_type)` so events can fan out to multiple destinations.
- 2026-04-10: **US-002 done** — `_provision_new_user_workspace()` in `orchestrator/core/auth/hybrid.py` now seeds 9 default `notification_preferences` rows (heartbeat/task/mission_complete/playbook_complete/trigger_fired/report_submitted/agent_error=in_app, mission_step_complete/playbook_step_complete=silent) with `user_id=NULL, enabled=true`. Extracted `_seed_default_notification_preferences()` helper and `DEFAULT_NOTIFICATION_PREFERENCES` tuple as single source of truth. Idempotent via `INSERT ... WHERE NOT EXISTS` (table has no unique constraint because multi-destination fan-out is allowed). Helper does not commit — caller owns the transaction. New unit tests in `orchestrator/tests/test_prd128_default_prefs.py` (4 tests, all passing) verify: constant matches spec, fresh workspace inserts exactly 9 rows with correct params, re-run yields 0 inserts, generated SQL retains `WHERE NOT EXISTS` + `user_id IS NULL` guard. Gotcha: `core.auth.hybrid` imports `SessionLocal` at module load which requires Postgres env vars — test file sets dummy POSTGRES_* with `os.environ.setdefault` before the import to make the DB-free unit test collectable.
