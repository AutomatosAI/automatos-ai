# PRD-82B Mission Intelligence Layer — Ralph Progress
# Branch: ralph/prd-82b-mission-intelligence
# Started: 2026-03-16

## Iteration 1 — US-001: Create decomposition template library
- Created orchestrator/modules/coordination/templates.py
- 4 built-in templates: research_and_report (5 tasks), content_pipeline (4 tasks), competitive_analysis (4 tasks), data_investigation (3 tasks)
- DecompositionTemplate + TaskTemplate frozen dataclasses
- match_template() with keyword scoring (threshold=2)
- render_template() outputs PlannedTask-compatible dicts
- All agent roles match _ROLE_SYNONYMS: researcher, search, analyst, writer, reviewer
- All 3 unit test ACs pass (research match, content match, no match)

## Iteration 2 — US-002: Wire template matching into MissionPlanner
- Added `template_used: Optional[str]` field to DecompositionResult (default None, backward-compatible)
- Imported match_template + render_template from templates.py into planner.py
- Template matching runs FIRST in MissionPlanner.decompose() before LLM call
- If template matches: render → parse → validate → return (no LLM call)
- If template fails validation: falls through to LLM with warning log
- If no template match: falls through to LLM (existing 82A behavior unchanged)
- Logging: "template=%s matched" or "no template match, using LLM decomposition"
- Structural validation (DAG, agent roles) still runs on template output

## Iteration 3 — US-003: Wire history-based agent scoring in AgentMatcher
- Added COORDINATOR_HISTORY_LOOKBACK_DAYS (default 30) and COORDINATOR_HISTORY_MIN_DATAPOINTS (default 3) to config.py
- Implemented _build_history_map() — batch queries verified tasks + verification events in 2 queries (not N)
- Scores extracted from TASK_VERIFICATION_COMPLETED event payloads (avg of relevance/completeness/accuracy/format_compliance)
- Agents with < min_datapoints get neutral 0.5 (not enough data)
- Wired into AgentMatcher.match() alongside _build_tool_map() and _get_busy_agent_ids()
- _score_agent() now accepts history_score param (replaces hardcoded 0.5 placeholder)
- Debug logging updated to include hist= score
- Note: no verifier_score column exists on OrchestrationTask — scores live in event payloads, so query goes through OrchestrationEvent table

## Iteration 4 — US-004: Add telemetry query endpoints for missions
- Added COORDINATOR_COST_PER_1K_TOKENS (default 0.003) to config.py
- GET /api/missions/stats — aggregate stats: total_missions, success_rate, avg_duration_minutes, avg_tokens_used, avg_tasks_per_mission, top_agents (top 5 by verified tasks), common_failure_reasons. Filterable by period (7d/30d/90d/all)
- GET /api/missions/{id}/events — paginated events with event_type filter, limit/offset (default 50)
- GET /api/missions/{id}/cost — token usage breakdown per task + estimated cost using config rate
- GET /api/agents/{agent_id}/mission-history — tasks_completed, avg_tokens_per_task, failure_rate, recent_missions (last 10 tasks)
- Added agent_telemetry_router (separate prefix /api/agents) registered in main.py alongside missions_router
- /stats route declared BEFORE /{mission_id} to avoid FastAPI path conflict
- All endpoints use workspace auth (get_request_context_hybrid)
- Pydantic response models: MissionStatsResponse, MissionCostResponse, PaginatedEventsResponse, AgentMissionHistoryResponse, TopAgentStats, TaskCostBreakdown
- 67 existing tests pass, 1 pre-existing failure (test_agents_api_plugins.py) — no new failures

## Iteration 5 — US-005: Add replanning state and replan endpoint
- Added REPLANNING to RunState enum with StateType.ACTIVE
- Added RUN_REPLANNING + RUN_REPLANNED to EventType enum
- Added transitions: running→replanning, replanning→running, replanning→failed, failed→replanning
- Added RUN_STATE_TYPE mapping for REPLANNING
- Added _run_state_to_event_type mapping for REPLANNING
- Added replan_count (int, default 0) column to OrchestrationRun model
- Added COORDINATOR_MAX_REPLANS (default 2) to config.py
- Implemented MissionPlanner.replan() — LLM-based replanning with completed task context, failed task info, user notes
- Added _REPLAN_SYSTEM_PROMPT and _build_replan_prompt() in planner.py
- Implemented CoordinatorService.replan_mission() — full lifecycle: validate state/count → failed→replanning → gather outputs → planner.replan() → skip old failed/pending tasks → insert new tasks + deps → replanning→running → queue initial tasks
- New tasks get sequence numbers starting after existing max (no collision)
- Replan info stored in run.plan JSONB as replan_N key
- Added POST /api/missions/{id}/replan endpoint with MissionReplanRequest (notes: Optional[str])
- Endpoint validates: must be in 'failed' state, replan_count < max_replans
- Added replan_count to MissionResponse and _run_to_response helper
- Created Alembic migration prd82b_us005_replan for replan_count column + updated state CHECK constraint
- Emits RUN_REPLANNED event with replan_number, new_task_count, token_estimate, user_notes
- All imports validated, 67 tests pass, no new failures

## Iteration 6 — US-006: Add cross-task consistency verification
- Added COORDINATOR_CONSISTENCY_CHECK (default true) config flag to config.py
- Added CONSISTENCY_CHECKED event type to EventType enum
- Created ConsistencyIssue frozen dataclass (task_ids, description, severity)
- Created ConsistencyResult frozen dataclass (passed, score, reasoning, issues, tokens_used)
- Implemented VerificationService.verify_cross_task_consistency() — LLM-based cross-task check
  - Uses cross-model verifier (different family from executor)
  - Checks: contradictions, goal coverage, redundant duplication, logical coherence
  - Retry logic matches existing verify_task pattern (max_retries from config)
  - On all retries exhausted: defaults to passed (don't block human review)
  - Single-task missions skip check (returns passed)
- Implemented _build_consistency_prompt() — formats goal + all task outputs (4K char truncation per task)
- Added _CONSISTENCY_SYSTEM_PROMPT — instructs LLM to return JSON with passed/score/reasoning/issues
- Wired into coordinator_service.py:
  - _build_and_advance_to_awaiting_human() now async (was sync), calls _run_consistency_check()
  - _run_consistency_check() gathers verified task outputs, runs consistency verification
  - Consistency result attached to output_summary["consistency"] for human review context
  - Emits CONSISTENCY_CHECKED event with passed/score/reasoning/issue_count/high_severity_count
  - Token usage tracked on run.tokens_used
  - If consistency check disabled (config=false) or fails with exception: proceeds without blocking
- Updated caller to await the now-async method
- All 4 modified files pass Python syntax validation
