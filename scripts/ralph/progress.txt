# Ralph Progress Log
Started: Wed  4 Feb 2026 09:58:03 WET

## Codebase Patterns
- Models use `Base` from `core.database.base` (separate module to avoid circular imports)
- UUID PKs use `Column(PGUUID(as_uuid=True), primary_key=True, default=uuid4)` pattern (import `UUID as PGUUID` from `sqlalchemy.dialects.postgresql`)
- All models use `__table_args__ = {"extend_existing": True}` to prevent duplicate table errors
- Timestamps use `server_default=func.now()` for DB-side defaults
- ARRAY columns: `Column(ARRAY(String), default=list)` from `sqlalchemy`
- JSONB columns: `Column(JSONB, default=dict)` from `sqlalchemy.dialects.postgresql`
- Junction/association tables in core.py use `Table()` function with Integer FKs; newer models use `Column()` class approach
- Agent model uses Integer PK (id), Workspace uses UUID PK — FK references must match
- Users model uses Integer PK
- Model registration in `__init__.py` — import order matters for FK resolution
- Junction tables with composite PKs: set `primary_key=True` on each FK column, no separate `id` needed
- FK type must match target PK: Agent(Integer), Workspace(UUID), User(Integer), MarketplacePlugin(UUID)
- Config pattern: `Config` class in `orchestrator/config.py` with `os.getenv()` calls, singleton `config` instance
- Router registration in `main.py`: import router, `app.include_router(router)`, optional routers use try/except
- Frontend has no TypeScript compiler installed; `next.config.js` has `ignoreBuildErrors: true`
- Python has no type checker (mypy/pyright) configured; verify imports work as typecheck proxy
- AWS/boto3 pattern: use `try/except ImportError` guard, `boto3.client()` with `BotoConfig` for retries, wrap sync calls with `asyncio.run_in_executor()` for async
- S3 service reads creds from config singleton — `config.AWS_ACCESS_KEY_ID`, `config.AWS_SECRET_ACCESS_KEY`, `config.AWS_REGION`, `config.MARKETPLACE_S3_BUCKET`
- boto3 not installed locally; service guards with try/except and raises ImportError at instantiation time
- Pydantic v2 `model_` prefix is reserved; fields like `model_used` need `model_config = {"protected_namespaces": ()}` on the BaseModel class
- LLM JSON responses may have markdown code fences — strip before `json.loads()`
- Admin auth: `_assert_admin(ctx)` checks `ctx.user.system_role == 'admin'`, raises 403
- API auth dependency: `ctx: RequestContext = Depends(get_request_context_hybrid)` from `core.auth.hybrid`
- DB session dependency: `db: Session = Depends(get_db)` from `core.database.database`
- UploadFile multipart: `file: UploadFile = File(...)`, non-file fields use `Form()` not `Query()`
- Import services inside endpoint functions to avoid circular import at module load time
- FastAPI route ordering: fixed-path routes (e.g., `/categories`) must be defined before parameterized routes (e.g., `/{plugin_id}`) in the same router
- ARRAY contains-element filter: `Model.array_col.any(value)` for PostgreSQL ARRAY columns
- Workspace auth scoping: compare `ctx.workspace_id` with path param, raise 403 on mismatch
- Cascade deletes on disable: query Agent.id by workspace_id, then bulk delete junction rows with `.delete(synchronize_session="fetch")`
- PUT-replace pattern for junction tables: delete all existing rows first, then add new ones (simpler than diffing)
- Agent PK is Integer; path param hint is `agent_id: int` (not UUID)
- Seed functions that take `db: Session` param (like seed_system_settings, seed_personas) integrate cleanly with load_seed_data.py via `with get_db_session() as db`
- Agent.model_config is a JSON column (not a Pydantic `model_config` attr); access via `agent.model_config` returns dict with keys: provider, model_id, temperature, max_tokens
- ComposioAppCache + AgentAppAssignment are the tool assignment models; import from `core.models.composio_cache`
- S3 MarketplaceS3Service instantiation inside endpoint: `from core.services.marketplace_s3 import MarketplaceS3Service; s3 = MarketplaceS3Service()`
- Frontend file upload: use `apiClient.getAuthHeaders()` for auth, then raw `fetch()` with `FormData` body — don't set Content-Type header
- Admin pages use `usePageAPI('admin')` and follow standard page pattern: `MainLayout` wrapper, `glass-card` styling
- No admin directory existed before US-017; `frontend/app/admin/` is the established path for admin pages
- Workspace ID in frontend components: `localStorage.getItem('last_active_workspace') || localStorage.getItem('last_active_org')`
- Marketplace homepage dual Tabs pattern: one `<Tabs>` for TabsList triggers, separate `<Tabs value={selectedTab}>` for TabsContent — both share state
- Select dropdown: import `Select, SelectTrigger, SelectValue, SelectContent, SelectItem` from `@/components/ui/select`
- Agent persona API: GET `/api/agents/{id}/persona` returns `{persona_id, persona_name, use_custom_persona, custom_persona_prompt, system_prompt}`; PUT accepts `{persona_id?, custom_prompt?, use_custom: bool}`
- Persona save is independent per-section — use separate save handler rather than coupling with main config save

---

## 2026-02-04 - US-001
- Implemented all 4 marketplace database models: PluginCategory, MarketplacePlugin, PluginSecurityScan, PluginSyncHistory
- Files changed:
  - `orchestrator/core/models/marketplace_plugins.py` (created)
  - `orchestrator/core/models/__init__.py` (added import)
- **Learnings for future iterations:**
  - Use `PGUUID(as_uuid=True)` with `default=uuid4` for UUID PKs (matching Chat/Message pattern from core.py)
  - PluginSecurityScan table must be defined before MarketplacePlugin in the file since MarketplacePlugin has FK to it — but SQLAlchemy resolves string FK references lazily, so ordering doesn't matter in practice
  - Self-referential FK (PluginCategory.parent_id) needs `remote_side` in relationship
  - Composite indexes go in `__table_args__` tuple before the dict
---

## 2026-02-04 - US-002
- Implemented WorkspaceEnabledPlugin and AgentAssignedPlugin junction models
- Files changed:
  - `orchestrator/core/models/marketplace_plugins.py` (appended junction models)
- **Learnings for future iterations:**
  - Agent model uses Integer PK, Workspace uses UUID PK — FK types must match accordingly
  - Users model uses Integer PK — `enabled_by` FK is Integer, not UUID
  - Junction models with composite PKs: set `primary_key=True` on both FK columns, no separate `id` column needed
  - `ondelete="CASCADE"` on FKs ensures cleanup when parent records are removed
  - No changes needed in `__init__.py` since the file already imports `from .marketplace_plugins import *`
---

## 2026-02-04 - US-003
- Implemented Persona database model and added persona columns to Agent model
- Files changed:
  - `orchestrator/core/models/personas.py` (created)
  - `orchestrator/core/models/core.py` (added persona_id, custom_persona_prompt, use_custom_persona columns + persona relationship to Agent)
  - `orchestrator/core/models/__init__.py` (added `from .personas import *`)
- **Learnings for future iterations:**
  - core.py imports `UUID` (not `PGUUID`); marketplace_plugins.py imports `UUID as PGUUID` — both work for `as_uuid=True` pattern
  - Adding `index=True` inline on a column is equivalent to creating a named Index in `__table_args__` — simpler for single-column indexes
  - String FK references (e.g., `ForeignKey('personas.id')`) resolve lazily, so import order in `__init__.py` doesn't matter for FK resolution
  - Agent model already has many columns — persona columns added after marketplace fields, before evaluation fields
---

## 2026-02-04 - US-004
- Implemented MarketplaceS3Service with all required methods: upload_zip, extract_plugin, get_manifest, get_file, list_plugin_files, delete_plugin
- Added marketplace config vars to config.py: MARKETPLACE_S3_BUCKET, AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY, AWS_REGION, PLUGIN_MAX_UPLOAD_SIZE_MB, PLUGIN_LLM_SCAN_MODEL
- Updated .env.example with new env vars
- Files changed:
  - `orchestrator/core/services/marketplace_s3.py` (created)
  - `orchestrator/config.py` (added marketplace/S3 config section)
  - `orchestrator/.env.example` (added marketplace env vars)
- **Learnings for future iterations:**
  - boto3 is not installed in local dev env — use `try/except ImportError` guard pattern (same as bedrock_client.py)
  - Async S3 calls: wrap synchronous boto3 calls with `asyncio.get_running_loop().run_in_executor(None, lambda: ...)`
  - S3 `list_objects_v2` is paginated — handle `IsTruncated`/`NextContinuationToken` for large plugin directories
  - S3 `delete_objects` accepts max 1000 keys per call — batch if needed
  - Config follows simple class attribute pattern with `os.getenv()` — no properties needed for static values
  - Lambda default arg trick (`lambda k=s3_key: ...`) needed in loops to capture iteration variable
- Static scanner uses `async def` but is CPU-bound (regex); keeps consistent async interface for orchestrator
- Pydantic models in services use `from pydantic import BaseModel, Field` (same as routing.py / enhanced.py)
---

## 2026-02-04 - US-005
- Implemented static security scanner with 4 pattern categories: code execution, network access, filesystem operations, prompt injection
- Created Pydantic models: StaticFinding and StaticScanResult
- Async `static_scan(plugin_files)` function scans all text files line-by-line against all pattern groups
- Files changed:
  - `orchestrator/core/services/plugin_security_scanner.py` (created)
- **Learnings for future iterations:**
  - Pattern definitions use `List[Dict[str, str]]` for easy iteration; each dict has pattern, type, severity, description
  - `ALL_PATTERN_GROUPS` list enables iterating all categories without repetition
  - `re.IGNORECASE` flag on all pattern matches as required by AC
  - Module will be extended by US-006 (LLM scan) and US-007 (scan orchestrator) — structured to allow appending
  - pydantic not installed locally; syntax/AST check is the verification proxy
---

## 2026-02-04 - US-006
- Implemented LLM security scanner (Stage 2) using Claude Haiku via Anthropic API
- Added Pydantic models: LLMFinding and LLMScanResult (with `model_config = {"protected_namespaces": ()}` to avoid Pydantic warning on `model_used` field)
- Defined LLM_SECURITY_SCAN_PROMPT constant covering 6 threat categories: malicious code, prompt injection, data exfiltration, privilege escalation, social engineering, obfuscated code
- Implemented `async llm_security_scan()` function with full error handling and JSON response parsing
- Files changed:
  - `orchestrator/core/services/plugin_security_scanner.py` (appended LLM scan code)
- **Learnings for future iterations:**
  - Pydantic v2 reserves `model_` prefix as protected namespace; fields like `model_used` need `model_config = {"protected_namespaces": ()}` in the BaseModel class to suppress warnings
  - Anthropic API pattern: `client.messages.create()` with `system` as separate param (not a message), wrap with `run_in_executor` for async
  - LLM responses may include markdown code fences around JSON — strip ```` ```json `` `` ``` ```` before parsing
  - Config import inside function (`from config import config`) with fallback to `os.getenv()` for flexibility when module is loaded outside orchestrator context
  - Default model `claude-haiku-4-20250414` matches `PLUGIN_LLM_SCAN_MODEL` in config.py
---

## 2026-02-04 - US-007
- Implemented PluginScanService class that orchestrates Stage 1 (static) + Stage 2 (LLM) scans and persists results
- Constructor accepts db session (standard constructor injection pattern)
- `scan_plugin()` method: runs static_scan first, auto-blocks on critical findings (skips LLM scan), otherwise runs llm_security_scan
- Verdict logic: risk_score 0-19 = 'safe', 20-69 = 'review_required', 70+ = 'blocked'; LLM failure falls back to 'review_required'
- Creates PluginSecurityScan database record with all static + LLM results, commits and refreshes
- Files changed:
  - `orchestrator/core/services/plugin_security_scanner.py` (appended PluginScanService class)
- **Learnings for future iterations:**
  - PluginSecurityScan model import done inside method (`from core.models.marketplace_plugins import PluginSecurityScan`) to avoid circular imports at module load time
  - Pydantic `model_dump()` converts finding objects to dicts for JSONB column storage
  - Static findings with severity='critical' trigger auto-block — no need to spend LLM tokens on obviously malicious plugins
  - Service classes follow constructor injection: `__init__(self, db)` with `self.db = db`, then `self.db.add()` / `self.db.commit()` / `self.db.refresh()`
---

## 2026-02-04 - US-008
- Implemented PluginUploadService class with full upload workflow: validate zip, parse manifest, read files, run security scan, upload to S3, create DB records
- Constructor takes db session, MarketplaceS3Service, PluginScanService (dependency injection)
- `upload_plugin()` method handles: zip extraction, manifest validation (slug/name/version required), text file reading for scanning, scan orchestration, S3 upload, MarketplacePlugin record creation, PluginSyncHistory audit record
- Maps scan verdict directly to security_status on the plugin record
- Files changed:
  - `orchestrator/core/services/plugin_upload_service.py` (created)
- **Learnings for future iterations:**
  - zipfile.ZipFile can be initialized from `io.BytesIO(zip_bytes)` — no need for temp files on disk
  - Binary files in zip archives raise `UnicodeDecodeError` on `.decode("utf-8")` — catch and skip for text-only scanning
  - Manifest `contents` dict expected to have keys: skills, commands, agents, hooks (each a list) for count extraction
  - `datetime.utcnow()` used for `completed_at` on PluginSyncHistory — matches the `server_default=func.now()` pattern for started_at
  - Service composes MarketplaceS3Service + PluginScanService via constructor — keeps upload orchestration separate from storage and scanning concerns
---

## 2026-02-04 - US-009
- Implemented 6 admin plugin API endpoints in orchestrator/api/admin_plugins.py:
  - POST /upload — multipart zip upload with PluginUploadService orchestration, returns 202
  - POST /{plugin_id}/approve — sets approval_status, approved_by, approved_at + sync history
  - POST /{plugin_id}/reject — sets rejection_reason from RejectBody + sync history
  - GET /{plugin_id}/scan — returns full ScanResultOut (static + LLM findings)
  - POST /{plugin_id}/deactivate — sets is_active=False + sync history
  - GET /pending — paginated list of pending plugins with scan summary data
- Pydantic models: PluginUploadOut, RejectBody, PendingPluginOut, ScanResultOut
- Admin auth via _assert_admin(ctx) helper (matches marketplace.py pattern)
- Registered router in main.py with import + app.include_router
- Files changed:
  - `orchestrator/api/admin_plugins.py` (created)
  - `orchestrator/main.py` (added import + include_router)
- **Learnings for future iterations:**
  - Admin endpoints use `_assert_admin(ctx)` prefix-underscore helper to avoid name collisions with marketplace.py's `assert_admin`
  - UUID path params work directly with `plugin_id: UUID` type hint — FastAPI handles parsing
  - UploadFile multipart pattern: `file: UploadFile = File(...)`, `source_type: str = Form("upload")` — Form() required for non-file multipart fields
  - Service instantiation in endpoint: import inside function to avoid circular imports at module load time
  - PendingPluginOut joins scan data by loading PluginSecurityScan separately for each plugin (N+1 query — acceptable for admin-only paginated endpoint)
---

## 2026-02-04 - US-010
- Implemented 4 public marketplace plugin API endpoints in orchestrator/api/marketplace_plugins.py:
  - GET /api/marketplace/plugins — list approved+active plugins with category, search, tags, sort (popular/newest/name), pagination
  - GET /api/marketplace/plugins/categories — list all plugin categories ordered by sort_order
  - GET /api/marketplace/plugins/{plugin_id} — full plugin detail including manifest.json from S3
  - GET /api/marketplace/plugins/{plugin_id}/content — full plugin file content from S3 (for AgentFactory)
- Pydantic response models: PluginSummaryOut, PluginDetailOut, PluginContentOut, CategoryOut
- Auth required via get_request_context_hybrid on all endpoints
- Only approved+active plugins returned (filtered in query)
- Registered router in main.py
- Files changed:
  - `orchestrator/api/marketplace_plugins.py` (created)
  - `orchestrator/main.py` (added import + include_router)
- **Learnings for future iterations:**
  - `/categories` endpoint must be defined BEFORE `/{plugin_id}` in the router to avoid FastAPI treating "categories" as a UUID path parameter
  - ARRAY column filtering: `MarketplacePlugin.tags.any(tag)` works for PostgreSQL ARRAY contains-element check
  - S3 manifest loading in detail endpoint uses try/except — gracefully returns null manifest if S3 is unavailable
  - Plugin content endpoint strips S3 prefix to return relative file paths in the response dict
  - Category name resolved via separate query per plugin in list endpoint (acceptable for paginated results with default limit 20)
---

## 2026-02-04 - US-011
- Implemented 3 workspace plugin enablement API endpoints in orchestrator/api/workspace_plugins.py:
  - GET /api/workspaces/{workspace_id}/plugins — list enabled plugins (joined with marketplace_plugins for details)
  - POST /api/workspaces/{workspace_id}/plugins — enable a plugin (validates approved+active, creates junction, increments enable_count)
  - DELETE /api/workspaces/{workspace_id}/plugins/{plugin_id} — disable a plugin (removes junction, decrements enable_count, cascades to remove AgentAssignedPlugin records for workspace agents)
- Pydantic models: EnablePluginBody, WorkspacePluginOut
- Workspace auth: validates ctx.workspace_id == path workspace_id (403 on mismatch)
- Registered router in main.py
- Files changed:
  - `orchestrator/api/workspace_plugins.py` (created)
  - `orchestrator/main.py` (added import + include_router)
  - `scripts/ralph/prd.json` (marked US-011 passes: true)
- **Learnings for future iterations:**
  - Workspace scoping pattern: compare `ctx.workspace_id` (from auth) with path `workspace_id` param, raise 403 on mismatch
  - enabled_by FK is Integer (FK to users.id) — Clerk user IDs are strings, so use try/except int() conversion with None fallback
  - Disable cascade: query Agent.id for workspace agents, then delete AgentAssignedPlugin rows matching those agent_ids + the plugin_id
  - enable_count increment/decrement on MarketplacePlugin must handle None default (use `(plugin.enable_count or 0) + 1`)
  - Separate router file (workspace_plugins.py) keeps prefix `/api/workspaces` clean and avoids conflicts with marketplace_plugins.py prefix `/api/marketplace/plugins`
---

## 2026-02-04 - US-012
- Implemented 2 agent plugin assignment API endpoints in orchestrator/api/agent_plugins.py:
  - GET /api/agents/{agent_id}/plugins — list plugins assigned to an agent (joined with marketplace_plugins for details, ordered by priority)
  - PUT /api/agents/{agent_id}/plugins — replace plugin assignments (body: plugin_ids UUID array, validates all are workspace-enabled)
- Pydantic models: UpdateAgentPluginsBody, AgentPluginOut
- Workspace auth: validates agent.workspace_id matches ctx.workspace_id (403 on mismatch)
- PUT endpoint validates all plugin_ids are enabled for the agent's workspace before assigning
- Priority auto-assigned from list order (enumerate index)
- Registered router in main.py
- Files changed:
  - `orchestrator/api/agent_plugins.py` (created)
  - `orchestrator/main.py` (added import + include_router)
  - `scripts/ralph/prd.json` (marked US-012 passes: true)
- **Learnings for future iterations:**
  - Agent PK is Integer (not UUID) — path param type hint is `agent_id: int`
  - Agent.workspace_id can be None (marketplace agents) — check `if agent.workspace_id and agent.workspace_id != ctx.workspace_id` to allow null workspace agents
  - PUT replace pattern: delete all existing rows with `synchronize_session="fetch"`, then add new ones — simpler than diffing
  - Workspace-enabled plugin validation: query WorkspaceEnabledPlugin.plugin_id as a set, then check each requested plugin_id against it
  - Separate router file (agent_plugins.py) with prefix `/api/agents` keeps endpoints under agent namespace without polluting agents.py
---

## 2026-02-04 - US-013
- Implemented 7 persona API endpoints in orchestrator/api/personas.py:
  - GET /api/personas — list available personas (global + workspace-custom) with category and scope filters
  - GET /api/personas/{persona_id} — get persona details including full system_prompt
  - POST /api/workspaces/{workspace_id}/personas — create custom workspace persona (auto-generates slug from name)
  - PUT /api/workspaces/{workspace_id}/personas/{persona_id} — update workspace persona (partial update)
  - DELETE /api/workspaces/{workspace_id}/personas/{persona_id} — delete workspace persona (clears agent persona refs)
  - PUT /api/agents/{agent_id}/persona — set agent persona (predefined, custom prompt, or clear)
  - GET /api/agents/{agent_id}/persona — get agent's current persona info
- Pydantic models: PersonaOut, PersonaSummaryOut, CreatePersonaBody, UpdatePersonaBody, SetAgentPersonaBody, AgentPersonaOut
- Registered router in main.py
- Files changed:
  - `orchestrator/api/personas.py` (created)
  - `orchestrator/main.py` (added import + include_router)
  - `scripts/ralph/prd.json` (marked US-013 passes: true)
- **Learnings for future iterations:**
  - Router prefix `/api` with mixed sub-paths (`/personas`, `/workspaces/{id}/personas`, `/agents/{id}/persona`) keeps all persona endpoints in one file under one tag
  - Slug generation from name: slugify + check uniqueness, append short uuid hex on conflict
  - Delete persona cascade: update Agent rows to clear persona_id and use_custom_persona where workspace matches
  - Workspace persona visibility: scope=='workspace' personas filtered to ctx.workspace_id in list, 403 in detail view
  - Agent persona has 3 modes: (1) predefined persona_id, (2) custom_prompt with use_custom=true, (3) no persona (both null)
  - agent_id path param is `int` (Agent PK is Integer), persona_id is `UUID`
---

## 2026-02-04 - US-014
- Implemented seed_personas.py with 10 predefined personas across 4 categories: Engineering (3), Sales (3), Marketing (2), Support (2)
- Each persona has: slug, name, description, system_prompt (2-3 sentences), voice_description, category, tags, suggested_temperature, suggested_models, source='manual', scope='global'
- Upsert logic: queries by slug, updates changed fields on existing records, creates new ones
- Registered in load_seed_data.py seed runner alongside existing seeds
- Files changed:
  - `orchestrator/core/seeds/seed_personas.py` (created)
  - `orchestrator/core/database/load_seed_data.py` (added personas seed call)
  - `scripts/ralph/prd.json` (marked US-014 passes: true)
- **Learnings for future iterations:**
  - Seed pattern for ORM upsert: query by unique field (slug), update fields if changed, add new if not found, commit at end
  - seed_system_settings.py pattern (takes db session) is preferred over seed_models.py pattern (manages own session) for composability with load_seed_data.py
  - load_seed_data.py already imports `get_db_session` at top for system settings; persona section uses aliased import `_get_db_session` to avoid shadowing
  - Persona model has no `is_active` default in ORM definition that differs from True, so no need to set it explicitly in seed data
---

## 2026-02-04 - US-015
- Implemented seed_plugin_categories.py with 20 predefined categories across 7 groups: Development (code-review, testing, documentation), DevOps (deployment, monitoring, ci-cd), Marketing (seo, content, analytics), Sales (outreach, crm, prospecting), Support (ticketing, knowledge-base), Data (analysis, visualization, etl), Security (scanning, compliance, audit)
- Each category has: slug, name, description, icon (emoji), sort_order (increments of 10 for easy reordering)
- Upsert logic: queries by slug, updates changed fields on existing records, creates new ones
- Registered in load_seed_data.py seed runner after personas section
- Files changed:
  - `orchestrator/core/seeds/seed_plugin_categories.py` (created)
  - `orchestrator/core/database/load_seed_data.py` (added plugin categories seed call)
  - `scripts/ralph/prd.json` (marked US-015 passes: true)
- **Learnings for future iterations:**
  - load_seed_data.py uses progressively more underscored aliases for get_db_session imports to avoid shadowing: `get_db_session`, `_get_db_session`, `__get_db_session`
  - sort_order in increments of 10 allows inserting new categories between existing ones without renumbering
  - PluginCategory has no parent_id set in seeds — all seeded categories are top-level; subcategories can be added later via API
  - 20 categories (not 7) — the PRD listed 7 groups but meant subcategories within each group
---

## 2026-02-04 - US-016
- Implemented GET /api/agents/{agent_id}/assembled-context endpoint in agent_plugins.py
- AssembledContextOut Pydantic model with: agent_id, model, temperature, system_prompt, persona, plugins_loaded, tools, token_estimate
- Persona section: checks use_custom_persona first, then persona_id lookup, else empty
- Plugin skills loaded from S3 SKILL.md files for each assigned plugin (graceful fallback on S3 errors)
- Tool definitions loaded from AgentAppAssignment + ComposioAppCache (same pattern as _build_agent_response in agents.py)
- Token estimate: len(system_prompt) // 4
- Files changed:
  - `orchestrator/api/agent_plugins.py` (added AssembledContextOut model + assembled-context endpoint)
  - `scripts/ralph/prd.json` (marked US-016 passes: true)
- **Learnings for future iterations:**
  - Agent.model_config is a JSON column returning dict — access keys via `.get()` with isinstance check for safety
  - ComposioAppCache and AgentAppAssignment models hold tool assignments — import from `core.models.composio_cache`
  - S3 service can be instantiated per-request inside endpoint functions (constructor reads from config singleton)
  - Persona.suggested_temperature can override the agent's model_config temperature when a predefined persona is assigned
  - Plugin SKILL.md file detection: check `key.lower().endswith("skill.md")` for naming convention
  - No new router registration needed — endpoint added to existing agent_plugins.py router already in main.py
---

## 2026-02-04 - US-017
- Implemented admin plugin upload UI page at frontend/app/admin/plugins/upload/page.tsx
- Upload method selection: zip file drag-and-drop (via react-dropzone) or GitHub URL input
- File validation: .zip only, max 10MB, shows file name and size after selection
- Upload & Scan button calls POST /api/admin/plugins/upload with multipart FormData
- Scan progress display with 7 phases: uploading, extracting, validating, static analysis, LLM scan, S3 upload, complete
- Security scan results display: overall verdict badge, risk score with color-coded progress bar, LLM analysis summary
- Expandable static findings and LLM findings lists with severity coloring
- Approve/Reject buttons after scan completes; reject prompts for reason
- Added 'admin' page to PAGE_MOCK_CONFIG in api-client.ts
- Files changed:
  - `frontend/app/admin/plugins/upload/page.tsx` (created)
  - `frontend/lib/api-client.ts` (added admin page mock config entry)
  - `scripts/ralph/prd.json` (marked US-017 passes: true)
- **Learnings for future iterations:**
  - No existing admin pages in frontend/app/admin/ — this is the first admin page, establishing the pattern
  - File upload pattern: use `apiClient.getAuthHeaders()` for auth headers, then `fetch()` directly with FormData body (don't set Content-Type — browser sets multipart boundary)
  - react-dropzone accept: `{ 'application/zip': ['.zip'] }` for zip-only validation
  - Progress component supports custom bar color via `[&>div]:bg-color-500` Tailwind arbitrary selector
  - glass-card class provides consistent frosted glass card styling matching marketplace pages
  - MainLayout wraps all pages for sidebar + header; no admin-specific layout needed
---

## 2026-02-04 - US-018
- Implemented admin approval queue UI page at frontend/app/admin/plugins/page.tsx
- Lists all pending plugins from GET /api/admin/plugins/pending with pagination
- Each plugin card shows: name, version, slug, description, upload date, source type, risk score, static findings count, LLM summary
- Individual Approve and Reject buttons per plugin card
- Reject shows inline textarea for rejection reason (not window.prompt) with confirm/cancel
- "View Scan Details" button expands to show full scan results: risk score bar, overall verdict, LLM summary, static findings list, LLM findings list with severity coloring
- Batch approve/reject via checkboxes with select-all header
- Stats bar: pending count, selected count, flagged-for-review count
- Upload Plugin button links to /admin/plugins/upload
- Empty state when no pending plugins
- Calls POST /api/admin/plugins/{id}/approve and /reject endpoints
- Files changed:
  - `frontend/app/admin/plugins/page.tsx` (created)
  - `scripts/ralph/prd.json` (marked US-018 passes: true)
- **Learnings for future iterations:**
  - Admin plugin pages share common types (ScanFinding, ScanResult) — could be extracted to a shared types file if more admin pages are added
  - Checkbox component from @/components/ui/checkbox uses Radix UI pattern: `checked` + `onCheckedChange` (not onChange)
  - Textarea component from @/components/ui/textarea works with standard React `onChange={(e) => setVal(e.target.value)}` pattern
  - Inline reject reason input (Textarea) is better UX than window.prompt — allows multiline, preview, and cancel without browser dialog
  - Batch actions: iterate sequentially with for-of loop (not Promise.all) to avoid overwhelming the backend with concurrent requests
  - Optimistic removal: remove approved/rejected plugins from local state immediately without refetching, but refetch on batch errors to resync
---

## 2026-02-04 - US-019
- Implemented marketplace plugins browse page as a new tab in the marketplace homepage
- Created marketplace-plugins-tab.tsx with full plugin browsing functionality:
  - Featured plugins horizontal scroll section at top (for is_featured plugins)
  - All plugins grid (4-column responsive) with cards showing: name, version, description, category, tags, skills_count, commands_count, token_estimate, enable_count
  - Category filter pills from GET /api/marketplace/plugins/categories
  - Sort dropdown (Popular, Newest, Name) controlling backend sort param
  - Client-side search filter (debounced via parent searchQuery prop)
  - Enable button per card that calls POST /api/workspaces/{id}/plugins
  - Enabled badge on already-enabled plugins (fetched from GET /api/workspaces/{id}/plugins)
  - Security status "Verified" badge for safe plugins
- Added Plugins tab trigger and content to marketplace-homepage.tsx alongside existing tabs
- Files changed:
  - `frontend/components/marketplace/marketplace-plugins-tab.tsx` (created)
  - `frontend/components/marketplace/marketplace-homepage.tsx` (added import, tab trigger, tab content)
  - `scripts/ralph/prd.json` (marked US-019 passes: true)
- **Learnings for future iterations:**
  - Workspace ID for API calls: `localStorage.getItem('last_active_workspace') || localStorage.getItem('last_active_org')` — same pattern as api-client.ts request() method
  - apiClient.get() and apiClient.post() handle auth headers and workspace ID injection automatically
  - Marketplace homepage uses dual Tabs components: one for TabsList (triggers) and separate one for TabsContent — both must share same `value` state
  - Plugin enable is optimistic: update local Set<string> and enable_count in state immediately after success, no refetch needed
  - 409 conflict on enable means plugin is already enabled — handle gracefully with "Already Enabled" toast
  - framer-motion AnimatePresence wraps the grid items for staggered entrance animation (delay capped at 0.5s for large lists)
  - Select component from @/components/ui/select uses Radix UI: Select, SelectTrigger, SelectValue, SelectContent, SelectItem
---

## 2026-02-04 - US-020
- Implemented marketplace plugin detail modal at frontend/components/marketplace/marketplace-plugin-detail-modal.tsx
- Modal shows full plugin details fetched from GET /api/marketplace/plugins/{id}:
  - Header with plugin name, version, featured badge, author, category
  - Security status badge (Verified Safe / Review Required / Pending)
  - Full description + long_description
  - Stats grid: skills count, commands count, token estimate, enable count
  - Included skills list from manifest.contents.skills
  - Included commands list from manifest.contents.commands
  - Recommended models badges
  - Tags display
  - Source information section: author, license, source URL, source type
  - Enable/Disable button in sticky footer
- Integrated modal into marketplace-plugins-tab.tsx:
  - Opens on card click (selectedPluginId state was already wired)
  - Enable action delegates to existing handleEnable function
  - Added handleDisable function for workspace plugin removal
  - Disable closes modal after success
- Follows AnimatePresence overlay pattern from marketplace-app-details-modal.tsx
- Files changed:
  - `frontend/components/marketplace/marketplace-plugin-detail-modal.tsx` (created)
  - `frontend/components/marketplace/marketplace-plugins-tab.tsx` (added import, disable handler, modal render)
  - `scripts/ralph/prd.json` (marked US-020 passes: true)
- **Learnings for future iterations:**
  - Modal follows the AnimatePresence overlay pattern (backdrop + centered Card), not the Dialog/DialogContent pattern — both patterns exist in codebase
  - PluginDetailOut from backend includes `manifest` field (nullable dict) — contents.skills and contents.commands are arrays of {name, description} parsed from manifest.json in S3
  - apiClient.delete() exists and handles auth headers automatically
  - Disable handler needs to close the modal after success to avoid stale isEnabled state
  - selectedPluginId state was already present in plugins tab from US-019 but wasn't wired to a modal — integration was the missing piece
---

## 2026-02-04 - US-021
- Added Persona step (Step 2) to create-agent-modal.tsx, renumbering Model to Step 3, Tools to Step 4, Skills to Step 5
- Three persona modes: No persona (default), Predefined (from API), Custom (textarea)
- Predefined persona selection with category filter dropdown, expandable system prompt preview, and "Selected" badge
- Custom persona with textarea, pre-fill from predefined when switching modes
- Persona data saved after agent creation via PUT /api/agents/{id}/persona endpoint
- Fetches personas from GET /api/personas when modal opens
- Full form reset including persona state on close/success
- Files changed:
  - `frontend/components/agents/create-agent-modal.tsx` (modified — persona step added)
  - `scripts/ralph/prd.json` (marked US-021 passes: true)
- **Learnings for future iterations:**
  - apiClient.request() is a public method on ApiClient that handles auth headers, workspace ID injection, and body serialization automatically — use for direct API calls not covered by named methods
  - apiClient.request() body auto-stringifies objects (not FormData) — pass plain JS objects for JSON body
  - Create-agent modal already had "Step X of 5" text in navigation but only 4 tabs — was pre-planned for a 5th step
  - Persona is set as a post-creation call (like model config) since the create_agent endpoint doesn't accept persona fields directly
  - Pre-fill custom prompt from predefined: useEffect watches personaMode + selectedPersonaId, only fills if customPersonaPrompt is empty to avoid overwriting user edits
  - TabsList grid-cols-5 with shortened label "Config" instead of "Configuration" to fit 5 tabs
---

## 2026-02-04 - US-022
- Added Plugins tab to agent-configuration-modal.tsx for assigning marketplace plugins to agents
- Tab shows workspace-enabled plugins with checkbox toggles for assignment/unassignment
- Each plugin card displays: name, version, description, security badge, category, skills count, commands count, token estimate
- Running total token estimate badge shown in header when plugins are assigned
- Empty state with "Browse Marketplace" link when no workspace plugins enabled
- Optimistic UI: assignment changes persisted immediately via PUT /api/agents/{id}/plugins
- Grid updated from 5 to 6 tabs (General, Resources, Skills, Plugins, Model, Tools)
- Files changed:
  - `frontend/components/agents/agent-configuration-modal.tsx` (modified — added Plugins tab, plugin state, fetch logic, toggle handler)
  - `scripts/ralph/prd.json` (marked US-022 passes: true)
- **Learnings for future iterations:**
  - apiClient.request() handles auth headers + workspace ID + body serialization — preferred over raw fetch for API calls in modal components
  - Plugin assignment uses PUT-replace pattern: sends full array of plugin_ids, backend replaces all assignments (same as US-012 backend pattern)
  - Workspace plugins fetched via GET /api/workspaces/{workspace_id}/plugins returns items with plugin_id (string UUID), not id — use plugin_id for Set tracking
  - Agent plugins fetched via GET /api/agents/{agent_id}/plugins also returns plugin_id field for consistency
  - TabsList grid-cols-6 fits 6 short labels well at max-w-5xl modal width
  - Checkbox disabled state during pluginsSaving prevents double-toggling during API roundtrip
---

## 2026-02-04 - US-023
- Added Persona tab (7th tab) to agent-configuration-modal.tsx for viewing and changing agent persona
- Three persona modes: No persona (default), Predefined (from API), Custom (textarea)
- Fetches personas list from GET /api/personas and current agent persona from GET /api/agents/{id}/persona on modal open
- Predefined persona selection: category dropdown filter, expandable system prompt preview, "Selected" badge
- Custom persona editing: textarea with monospace font, pre-fill from predefined when switching modes
- Save Persona button calls PUT /api/agents/{id}/persona with persona_id, custom_prompt, or clear
- Current persona state summary shown in footer with character count for custom prompts
- Clear persona option via "No Persona" mode selector
- Grid updated from 6 to 7 tabs (General, Persona, Resources, Skills, Plugins, Model, Tools) with space-x-1 for tighter label spacing
- Files changed:
  - `frontend/components/agents/agent-configuration-modal.tsx` (modified — added Persona tab, persona state, fetch logic, save handler)
  - `scripts/ralph/prd.json` (marked US-023 passes: true)
- **Learnings for future iterations:**
  - Persona tab reuses same UI patterns from create-agent-modal Step 2 (US-021) — mode cards, category filter, expandable prompt preview, custom textarea
  - Agent persona API returns: persona_id, persona_name, use_custom_persona, custom_persona_prompt, system_prompt — check use_custom_persona first to determine mode
  - Persona save is independent of the main "Save Changes" button — uses separate handleSavePersona to avoid coupling with general config save flow
  - TabsList grid-cols-7 fits 7 short labels at max-w-5xl with space-x-1 (reduced from space-x-2) for tighter spacing
  - Pre-fill custom prompt from predefined: useEffect watches personaMode + selectedPersonaId, only fills if customPersonaPrompt is empty
  - PersonaMode type defined inline in component since it's only used in this modal (not worth extracting to shared types)
---

## 2026-02-04 - US-024
- Verified all marketplace config env vars already present in config.py (added in US-004): MARKETPLACE_S3_BUCKET, AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY, AWS_REGION, PLUGIN_MAX_UPLOAD_SIZE_MB, PLUGIN_LLM_SCAN_MODEL
- Verified .env.example already has all marketplace env vars with comments (added in US-004)
- Validated config loads correctly on startup via AST parsing and attribute verification
- Validated typecheck passes — all 433 orchestrator Python files parse, all 13 marketplace-specific files pass
- Files changed: none (all config work was completed as part of US-004)
  - `scripts/ralph/prd.json` (marked US-024 passes: true)
- **Learnings for future iterations:**
  - US-004 (S3 service) pre-emptively completed all US-024 acceptance criteria — config vars and .env.example were needed to build the service
  - Config validation without dotenv installed locally: use `ast.parse()` + attribute inspection as typecheck proxy
  - Pre-existing syntax error in orchestrator/modules/tools/services/report_generator.py (unrelated) — don't block on it
---

## 2026-02-04 - US-025
- Added 14 named API client methods for marketplace plugins to frontend/lib/api-client.ts:
  - Marketplace plugins: listPlugins(params), getPluginDetail(pluginId), getPluginContent(pluginId), listPluginCategories()
  - Workspace plugins: getWorkspacePlugins(workspaceId), enablePlugin(workspaceId, pluginId), disablePlugin(workspaceId, pluginId)
  - Agent plugins: getAgentPlugins(agentId), updateAgentPlugins(agentId, pluginIds)
  - Admin plugins: uploadPlugin(formData), approvePlugin(pluginId), rejectPlugin(pluginId, reason), getScanResults(pluginId), getPendingPlugins(page, limit)
- uploadPlugin() uses raw fetch with getAuthHeaders() for multipart FormData (no Content-Type header set)
- All other methods use this.request() with JSON.stringify for bodies following existing codebase patterns
- Typecheck passes (esbuild parse validation — no TypeScript compiler installed)
- Files changed:
  - `frontend/lib/api-client.ts` (added marketplace plugin + admin plugin methods section)
  - `scripts/ralph/prd.json` (marked US-025 passes: true)
- **Learnings for future iterations:**
  - No TypeScript compiler available locally; esbuild can validate TS syntax: `npx esbuild file.ts --bundle=false --outfile=/dev/null --log-level=error`
  - Multipart file upload requires raw fetch with getAuthHeaders() — don't use this.request() which sets Content-Type to application/json
  - listPlugins() uses URLSearchParams for query params with tags as repeated `tags` params (backend expects multiple query params)
  - Named methods follow the pattern: `async methodName(params): return this.request(endpoint, options)`
---
