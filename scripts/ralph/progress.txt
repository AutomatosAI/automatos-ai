# PRD-122: Tool Routing Promotion, Permission Enforcement & First-Class Schemas — Progress Log

## 2026-03-31 — US-001: admin_only field + 6 infra tools gated
- Added `admin_only: bool = False` to ActionDefinition dataclass (after workspace_scoped)
- Marked 6 tools as admin_only=True:
  - actions_monitoring.py: platform_query_loki_logs, platform_query_prometheus, platform_get_alerts, platform_get_logs, platform_list_services
  - actions_workspace.py: platform_get_system_health (registered here, not actions_monitoring.py as PRD assumed)
- to_openai_schema() unaffected (admin_only is metadata only)
- Syntax validation passed

## 2026-03-31 — US-002: Thread caller_context through executor chain
- Added `caller_context: Optional[Dict[str, Any]] = None` to `execute_tool()` in unified_executor.py
- Passed caller_context through both platform_execute dispatcher path and direct platform_* routing
- Updated delegate `_execute_platform_action()` to accept and forward caller_context
- Updated `exec_platform.execute_platform_action()` to accept and pass caller_context to PlatformActionExecutor
- Updated `PlatformActionExecutor.execute()` to accept `caller_context: Dict[str, Any] | None = None`
- All parameters have defaults (None) — existing callers unaffected
- Syntax validation passed for all 3 files
- NOTE: unified_executor.py is at `modules/tools/execution/` not `modules/tools/` as PRD assumed

## 2026-03-31 — US-003: Admin gate + destructive safety check in platform_executor.py
- Added admin_only gate BEFORE requires_confirmation check:
  - Looks up action_def from registry, checks admin_only flag
  - If admin_only: caller must have workspace_role in (owner, admin) OR system_role == admin
  - If caller_context is None (no identity), admin_only actions are denied (fail-closed)
  - Returns {success: False, permission_denied: True, error: ...}
  - Logs denial at WARNING with action name, workspace_id, and sanitized caller_context (no user_id)
- Added destructive safety check AFTER rate limit block:
  - If permission_level == "destructive" and requires_confirmation is False, reject with internal error
  - Logs at ERROR level (misconfigured action)
- Syntax validation passed

## 2026-03-31 — US-004: Filter admin_only tools from non-admin callers
- Added `is_admin: bool = False` param to `get_tools_for_agent()` in tool_router.py
- Added `exclude_admin: bool = False` param to `to_dispatcher_schema()` in action_registry.py
- Added `exclude_admin: bool = False` param to `build_prompt_summary()` in action_registry.py
- build_prompt_summary skips admin_only actions when exclude_admin=True
- get_tools_for_agent passes `exclude_admin=not is_admin` to dispatcher schema
- All params default to False — existing callers unaffected
- Validated: admin actions correctly filtered from summary, dispatcher schema accepts param, is_admin in function signature

## 2026-03-31 — US-005: Fix search_chat_history multi-tenancy bug
- Replaced `WHERE c.user_id = (SELECT id FROM users LIMIT 1)` with workspace-scoped JOIN
- Chats table has no workspace_id column — joined through workspace_members: `JOIN workspace_members wm ON wm.user_id = c.user_id WHERE wm.workspace_id = :workspace_id AND wm.is_active = true`
- Also fixed INTERVAL bug: `INTERVAL ':days days'` doesn't parameterize (days is inside string literal) — replaced with `make_interval(days => :days)`
- workspace_id parameter was already available in the handler function signature
- Syntax validation passed

## 2026-03-31 — US-006: Add promoted field + schema generation methods
- Added `promoted: bool = False` to ActionDefinition dataclass (after admin_only)
- Added `get_promoted()` method to ActionRegistry — returns list of promoted ActionDefinitions
- Added `to_first_class_schemas(exclude_admin=False)` method — returns OpenAI function schemas for promoted actions
  - When exclude_admin=True, admin_only promoted actions are excluded (non-admin callers don't see them)
- All new fields/params have defaults — existing callers unaffected
- Validated with isolated import: promoted field works, get_promoted filters correctly, to_first_class_schemas respects exclude_admin

## 2026-03-31 — US-007: Exclude promoted actions from prompt summary + dispatcher
- Added `exclude_promoted: bool = True` param to `build_prompt_summary()` in action_registry.py
  - When True (default), actions with promoted=True are skipped from the markdown catalog
  - Filter applied alongside existing exclude_admin filter
- Added `exclude_promoted: bool = True` param to `to_dispatcher_schema()` in action_registry.py
  - Parameter accepted for forward compatibility — actual enum filtering will be added in US-012
  - Default True ensures promoted actions will be excluded from dispatcher enum by default
- All params have defaults — existing callers unaffected
- Syntax validation passed (py_compile OK)

## 2026-03-31 — US-008: Mark ~13 high-value actions as promoted=True
- Added promoted=True to 13 actions across 5 files:
  - actions_agents.py (4): platform_list_agents, platform_get_agent, platform_create_agent, platform_update_agent
  - actions_marketplace.py (5): platform_browse_marketplace_agents, platform_browse_marketplace_skills, platform_browse_marketplace_plugins, platform_install_skill, platform_install_plugin
  - actions_workspace.py (2): platform_store_memory, platform_get_system_health (also admin_only=True)
  - actions_analytics.py (1): platform_get_activity_feed
  - actions_search.py (1): platform_search_memory
- Note: platform_store_memory and platform_get_system_health are in actions_workspace.py, not actions_search.py/actions_monitoring.py as PRD assumed
- Validated: 97 total actions, 13 promoted, 6 admin-only. platform_get_system_health is both promoted + admin_only
- Syntax validation passed (py_compile OK for all 5 files)

## 2026-03-31 — US-009: Append promoted schemas in tool_router + consolidate field tools
- Replaced hardcoded _FIELD_TOOL_SCHEMAS block (60 lines) in tool_router.py with `action_registry.to_first_class_schemas(exclude_admin=not is_admin)`
  - All promoted actions now get first-class schemas dynamically from ActionRegistry
  - Admin-only promoted actions excluded for non-admin callers
  - Log line: "Added {n} promoted action schemas"
- Marked platform_field_query and platform_field_inject as promoted=True in actions_field.py
  - platform_field_stability left as non-promoted (not a high-value direct-call tool)
- Field tool schemas now come from ActionDefinition.to_openai_schema() instead of hardcoded dicts
  - Schema structure matches: same type/function/name/description/parameters pattern
  - Descriptions are richer in ActionDefinition (more context for LLM) — improvement, not regression
- Total promoted count: 15 (13 from US-008 + 2 field tools)
- Syntax validation passed (py_compile OK for actions_field.py + tool_router.py)
