US-001 COMPLETED (2026-03-24)
- Added ComplexityTier (SIMPLE/MODERATE/COMPLEX) and BudgetStatus (HEALTHY/WARNING/CRITICAL/EXCEEDED) enums to orchestration_enums.py
- Added complexity, parallel_group, estimated_tokens columns to OrchestrationTask model
- Changed max_concurrent default from 1 to 3 on OrchestrationRun
- Added COMPLEXITY_TOKEN_BUDGET dict to config.py (simple=1000, moderate=4000, complex=8000, synthesis=6000)
- Created Alembic migration prd82c_parallel_schema (depends on prd82b_us009_archive)
- All imports validated, 5 orchestration tests pass
- Pre-existing issues: pgvector not installed locally (test_memory_fixes.py), test_recipe_scheduler.py has import error — both unrelated to 82C

US-002 COMPLETED (2026-03-24)
- Added count_active_tasks(db, run_id) -> int to MissionDispatcher
- Added dispatch_ready(db, run, agents) -> List[DispatchResult] for parallel dispatch up to max_concurrent slots
- Extracted _dispatch_single() from dispatch_next() to share dispatch logic
- Deprecated has_active_task() and dispatch_next() with DeprecationWarning
- dispatch_ready() combines queued/retrying + DependencyResolver.get_ready_tasks() candidates
- Created tests/test_dispatcher_parallel.py with 9 wiring tests (all pass)
- 159 tests pass across non-broken test files, no regressions
- coordinator_service.py:481 still calls dispatch_next() — will be wired to dispatch_ready() in US-003

US-003 COMPLETED (2026-03-24)
- Replaced dispatch_next() with dispatch_ready() in coordinator_service.py _process_run()
- Multiple dispatched tasks execute concurrently via asyncio.gather(return_exceptions=True)
- Per-task exception handling: one failure logs error but doesn't crash tick or prevent reconciliation
- Reconcile phase always runs after dispatch, regardless of task outcomes
- Created tests/test_coordinator_parallel.py with 5 wiring tests:
  - 2 tasks both execute concurrently (call_count == 2)
  - Sequential (max_concurrent=1) still dispatches 1 task
  - No dispatch still reconciles
  - Exception in one task doesn't crash tick
  - dispatch_ready called, not deprecated dispatch_next
- 406 tests pass across non-broken test files, no regressions from US-003

US-004 COMPLETED (2026-03-24)
- Added _detect_complexity(), _count_deliverables(), _estimate_domains() to planner.py
- Added _complexity_to_max_concurrent() mapping: SIMPLE→1, MODERATE→2, COMPLEX→3
- Added max_concurrent field to DecompositionResult dataclass (default 1)
- Wired _detect_complexity() into MissionPlanner.decompose() — runs before template/LLM
- Coordinator sets run.max_concurrent = decomposition.max_concurrent after planning
- Domain matching uses word-boundary for single words, substring for multi-word terms
- Scoring: deliverables>=1(+1), >=3(+1 bonus), domains>=2(+1), >=4(+1 bonus), words>50(+1), attachments(+1)
- Removed "document" from deliverable keywords (too generic, caused false positives)
- Created tests/test_complexity_detection.py with 19 tests (all pass):
  - Unit tests for _count_deliverables, _estimate_domains, _detect_complexity, _complexity_to_max_concurrent
  - Wiring test: complex research goal → max_concurrent >= 2
  - Wiring test: simple summarize goal → max_concurrent == 1
- 424 tests pass across non-broken test files, no regressions from US-004

US-005 COMPLETED (2026-03-24)
- Updated _SYSTEM_PROMPT: removed "sequential" framing, added parallel group + complexity + synthesis instructions
- Updated _OUTPUT_SCHEMA_INSTRUCTIONS: added complexity and parallel_group fields with example showing parallel tasks + synthesis
- Updated _REPLAN_SYSTEM_PROMPT: same parallel/complexity guidance
- Added complexity (str, default "moderate") and parallel_group (Optional[str]) to PlannedTask dataclass
- Updated _parse_plan(): extracts complexity (validated against _VALID_COMPLEXITIES set) and parallel_group from LLM output
- Added parallel_group cross-dependency validation to _validate_plan(): tasks in same group must NOT depend on each other
- Replaced flat token estimate (2000 * task_count) with _estimate_token_budget() using COMPLEXITY_TOKEN_BUDGET dict
- Updated coordinator_service.py: both decompose and replan paths now set complexity, parallel_group, estimated_tokens on OrchestrationTask rows
- Created tests/test_parallel_decomposition.py with 7 tests:
  - Parse plan extracts complexity and parallel_group
  - Defaults complexity to "moderate" when missing or invalid
  - Validates cross-dependent parallel group → error
  - Accepts valid parallel group → no error
  - Token estimate uses complexity (not flat 2000 * count)
  - Wiring: complex goal produces parallel groups + synthesis tasks
- 431 tests pass across non-broken test files, no regressions from US-005

US-006 COMPLETED (2026-03-24)
- Added complexity (str), parallel_group (Optional[str]), depends_on (Optional[List[str]]), task_type (str) fields to TaskTemplate dataclass
- Updated render_template(): uses explicit depends_on from TaskTemplate instead of auto-chaining; assigns sequential temp_ids (task_1..N); includes complexity, parallel_group in output dicts; resolves task_type via TaskType enum
- Rewrote research_and_report: seq 1 parallel research (researcher + search) → seq 2 synthesis → seq 3 analysis → seq 4 draft → seq 5 review (6 tasks)
- Rewrote content_pipeline: seq 1 parallel research (researcher + search) → seq 2 synthesis outline → seq 3 draft → seq 4 synthesis merge → seq 5 review (6 tasks)
- Rewrote competitive_analysis: seq 1 parallel competitor research (search + researcher) → seq 2 synthesis → seq 3 analysis → seq 4 report → seq 5 review (6 tasks)
- Rewrote data_investigation: seq 1 parallel gathering (search + researcher) → seq 2 synthesis → seq 3 analysis → seq 4 report (5 tasks)
- All templates have at least one parallel group with 2+ tasks and at least one synthesis task
- Added 6 wiring tests to test_parallel_decomposition.py:
  - content_pipeline has parallel 'research' group with 2 tasks, synthesis depends on both
  - match_template returns content_pipeline with parallel groups
  - All templates have synthesis tasks
  - All templates have parallel groups with 2+ tasks
  - All rendered templates pass _parse_plan and _validate_plan
  - Rendered tasks include complexity and parallel_group fields
- 13 tests pass in test_parallel_decomposition.py, no regressions
