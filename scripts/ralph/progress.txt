# PRD-129: Workspace Outputs Hub — Progress Log

2026-04-10 — Rebuilt IMPLEMENTATION_PLAN.md for PRD-129 (was stale PRD-122).
2026-04-10 — US-001 complete: created alembic/versions/prd129_deliverables.py
  - Standalone migration (down_revision=None) following prd127/prd76 pattern
  - deliverables table: UUID PK, workspace FK, source/artifact/storage/preview cols
  - `extra` JSONB (not `metadata` — avoids SQLAlchemy Base.metadata conflict)
  - file_size_bytes BIGINT
  - Soft delete via deleted_at
  - 5 indices + UNIQUE partial index uq_deliverables_workspace_path WHERE deleted_at IS NULL
  - Import-tested OK; DB not running locally so `alembic upgrade` deferred to deploy.
2026-04-10 — US-002 complete: services/deliverable_service.py + tests/services/test_deliverable_service.py
  - DeliverableService class with register/list_deliverables/get_deliverable/get_stats/soft_delete
  - Module-level EXTENSION_TO_ARTIFACT (~45 extensions across 9 categories), _slugify, _infer_artifact_type, _humanize_basename
  - register() is idempotent via ON CONFLICT (workspace_id, file_path) WHERE deleted_at IS NULL
    - DO UPDATE uses COALESCE to preserve old values when caller omits them
    - `extra` JSONB merged via `||` operator (preserves prior keys)
    - Returns {success, deliverable_id, created, artifact_type, title} — `created` tells insert vs update
    - NEVER calls WorkspaceClient (confirmed by test_register_does_not_call_workspace_client)
  - list_deliverables() supports artifact_type/source_type/agent_id/date_from/date_to/search filters
    - LEFT JOINs agents for fallback agent_name
    - Clamps limit to [1, 100], excludes deleted_at IS NOT NULL
  - get_deliverable(include_content=True): reads file via WorkspaceClient for text; for images returns content_url
  - get_stats(): total, by_type (count), by_agent (list of {agent_id, agent_name, count})
  - All SQL uses sqlalchemy.text() with bound params. All exceptions logged with exc_info=True.
  - 26 unit tests, all passing (0.75s).
2026-04-10 — US-003 complete: api/deliverables.py + tests/api/test_deliverables_api.py
  - Routes: GET / (list + filters), GET /stats, GET /{id}?include_content=, DELETE /{id}
  - /stats declared BEFORE /{deliverable_id} to avoid FastAPI capturing 'stats' as an id
  - All endpoints use Depends(get_request_context_hybrid); svc scoped to ctx.workspace_id
  - Registered in main.py inside try/except ImportError (mirrors reports router)
  - 12 integration tests pass in 0.48s — covers list default, filter forwarding, limit>100 → 422,
    stats, stats vs id path shadowing, get (found, 404, include_content), delete (success, 404),
    and workspace isolation: X-Workspace-ID header is ignored, svc always gets ctx.workspace_id
  - Test uses FastAPI dependency_overrides + patch('api.deliverables.DeliverableService')
    with sys.modules stubs for jwt/clerk/core.database/core.auth — no DB or env needed.
2026-04-10 — US-004 complete: auto-register deliverables on workspace_write_file + report_service
  - Added AGENT_REGISTERABLE_ARTIFACT_TYPES (report/image/document/slide/spreadsheet/code) to deliverable_service.py
  - exec_workspace.py: new _auto_register_deliverable() helper + _derive_source() that extracts
    source_type/source_id from caller_context (heartbeat_id → 'heartbeat', mission_id → 'mission',
    task_id → 'task', playbook_id → 'playbook', trigger_id → 'trigger', else 'chat').
  - execute_workspace_action() now accepts agent_id + caller_context, calls auto-register on
    successful workspace_write_file. Skips non-registerable extensions (archive/audio/video).
    ALL failures caught via try/except with exc_info=True — never break the write.
  - File size extracted from worker response (size/bytes_written/file_size_bytes/bytes keys).
  - Agent name resolved via cheap DB lookup (SessionLocal + Agent.query).
  - unified_executor.py: plumbed agent_id + caller_context into both workspace action paths
    (platform_execute dispatcher branch + direct workspace_* branch).
  - report_service.create_report(): mirrors reports into deliverables after DB insert with
    artifact_type='report', source_type='heartbeat' (or 'task' if no heartbeat_result_id and
    report_type != 'standup'), source_id from heartbeat_result_id or report_id.
  - 11 unit tests in tests/modules/tools/execution/test_exec_workspace_deliverable.py — all pass.
    Tests use sys.modules stubs for core.workspace_client, core.database.database, core.models.core,
    and services.deliverable_service to avoid pulling pgvector + DB config into test import.
    Covered: _derive_source precedence, successful register, skip non-registerable (.zip),
    register failure does not break write, read_file does not register, default source=chat.

