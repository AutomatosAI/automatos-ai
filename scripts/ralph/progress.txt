
## US-007 — Eval harness --mode graph + holdout evaluation (PRD-139)

Branch: worktree-agent-abb4c6f3 (for merge into ralph/prd-139-tool-routing-graph)

### Files modified
- orchestrator/scripts/eval/tool_routing/prompt_builder.py (+78 lines)
  - Added `build_graph(query, top_k)` -> (prompt, surfaced, is_fallback)
  - Added `_graph()` private method: delegates to GraphRouter.rank_chains(), extracts unique action names, builds filtered prompt summary + chain hint block
  - Updated `build()` to route graph mode
  - Updated `build_tools()` to narrow schema for graph mode
  - Added `_CHAIN_HINT_HEADER` constant
- orchestrator/scripts/eval/tool_routing/run_eval.py (+20 lines)
  - `--mode` accepts 'graph' and 'all' (4 modes: full, filtered, filtered_schema, graph)
  - `graph_fallback` dict tracks no-edges state
  - `get_prompt()` calls `build_graph()` for graph mode
  - `get_tools()` passes ranked_names for graph mode
  - Row output uses effective_mode 'graph (no-edges)' when fallback
- orchestrator/scripts/eval/tool_routing/score.py (+6 lines)
  - mode_order dict: graph=3, graph(no-edges)=4
  - 3 new delta pairs: graph-full, graph-filtered, graph-filtered_schema
  - Report title updated to "PRD-138 + PRD-139"

### Files created
- orchestrator/tests/test_eval_graph_mode.py (24 tests)

### Test results (24 passed)
- TestBuildGraphMode: graph mode accepted, invalid mode raises
- TestBuildGraph: returns 3 elements, multi-action not fallback, single-action is fallback
- TestGraphFallback: empty chains falls back, exception falls back
- TestChainHints: hints present for multi-action, absent for single-action, action names deduplicated
- TestBuildToolsGraph: graph narrows enum, full unchanged, filtered unchanged
- TestScoreGraphMode: aggregate includes graph, accuracy correct, category breakdown includes graph, table sorts graph after filtered_schema, pair diff includes graph deltas, missing graph rows handled
- TestRunEvalModes: all includes graph, standalone graph, valid argparse choice, no-edges label, with-edges label

### Acceptance Criteria status
- AC 1: PASS - prompt_builder.py _graph() mode delegates to GraphRouter
- AC 2: PASS - run_eval.py --mode accepts 'graph' and 'all' (4 modes)
- AC 3: PASS - score.py per-(model, mode) includes graph row + deltas
- AC 4: PASS - Empty graph falls back, reports as 'graph (no-edges)'
- AC 5-9: PENDING - Require OPENROUTER_API_KEY and manual eval run by Gerard
- AC 10: PASS - AST parse + py_compile clean
- AC 11: PASS - 24/24 tests green

---

## US-006 — Bootstrap synthetic telemetry for cold-start pipeline (PRD-139)

Commit: 8134828b2
Branch: worktree-agent-a854fd00 (for merge into ralph/prd-139-tool-routing-graph)

### Files created
- orchestrator/scripts/eval/tool_routing/seed_telemetry.py (370 lines)
- orchestrator/tests/test_seed_telemetry.py (450 lines)

### Files modified
- orchestrator/scripts/eval/tool_routing/README.md (added seed->build->eval pipeline docs)

### Implementation summary
- Loads 47 eval queries from eval_set.jsonl
- Generates 2341 synthetic ToolExecutionLog rows:
  - 47 queries x 42 repetitions = 1974 base rows
  - 367 multi-action turn pairings (used_after signal)
- 3 synthetic agents with category-biased preferences:
  - Agent 9001 (Sentinel-like): heavy reports + memory (27.4% of rows)
  - Agent 9002 (Scout-like): heavy workspace + documents (31.6%)
  - Agent 9003 (Ops-like): balanced, favors agents + missions (41.0%)
- 81.6% success / 18.4% failure ratio
- Multi-action pairs: get_latest_report->submit_report, search_memory->store_memory,
  read_file->write_file, list_agents->get_agent, etc. (45% probability per eligible turn)
- Timestamps spread over 30 days with realistic hour-of-day distribution
- All rows tagged telemetry_source='synthetic' for production exclusion
- Idempotent: DELETE WHERE telemetry_source='synthetic' before INSERT
- CLI: --dry-run (stats only), --verify (calls build_edges after seeding)
- Deterministic with pinned random seed (RANDOM_SEED=139006)

### Test results (23 passed)
- TestRowGeneration: 3 tests (min 2000, all queries, per-query counts)
- TestAgentBias: 4 tests (3 agents used, sentinel reports, scout workspace, ops agents/missions)
- TestStatusDistribution: 2 tests (80% success rate, both statuses)
- TestTelemetrySourceTag: 1 test (all rows tagged synthetic)
- TestMultiActionTurns: 3 tests (turns exist, known pairs, timestamp ordering)
- TestDataQuality: 5 tests (app_name, exec time, timestamps, workspace_id, turn_id)
- TestIdempotency: 1 test (clears prior synthetic on re-run)
- TestEdgePipelineIntegration: 3 tests (produces edges, all affinity types, above sample floor)
- TestDeterminism: 1 test (pinned base_time = identical output)

Acceptance Criteria met: All 9 criteria satisfied.

---

## US-004 — GraphRouter service (PRD-139)

Commit: a52074d98
Branch: worktree-agent-af63a813 (for merge into ralph/prd-139-tool-routing-graph)

### Files created
- orchestrator/modules/tools/discovery/graph_router.py (290 lines)
- orchestrator/tests/test_graph_router.py (510 lines)

### Implementation summary
- GraphRouter class wraps ActionSemanticIndex.rank_actions() for entry node selection (top 5)
- Expands entry nodes through tool_routing_edges (used_after, single hop)
- Queries tool_routing_affinities for intent/agent boosts
- Scoring: cosine_score * edge_confidence + affinity_boosts
- Hard caps: depth 2, max 50 expanded nodes
- Per-agent filtering: agent_id with >= TOOL_ROUTING_GRAPH_AGENT_SAMPLE_FLOOR samples uses agent-specific affinities; else global
- Empty graph fallback: returns single-action chains from embedding scores
- DB error fallback: logs warning, returns single-action chains
- Singleton via get_graph_router() factory (matches get_action_semantic_index pattern)
- Cache: traversal results cached via CacheService Redis (TTL 5 minutes), keyed on (query_hash, agent_id, top_k)
- Config vars read via safe getattr with defaults (TOOL_ROUTING_GRAPH_MIN_CONFIDENCE=0.6, TOOL_ROUTING_GRAPH_AGENT_SAMPLE_FLOOR=50)

### Test results (14 passed)
- AC-8: chains sorted descending, chain beats weak single, tuple shape, affinity boosts score
- AC-9: empty edges returns singles, order matches cosine, no error on single/zero entries
- AC-10: below floor uses global graph, above floor uses agent edges
- AC-11: low confidence excluded, all below floor returns singles only
- Singleton: returns same instance
- DB error: falls back to single-action chains without error

---

## US-005 — PlatformActionsSection graph integration (PRD-139)

Commit: 03f879e9c
Branch: worktree-agent-aa483ee7 (for merge into ralph/prd-139-tool-routing-graph)

### Files modified
- orchestrator/modules/context/sections/platform_actions.py (+75 lines)
- orchestrator/config.py (+3 lines)

### Files created
- orchestrator/tests/test_platform_actions_section_graph.py (8 tests)

### Implementation summary
- Added _graph_routing_enabled() helper (reads config.TOOL_ROUTING_GRAPH)
- Added _build_graph_filtered(query, ctx): calls GraphRouter.rank_chains(), collects action names, builds chain hints + filtered summary
- Added _build_chain_hints(chains): renders hint block for multi-action sequences only
- Modified render(): graph path gated by flag, exceptions fall back to embedding
- 3 config vars: TOOL_ROUTING_GRAPH, TOOL_ROUTING_GRAPH_MIN_CONFIDENCE, TOOL_ROUTING_GRAPH_AGENT_SAMPLE_FLOOR
- Existing PRD-138 test suite (10 tests) passes unchanged

### Test results (8 passed)
- graph path called when flag on + chains populated
- agent_id passed from ctx.kwargs, None when absent
- single-action chains skip hint block
- graph raises -> embedding fallback
- graph empty -> embedding fallback
- flag off -> graph never called
- semantic routing off -> neither called
