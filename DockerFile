FROM python:3.11-slim
WORKDIR /app
COPY mcp_bridge.py /app/
COPY requirements.txt /app/
RUN pip install --no-cache-dir -r requirements.txt
CMD ["uvicorn", "mcp_bridge:app", "--host", "0.0.0.0", "--port", "5678"]