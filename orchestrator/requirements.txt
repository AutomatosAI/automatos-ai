# Core FastAPI dependencies
fastapi>=0.115.0,<1.0.0
uvicorn[standard]==0.24.0
websockets==12.0

# Database
sqlalchemy==2.0.23
alembic==1.12.1
psycopg2-binary==2.9.9
asyncpg==0.29.0
pgvector==0.2.4
sqlparse>=0.5.4
greenlet>=3.0.0

# Data validation
pydantic>=2.7.4,<3.0.0
pydantic-settings>=2.1.0
email-validator>=2.1.0

# Security
python-jose[cryptography]>=3.4.0
passlib[bcrypt]==1.7.4
python-multipart>=0.0.18

# File processing
pypdf>=4.0.0,<7.0.0
python-docx==1.1.0
markdown==3.5.1
bleach>=6.0
beautifulsoup4==4.12.2
python-magic==0.4.27
pytesseract==0.3.10

# System monitoring (essential for our dashboard)
psutil==5.9.6

# Scheduling (heartbeat service)
apscheduler>=3.10

# Network & async
aiofiles==23.2.1
aiohttp>=3.9.0

# Configuration
python-dotenv==1.0.0
requests>=2.32.5
httpx>=0.27.0,<1.0.0
tenacity==8.2.3
cachetools>=5.3.0

# Development/Testing
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-timeout==2.3.1  # per-test timeout net for CI (PRD-142 W2-S3); kills hangs fast
black==23.11.0
isort==5.12.0

# Redis & SSH
redis>=4.5.0

# Qdrant — PRD-108 shared mission field
qdrant-client>=1.12.0
paramiko>=2.9.0
jsonschema>=4.0.0

# Document processing & OCR
pdfplumber>=0.11.7
pdfminer.six>=20251230
camelot-py==0.11.0
opencv-python-headless>=4.10.0  # Upgraded for numpy 2.x compat (needed by camelot)

# AI provider clients (API-based, no local models)
tiktoken>=0.5.0  # Token counting for context management (RAG, context_guard)
openai>=1.10.0
anthropic>=0.40.0,<1.0.0
google-generativeai>=0.8.0  # Google Gemini native API

# Data & ML (actively used in multi-agent optimization + RAG)
numpy>=1.26.2,<3.0.0
pandas>=2.2.0,<3.0
scikit-learn>=1.5.0,<2.0.0
nltk>=3.9.3
networkx==3.1

# RAG chunking (only text-splitters + core are actually imported)
langchain-text-splitters==1.0.0
langchain-core>=1.0.2,<2.0.0

# CodeGraph dependencies (PRD-11 + PRD-62)
tree-sitter>=0.23.0
tree-sitter-language-pack>=0.2.0  # 14+ languages in one package
tree-sitter-python>=0.23.0    # Fallback if language-pack unavailable
tree-sitter-javascript>=0.20.3
tree-sitter-typescript>=0.20.5
GitPython>=3.1.41

# PRD-37: SaaS Foundation
PyJWT>=2.8.0
cryptography>=43.0.0
slowapi>=0.1.9

# PRD-36: Composio Integration (500+ tools, Tool Router, Triggers)
# Pin BOTH packages to matching versions — composio-openai's `composio` dep is
# unpinned upstream, so leaving it implicit lets the resolver pick a newer core
# that breaks 0.11.x interfaces (caused /api/v3/tools 401s on 2026-04-29).
composio==0.12.0
composio-openai==0.12.0

# PRD-42: Cloud Document Sync with S3 Vectors
boto3>=1.39.5

# PRD-63: Document Generation Module
weasyprint>=62.0
docxtpl>=0.18.0
xlsxwriter>=3.2.0
gotenberg-client>=0.8.0
prometheus-client>=0.20.0

# PRD-126: Business Knowledge Graph (Graphify Integration)
graphifyy[leiden]>=0.3.28
