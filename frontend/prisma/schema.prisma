generator client {
    provider = "prisma-client-js"
    binaryTargets = ["native", "linux-musl-arm64-openssl-3.0.x"]
    // Let Prisma manage output in node_modules; avoid hard-coded host-specific paths
}

datasource db {
    provider = "postgresql"
    url      = env("DATABASE_URL")
}
